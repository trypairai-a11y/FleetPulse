---
phase: 05-mobile-gps-beacon
plan: 02
type: execute
wave: 2
depends_on: ["05-00"]
files_modified:
  - backend/package.json
  - backend/.env.example
  - backend/src/services/r2Service.ts
  - backend/src/services/activePlatformAttribution.ts
  - backend/src/middleware/agentRateLimit.ts
  - backend/src/routes/agent.ts
  - backend/src/__tests__/mocks/config.ts
autonomous: true
requirements:
  - REQ-mobile-always-on-gps
  - REQ-mobile-active-platform-detection
  - REQ-mobile-delivery-photo-capture

must_haves:
  truths:
    - "POST /api/agent/location accepts deviceId+driverId, writes LocationLog rows, upserts CourierOnlineSession with fresh lastGpsAt/Lat/Lng"
    - "POST /api/agent/location dedupes duplicate idempotencyKey within a 5-min in-process window"
    - "POST /api/agent/location is rate-limited per-deviceId (200 / 5min) — 429 on overflow"
    - "POST /api/agent/upload-url issues a presigned PUT URL keyed `{tenantId}/{orderId}/{deviceId}/{ts}.jpg` with 5-min expiry"
    - "POST /api/agent/upload-url rejects contentType not in {image/jpeg, image/png}"
    - "POST /api/agent/upload-url is rate-limited per-deviceId (30 / 10min) — 429 on overflow"
    - "POST /api/agent/delivery-photo creates an OrderEvent with action=DELIVERY_PHOTO + metadata={photoKey, lat, lng}"
    - "POST /api/agent/delivery-photo rejects 403 when key prefix tenant != caller's tenant"
    - "POST /api/agent/delivery-photo validates lat/lng ranges; rejects 400 if out of bounds"
    - "POST /api/agent/heartbeat accepts batteryLevel, isLowPowerMode, platformGuess (extends existing route)"
    - "resolveActivePlatform returns HIGH/order_event when OrderEvent in last 30min, MEDIUM/shift when shift IN_PROGRESS, LOW/driver_default otherwise; UNKNOWN if driver missing"
    - "resolveActivePlatform queries are tenant-scoped — cross-tenant shifts/order events are never returned"
    - "All Wave 0 backend RED tests are now GREEN"
    - "lint:tenant scope passes for all new files"
  artifacts:
    - path: "backend/src/services/r2Service.ts"
      provides: "presignPutUrl, presignGetUrl using @aws-sdk/client-s3"
      min_lines: 30
    - path: "backend/src/services/activePlatformAttribution.ts"
      provides: "resolveActivePlatform({tenantId, driverId, at, mobileHint}) — tiered evidence chain"
      min_lines: 60
    - path: "backend/src/middleware/agentRateLimit.ts"
      provides: "agentLocationRateLimit + agentUploadRateLimit"
      min_lines: 20
    - path: "backend/src/routes/agent.ts"
      provides: "EXTENDED: location with idempotency+rate-limit+CourierOnlineSession upsert; new upload-url + delivery-photo endpoints; heartbeat extended"
      contains: "upload-url"
    - path: "backend/.env.example"
      provides: "R2_ENDPOINT, R2_ACCESS_KEY_ID, R2_SECRET_ACCESS_KEY, R2_BUCKET vars"
      contains: "R2_BUCKET"
    - path: "backend/package.json"
      provides: "@aws-sdk/client-s3, @aws-sdk/s3-request-presigner installed"
      contains: "@aws-sdk/client-s3"
  key_links:
    - from: "backend/src/routes/agent.ts POST /upload-url"
      to: "backend/src/services/r2Service.ts presignPutUrl"
      via: "import presignPutUrl from ../services/r2Service"
      pattern: "presignPutUrl"
    - from: "backend/src/routes/agent.ts POST /location"
      to: "backend/src/middleware/agentRateLimit.ts agentLocationRateLimit"
      via: "router.post('/location', agentLocationRateLimit, handler)"
      pattern: "agentLocationRateLimit"
    - from: "backend/src/routes/agent.ts POST /location"
      to: "prisma.courierOnlineSession (upsert pattern via findFirst/create/update)"
      via: "after locationLog.createMany, upsert session row"
      pattern: "courierOnlineSession"
    - from: "backend/src/services/activePlatformAttribution.ts"
      to: "prisma.orderEvent + prisma.shift + prisma.driver (all tenant-scoped)"
      via: "where tenantId filter on every query"
      pattern: "tenantId"
---

<objective>
Wave 2 — Backend foundation. Ship the four backend additions Phase 5 needs:
1. Extend `POST /api/agent/location` with idempotency + rate-limit + CourierOnlineSession upsert
2. Add `POST /api/agent/upload-url` for presigned R2 PUT URLs
3. Add `POST /api/agent/delivery-photo` for delivery-photo metadata persistence (writes OrderEvent rows)
4. Ship `services/activePlatformAttribution.ts` with the tiered evidence chain
5. Extend `POST /api/agent/heartbeat` to accept batteryLevel/isLowPowerMode/platformGuess
6. Ship `services/r2Service.ts` (presigned URL generation) and `middleware/agentRateLimit.ts`

Purpose: Mobile (Wave 1) is now producing well-formed payloads. The backend must accept them with idempotency (Pitfall 1 server-side defense in depth), rate-limit them (DoS protection), and persist them across the right tables. Active-platform attribution lands as a reusable service so Floor (Phase 7) and Phase 1's `liveFleetStatus` consumer can call it. R2 presigned URLs replace the multer-on-disk delivery-photo path that wouldn't survive Vercel's `/tmp` ephemerality.

Output:
- 3 new services (`r2Service`, `activePlatformAttribution`) + 1 new middleware (`agentRateLimit`)
- 2 new routes added to `agent.ts` + 2 existing routes extended
- 4 new env vars in `.env.example`
- 2 new npm packages installed
- All 5 Wave 0 backend RED tests GREEN
- `npm run lint:tenant` passes
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-mobile-gps-beacon/05-RESEARCH.md
@.planning/phases/05-mobile-gps-beacon/05-00-SUMMARY.md
@backend/src/routes/agent.ts
@backend/prisma/schema.prisma
@backend/src/__tests__/agent/locationIngest.test.ts
@backend/src/__tests__/agent/presignFlow.test.ts
@backend/src/__tests__/agent/deliveryPhoto.test.ts
@backend/src/__tests__/services/activePlatformAttribution.test.ts
@backend/src/__tests__/middleware/agentRateLimit.test.ts
@backend/src/__tests__/setup.ts
@backend/src/__tests__/mocks/config.ts
@backend/.env.example
@backend/package.json

<interfaces>
<!-- Contracts that Wave 0 RED tests assert. -->

backend/src/services/r2Service.ts MUST export:
  presignPutUrl(key: string, contentType: string, expiresInSec?: number): Promise of string URL
  presignGetUrl(key: string, expiresInSec?: number): Promise of string URL

The S3Client is constructed at first call (lazy) using R2_ENDPOINT + R2_ACCESS_KEY_ID +
R2_SECRET_ACCESS_KEY + R2_BUCKET env vars; if any are missing, presign* throws a clear
"R2 not configured" error rather than crashing during module import. This way existing
endpoints keep working in dev without R2 keys.

backend/src/services/activePlatformAttribution.ts MUST export:
  type Platform from @prisma/client
  ActivePlatformResult { platform: Platform | null, confidence: 'HIGH'|'MEDIUM'|'LOW'|'UNKNOWN', source: 'order_event'|'shift'|'driver_default'|'mobile_hint'|'none', evidence: { id?, at?, text? } }
  resolveActivePlatform(args: { tenantId, driverId, at?, mobileHint? }) → Promise of ActivePlatformResult

Tiered evidence chain (per orchestrator decision — uses existing OrderEvent model, NOT a new model):
- Tier 1 (HIGH/order_event): prisma.orderEvent.findFirst with where: { tenantId, timestamp: { gte: at-30min, lte: at }, OR: [ { operatorId: driverId }, { metadata: { path: ['driverId'], equals: driverId } } ] }, orderBy: { timestamp: 'desc' }. Platform comes from metadata.platform if present (mobile sends platformGuess on delivery-photo OrderEvents); otherwise null on the result.
- Tier 2 (MEDIUM/shift): prisma.shift.findFirst with where: { tenantId, driverId, status: 'IN_PROGRESS', actualStart: { lte: at } }, orderBy: { actualStart: 'desc' }. Returns shift.platform.
- Tier 3 (LOW/driver_default OR MEDIUM/mobile_hint): prisma.driver.findUnique with where: { id: driverId }, select: { tenantId, platform }. If driver.tenantId !== tenantId → return UNKNOWN. If mobileHint matches driver.platform → MEDIUM/mobile_hint, else LOW/driver_default.

backend/src/middleware/agentRateLimit.ts MUST export:
  agentLocationRateLimit (express RequestHandler — 200 / 5 min, keyed by req.body.deviceId)
  agentUploadRateLimit (express RequestHandler — 30 / 10 min, keyed by req.body.deviceId)

backend/src/routes/agent.ts CHANGES:
- POST /location MUST: (a) attach agentLocationRateLimit; (b) reject 404 if deviceId not found;
  (c) dedupe by (deviceId, idempotencyKey) in an in-process Map TTL=5min — drop dupes from the
  payload before insert; (d) prisma.locationLog.createMany; (e) upsert CourierOnlineSession.
  NOTE: CourierOnlineSession has @@index([tenantId, isOnline]) but NO unique constraint suitable
  for prisma.upsert. Implementation MUST use a findFirst-then-update-or-create pattern:
    const existing = await prisma.courierOnlineSession.findFirst({ where: { tenantId, driverId, isOnline: true } });
    if (existing) await prisma.courierOnlineSession.update({ where: { id: existing.id }, data: { lastGpsAt, lastGpsLat, lastGpsLng } });
    else          await prisma.courierOnlineSession.create({ data: { tenantId, driverId, isOnline: true, startTime: new Date(), lastGpsAt, lastGpsLat, lastGpsLng } });
- POST /upload-url (NEW) — see Action below.
- POST /delivery-photo (NEW) — writes OrderEvent.
- POST /heartbeat — accept additional fields, persist batteryLevel, ignore platformGuess for
  storage (it is read fresh at attribution time, not persisted).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Install AWS SDK + R2 env vars + r2Service + agentRateLimit</name>
  <files>backend/package.json, backend/.env.example, backend/src/services/r2Service.ts, backend/src/middleware/agentRateLimit.ts</files>

  <read_first>
    - backend/package.json (verify NO @aws-sdk/* yet)
    - backend/.env.example (where to add the 4 R2 vars)
    - .planning/phases/05-mobile-gps-beacon/05-RESEARCH.md "Architecture Patterns §5" + "Additional helper: rate-limit middleware"
    - backend/src/__tests__/middleware/agentRateLimit.test.ts (Wave 0 RED contract)
  </read_first>

  <behavior>
    - r2Service.presignPutUrl returns a string starting with "https://" when R2 env vars are present
    - r2Service throws "R2 not configured" when R2_ENDPOINT or any required env var is missing
    - agentLocationRateLimit limits to 200 / 5min keyed by req.body.deviceId
    - agentUploadRateLimit limits to 30 / 10min keyed by req.body.deviceId
    - Different deviceIds get independent rate-limit buckets
  </behavior>

  <action>
    Step 1 — Install AWS SDK packages:
      cd backend && npm install @aws-sdk/client-s3@^3.700.0 @aws-sdk/s3-request-presigner@^3.700.0

    Step 2 — Add four R2 env vars to backend/.env.example under a new "Photo storage (Cloudflare R2)" section. Append after the existing "Twilio (SMS / WhatsApp)" block:

      # --- Photo storage (Cloudflare R2 — S3-compatible) ---
      # Required for delivery-photo upload (POST /api/agent/upload-url + /api/agent/delivery-photo)
      # Bucket holds private courier delivery photos; access only via short-lived presigned URLs.
      R2_ENDPOINT=""
      R2_ACCESS_KEY_ID=""
      R2_SECRET_ACCESS_KEY=""
      R2_BUCKET=""

    Step 3 — Create backend/src/services/r2Service.ts (verbatim shape):

      import { S3Client, PutObjectCommand, GetObjectCommand } from "@aws-sdk/client-s3";
      import { getSignedUrl } from "@aws-sdk/s3-request-presigner";

      function getClient(): { s3: S3Client; bucket: string } {
        const endpoint = process.env.R2_ENDPOINT;
        const accessKeyId = process.env.R2_ACCESS_KEY_ID;
        const secretAccessKey = process.env.R2_SECRET_ACCESS_KEY;
        const bucket = process.env.R2_BUCKET;
        if (!endpoint || !accessKeyId || !secretAccessKey || !bucket) {
          throw new Error("R2 not configured: set R2_ENDPOINT, R2_ACCESS_KEY_ID, R2_SECRET_ACCESS_KEY, R2_BUCKET");
        }
        return {
          s3: new S3Client({ region: "auto", endpoint, credentials: { accessKeyId, secretAccessKey } }),
          bucket,
        };
      }

      export async function presignPutUrl(key: string, contentType: string, expiresInSec = 300): Promise<string> {
        const { s3, bucket } = getClient();
        const cmd = new PutObjectCommand({ Bucket: bucket, Key: key, ContentType: contentType });
        return getSignedUrl(s3, cmd, { expiresIn: expiresInSec });
      }

      export async function presignGetUrl(key: string, expiresInSec = 3600): Promise<string> {
        const { s3, bucket } = getClient();
        const cmd = new GetObjectCommand({ Bucket: bucket, Key: key });
        return getSignedUrl(s3, cmd, { expiresIn: expiresInSec });
      }

    The S3Client is constructed lazily inside getClient(). This means `import "./r2Service"` does NOT crash on dev machines without R2 keys — only the actual presignPutUrl call fails. Tests that don't exercise these routes need no R2 env vars.

    Step 4 — Create backend/src/middleware/agentRateLimit.ts per RESEARCH.md "Additional helper":

      import rateLimit from "express-rate-limit";
      import type { Request } from "express";

      const keyFromBody = (req: Request) => String((req.body as any)?.deviceId ?? req.ip);

      export const agentLocationRateLimit = rateLimit({
        windowMs: 5 * 60 * 1000,
        max: 200,
        keyGenerator: keyFromBody,
        standardHeaders: true,
        legacyHeaders: false,
        message: { error: "rate-limited" },
      });

      export const agentUploadRateLimit = rateLimit({
        windowMs: 10 * 60 * 1000,
        max: 30,
        keyGenerator: keyFromBody,
        standardHeaders: true,
        legacyHeaders: false,
        message: { error: "rate-limited" },
      });

    Step 5 — Run the agentRateLimit RED test in isolation:
      cd backend && npx jest src/__tests__/middleware/agentRateLimit.test.ts

    Expect: 2/2 GREEN.

    Step 6 — Run lint:tenant. The new files don't use prisma so they pass trivially:
      cd backend && npm run lint:tenant
  </action>

  <verify>
    <automated>cd /Users/mac/Documents/Darb/backend && npx jest src/__tests__/middleware/agentRateLimit.test.ts 2>&1 | grep -E "Tests:.*passed" | head -1</automated>
  </verify>

  <done>
    - backend/package.json has @aws-sdk/client-s3 + @aws-sdk/s3-request-presigner dependencies
    - backend/.env.example contains the four R2_* env vars
    - backend/src/services/r2Service.ts exists with presignPutUrl + presignGetUrl
    - backend/src/middleware/agentRateLimit.ts exists with both limiters
    - 2/2 agentRateLimit tests GREEN
    - lint:tenant exits 0
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement activePlatformAttribution service</name>
  <files>backend/src/services/activePlatformAttribution.ts, backend/src/__tests__/mocks/config.ts</files>

  <read_first>
    - backend/src/__tests__/services/activePlatformAttribution.test.ts (the contract — 6 tests)
    - backend/prisma/schema.prisma OrderEvent (line 1833) + Shift (line 668) + Driver (line 482) — verify field names
    - .planning/phases/05-mobile-gps-beacon/05-RESEARCH.md "Architecture Patterns §4: Active-Platform Attribution" (lines 549-629)
  </read_first>

  <behavior>
    - Tier 1: returns HIGH/order_event when OrderEvent exists in last 30 min for this tenant + driver
    - Tier 2: returns MEDIUM/shift when shift status=IN_PROGRESS for this tenant + driver
    - Tier 3 default: returns LOW/driver_default
    - Tier 3 with hint match: returns MEDIUM/mobile_hint
    - UNKNOWN when driver not found OR driver tenant mismatch
    - Cross-tenant shift in another tenant does NOT match (tenant scoping enforced)
  </behavior>

  <action>
    Step 1 — Verify backend/src/__tests__/mocks/config.ts exposes all delegates the tests assert on:
      prisma.orderEvent.findFirst
      prisma.shift.findFirst
      prisma.driver.findUnique
    If any is missing, add it as `jest.fn()`. The Wave 0 plan already created these stubs, but
    re-confirm before writing the implementation.

    Step 2 — Create backend/src/services/activePlatformAttribution.ts:

      import { prisma } from "../config";
      import type { Platform } from "@prisma/client";

      export interface ActivePlatformResult {
        platform: Platform | null;
        confidence: "HIGH" | "MEDIUM" | "LOW" | "UNKNOWN";
        source: "order_event" | "shift" | "driver_default" | "mobile_hint" | "none";
        evidence: { id?: string; at?: Date; text?: string };
      }

      export interface ResolveArgs {
        tenantId: string;
        driverId: string;
        at?: Date;
        mobileHint?: Platform | null;
      }

      const THIRTY_MIN_MS = 30 * 60 * 1000;

      export async function resolveActivePlatform(args: ResolveArgs): Promise<ActivePlatformResult> {
        const at = args.at ?? new Date();
        const since = new Date(at.getTime() - THIRTY_MIN_MS);

        // Tier 1 — Recent OrderEvent (HIGH).
        // OrderEvent has no driverId column — use operatorId (the mobile sets operatorId=driverId
        // on delivery-photo events) AND/OR the metadata.driverId path.
        const recentEvent = await prisma.orderEvent.findFirst({
          where: {
            tenantId: args.tenantId,
            timestamp: { gte: since, lte: at },
            OR: [
              { operatorId: args.driverId },
              { metadata: { path: ["driverId"], equals: args.driverId } as any },
            ],
          },
          orderBy: { timestamp: "desc" },
          select: { id: true, timestamp: true, metadata: true },
        }).catch(() => null);

        if (recentEvent) {
          // Platform may live in metadata.platform — extract or fall through to null platform.
          const meta = (recentEvent.metadata ?? {}) as Record<string, any>;
          const platform = (typeof meta.platform === "string" ? meta.platform : null) as Platform | null;
          return {
            platform,
            confidence: "HIGH",
            source: "order_event",
            evidence: { id: recentEvent.id, at: recentEvent.timestamp },
          };
        }

        // Tier 2 — IN_PROGRESS Shift (MEDIUM).
        const activeShift = await prisma.shift.findFirst({
          where: {
            tenantId: args.tenantId,
            driverId: args.driverId,
            status: "IN_PROGRESS",
            actualStart: { lte: at },
          },
          orderBy: { actualStart: "desc" },
          select: { id: true, platform: true, actualStart: true },
        });

        if (activeShift) {
          return {
            platform: activeShift.platform,
            confidence: "MEDIUM",
            source: "shift",
            evidence: { id: activeShift.id, at: activeShift.actualStart ?? undefined },
          };
        }

        // Tier 3 — Driver default (LOW) or mobile_hint match (MEDIUM).
        const driver = await prisma.driver.findUnique({
          where: { id: args.driverId },
          select: { tenantId: true, platform: true },
        });

        if (!driver || driver.tenantId !== args.tenantId) {
          return {
            platform: null,
            confidence: "UNKNOWN",
            source: "none",
            evidence: { text: driver ? "tenant mismatch" : "driver not found" },
          };
        }

        const matchesHint = args.mobileHint != null && args.mobileHint === driver.platform;
        return {
          platform: driver.platform,
          confidence: matchesHint ? "MEDIUM" : "LOW",
          source: matchesHint ? "mobile_hint" : "driver_default",
          evidence: { text: matchesHint ? "default + mobile-hint match" : "driver.platform default" },
        };
      }

    Step 3 — Run the RED tests:
      cd backend && npx jest src/__tests__/services/activePlatformAttribution.test.ts

    Expect: 6/6 GREEN.

    Step 4 — Run lint:tenant:
      cd backend && npm run lint:tenant
    Expect: 0 violations on activePlatformAttribution.ts (it always passes tenantId in every where clause).
  </action>

  <verify>
    <automated>cd /Users/mac/Documents/Darb/backend && npx jest src/__tests__/services/activePlatformAttribution.test.ts 2>&1 | grep -E "Tests:.*passed" | head -1</automated>
  </verify>

  <done>
    - activePlatformAttribution.ts exists with the tiered chain
    - 6/6 tests GREEN
    - lint:tenant scope still passes (tenantId is in every Prisma where clause)
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Extend agent.ts with location idempotency + upload-url + delivery-photo + heartbeat fields</name>
  <files>backend/src/routes/agent.ts</files>

  <read_first>
    - backend/src/routes/agent.ts (existing endpoints — extend, don't rewrite)
    - backend/src/__tests__/agent/locationIngest.test.ts, presignFlow.test.ts, deliveryPhoto.test.ts (the contracts)
    - backend/prisma/schema.prisma OrderEvent + CourierOnlineSession + LocationLog models
    - backend/src/services/r2Service.ts (Wave 2 task 1)
    - backend/src/middleware/agentRateLimit.ts (Wave 2 task 1)
  </read_first>

  <behavior>
    - POST /location: rate-limited; rejects 404 unknown deviceId; dedupes idempotencyKey within 5min; writes LocationLog rows; upserts CourierOnlineSession with fresh lastGpsAt/Lat/Lng; updates Device.lastLatitude/lastLongitude/lastSeen
    - POST /upload-url: rate-limited; rejects bad contentType; rejects 404 unknown deviceId; returns presigned URL with key matching tenantA/orderX/devY/timestamp.jpg
    - POST /delivery-photo: rejects 403 on tenant-prefix mismatch; validates lat/lng ranges; writes OrderEvent action=DELIVERY_PHOTO with metadata { photoKey, latitude, longitude }
    - POST /heartbeat: accepts and persists new optional fields (batteryLevel as Int, isLowPowerMode ignored for storage, platformGuess ignored)
  </behavior>

  <action>
    Step 1 — At top of backend/src/routes/agent.ts add new imports:

      import { presignPutUrl } from "../services/r2Service";
      import { agentLocationRateLimit, agentUploadRateLimit } from "../middleware/agentRateLimit";

    Step 2 — Add an in-process dedup Map at module scope (above the router definitions):

      // Idempotency dedup for /location: Map<`${deviceId}:${idempotencyKey}`, expiresAt>
      // 5-min sliding window. In-process only — multi-instance deployments behind a load
      // balancer accept the per-instance assumption (a Vercel function is single-instance
      // per cold-start; a courier sticks to one function for the duration of a batch).
      const _locationIdempotencyMap = new Map<string, number>();
      const IDEMPOTENCY_TTL_MS = 5 * 60 * 1000;
      function _gcIdempotency() {
        const now = Date.now();
        for (const [k, exp] of _locationIdempotencyMap) {
          if (exp <= now) _locationIdempotencyMap.delete(k);
        }
      }

    Step 3 — REPLACE the existing POST /location handler (lines 95-121 in current file). New shape:

      router.post("/location", agentLocationRateLimit, async (req: Request, res: Response) => {
        try {
          const { deviceId, driverId, locations, platformGuess } = req.body as {
            deviceId?: string;
            driverId?: string;
            locations?: Array<{ latitude: number; longitude: number; accuracy: number; speed?: number | null; capturedAt: string; idempotencyKey?: string }>;
            platformGuess?: string | null;
          };

          if (!deviceId) { res.status(400).json({ error: "deviceId required" }); return; }
          if (!driverId) { res.status(400).json({ error: "driverId required" }); return; }
          if (!Array.isArray(locations)) { res.status(400).json({ error: "locations array required" }); return; }

          const device = await prisma.device.findUnique({
            where: { id: deviceId },
            include: { driver: true },
          });
          if (!device || !device.driver) { res.status(404).json({ error: "Device or driver not found" }); return; }
          const tenantId = device.driver.tenantId;

          // Dedup idempotencyKey within 5-min window.
          _gcIdempotency();
          const fresh = locations.filter((l) => {
            if (!l.idempotencyKey) return true;
            const k = `${deviceId}:${l.idempotencyKey}`;
            if (_locationIdempotencyMap.has(k)) return false;
            _locationIdempotencyMap.set(k, Date.now() + IDEMPOTENCY_TTL_MS);
            return true;
          });

          if (fresh.length > 0) {
            // Validate lat/lng ranges once.
            for (const l of fresh) {
              if (l.latitude < -90 || l.latitude > 90 || l.longitude < -180 || l.longitude > 180) {
                res.status(400).json({ error: "lat/lng out of range" });
                return;
              }
            }

            await prisma.locationLog.createMany({
              data: fresh.map((l) => ({
                deviceId,
                driverId,
                latitude: l.latitude,
                longitude: l.longitude,
                accuracy: l.accuracy,
                speed: l.speed ?? null,
                capturedAt: new Date(l.capturedAt),
              })),
            });

            const last = fresh[fresh.length - 1];
            await prisma.device.update({
              where: { id: deviceId },
              data: { lastLatitude: last.latitude, lastLongitude: last.longitude, lastSeen: new Date() },
            });

            // Upsert CourierOnlineSession (no @@unique → use findFirst + update/create pattern).
            const existing = await prisma.courierOnlineSession.findFirst({
              where: { tenantId, driverId, isOnline: true },
            });
            const lastCapturedAt = new Date(last.capturedAt);
            if (existing) {
              await prisma.courierOnlineSession.update({
                where: { id: existing.id },
                data: { lastGpsAt: lastCapturedAt, lastGpsLat: last.latitude, lastGpsLng: last.longitude },
              });
            } else {
              await prisma.courierOnlineSession.create({
                data: {
                  tenantId,
                  driverId,
                  isOnline: true,
                  startTime: lastCapturedAt,
                  lastGpsAt: lastCapturedAt,
                  lastGpsLat: last.latitude,
                  lastGpsLng: last.longitude,
                },
              });
            }
          }

          res.json({ synced: fresh.length, deduped: locations.length - fresh.length });
        } catch (err: any) {
          res.status(400).json({ error: err.message });
        }
      });

    Step 4 — REPLACE the existing POST /heartbeat handler (lines 54-72). Extend to accept new fields; persist batteryLevel as integer percentage:

      router.post("/heartbeat", async (req: Request, res: Response) => {
        try {
          const { deviceId, batteryLevel, isCharging, latitude, longitude, agentVersion, isLowPowerMode, platformGuess } = req.body as {
            deviceId?: string;
            batteryLevel?: number;
            isCharging?: boolean;
            latitude?: number;
            longitude?: number;
            agentVersion?: string;
            isLowPowerMode?: boolean;
            platformGuess?: string | null;
          };
          if (!deviceId) { res.status(400).json({ error: "deviceId required" }); return; }

          // batteryLevel arrives as a fraction 0..1 from expo-battery; Device.batteryLevel is Int.
          const batteryPct = typeof batteryLevel === "number" ? Math.round(Math.max(0, Math.min(1, batteryLevel)) * 100) : null;

          await prisma.device.update({
            where: { id: deviceId },
            data: {
              batteryLevel: batteryPct ?? undefined,
              isOnline: true,
              lastSeen: new Date(),
              agentVersion: agentVersion ?? undefined,
              lastLatitude: latitude ?? undefined,
              lastLongitude: longitude ?? undefined,
            },
          });

          // isLowPowerMode and platformGuess are accepted but not persisted on the Device row in
          // Phase 5; they are inputs to active-platform attribution which reads OrderEvent / Shift
          // directly. If a future phase wants to persist them, add columns to Device.
          res.json({ status: "ok" });
        } catch (err: any) {
          res.status(400).json({ error: err.message });
        }
      });

    Step 5 — Add the new POST /upload-url route. Place it after the heartbeat handler:

      router.post("/upload-url", agentUploadRateLimit, async (req: Request, res: Response) => {
        try {
          const { deviceId, orderId, contentType = "image/jpeg" } = req.body as {
            deviceId?: string;
            orderId?: string;
            contentType?: string;
          };
          if (!deviceId) { res.status(400).json({ error: "deviceId required" }); return; }
          if (!orderId) { res.status(400).json({ error: "orderId required" }); return; }
          if (contentType !== "image/jpeg" && contentType !== "image/png") {
            res.status(400).json({ error: "contentType must be image/jpeg or image/png" });
            return;
          }

          const device = await prisma.device.findUnique({
            where: { id: deviceId },
            include: { driver: true },
          });
          if (!device || !device.driver) { res.status(404).json({ error: "Device or driver not found" }); return; }
          const tenantId = device.driver.tenantId;

          const key = `${tenantId}/${orderId}/${deviceId}/${Date.now()}.jpg`;
          const url = await presignPutUrl(key, contentType, 300);
          res.json({ url, key, expiresInSec: 300 });
        } catch (err: any) {
          res.status(400).json({ error: err.message });
        }
      });

    Step 6 — Add the new POST /delivery-photo route:

      router.post("/delivery-photo", async (req: Request, res: Response) => {
        try {
          const { deviceId, orderId, key, capturedAt, latitude, longitude } = req.body as {
            deviceId?: string;
            orderId?: string;
            key?: string;
            capturedAt?: string;
            latitude?: number;
            longitude?: number;
          };
          if (!deviceId || !orderId || !key || !capturedAt) {
            res.status(400).json({ error: "deviceId, orderId, key, capturedAt required" });
            return;
          }
          if (typeof latitude !== "number" || latitude < -90 || latitude > 90) {
            res.status(400).json({ error: "latitude out of range" });
            return;
          }
          if (typeof longitude !== "number" || longitude < -180 || longitude > 180) {
            res.status(400).json({ error: "longitude out of range" });
            return;
          }

          const device = await prisma.device.findUnique({
            where: { id: deviceId },
            include: { driver: true },
          });
          if (!device || !device.driver) { res.status(404).json({ error: "Device or driver not found" }); return; }
          const tenantId = device.driver.tenantId;

          // Defense-in-depth: even though presigned URL was issued for tenantId-prefixed key,
          // re-validate that the key the mobile is reporting matches its tenant.
          if (!String(key).startsWith(tenantId + "/")) {
            res.status(403).json({ error: "key tenant mismatch" });
            return;
          }

          await prisma.orderEvent.create({
            data: {
              tenantId,
              orderId,
              action: "DELIVERY_PHOTO",
              description: "Courier delivery photo captured",
              operator: device.driver.name ?? null,
              operatorId: device.driver.id,
              timestamp: new Date(capturedAt),
              metadata: {
                photoKey: key,
                latitude,
                longitude,
              },
            },
          });

          res.json({ ok: true });
        } catch (err: any) {
          res.status(400).json({ error: err.message });
        }
      });

    Step 7 — Run all Wave 0 backend RED tests. Expected: ALL GREEN now.
      cd backend && npx jest src/__tests__/agent/locationIngest.test.ts src/__tests__/agent/presignFlow.test.ts src/__tests__/agent/deliveryPhoto.test.ts src/__tests__/services/activePlatformAttribution.test.ts src/__tests__/middleware/agentRateLimit.test.ts

    Step 8 — Re-run the full backend test suite to confirm no regression on Phase 1/2 tests:
      cd backend && npm test

    Step 9 — Run lint:tenant:
      cd backend && npm run lint:tenant
    Expect: 0 violations. The new code in agent.ts uses tenantId on every prisma write
    (locationLog.createMany via the deviceId→driver→tenantId chain; courierOnlineSession via
    direct tenantId field; orderEvent via tenantId field).

    Self-check:
    - Does the `agent.ts` file still compile (`tsc --noEmit`)?
    - Does the original `selfie` / `tickets` / `register` route logic remain unchanged?
  </action>

  <verify>
    <automated>cd /Users/mac/Documents/Darb/backend && npx jest src/__tests__/agent/locationIngest.test.ts src/__tests__/agent/presignFlow.test.ts src/__tests__/agent/deliveryPhoto.test.ts 2>&1 | grep -E "Tests:.*passed" | head -1</automated>
  </verify>

  <done>
    - agent.ts has 2 new routes (upload-url, delivery-photo) + 2 extended (location, heartbeat)
    - All 5 Wave 0 backend RED tests GREEN
    - Pre-existing backend test count is unchanged (no regression)
    - lint:tenant exits 0
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Mobile HTTPS → Express route handler | Untrusted client input enters the backend; deviceId, idempotencyKey, capturedAt, lat/lng all come from a possibly-tampered courier device |
| Express handler → Prisma → Postgres | Cross-tenant scope enforcement happens here |
| Express handler → Cloudflare R2 (S3 SDK) | Server-issued presigned URL transits to mobile and back; URL itself becomes a capability token |
| Express handler → in-process Map (idempotency) | Volatile state survives only within a single function instance |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-02-01 | Information disclosure | GPS data exfiltration: courier's location stream for tenant A leaks to tenant B | mitigate | Every POST /location resolves tenantId via `device.driver.tenantId`; LocationLog has no tenant column (boundary is via Driver), so the existing `gpsTrack` Phase 1 read tool already enforces driver+tenant. Wave 2 tests assert deviceId 404 path. |
| T-05-02-02 | Information disclosure | Active-platform attribution leak: backend leaks "courier X is currently on Talabat" cross-tenant | mitigate | resolveActivePlatform takes tenantId as REQUIRED arg; every prisma.{orderEvent,shift,driver} query filters by tenantId. The cross-tenant test in activePlatformAttribution.test.ts asserts the boundary. |
| T-05-02-03 | Tampering | Photo malware upload: adversary uploads exec/script disguised as image | mitigate | POST /upload-url validates contentType strictly to {image/jpeg, image/png}; presigned URL signed with that exact ContentType so R2 rejects mismatched uploads. |
| T-05-02-04 | Information disclosure | Presigned URL leakage: URL captured in transit + replayed by adversary | mitigate | Presigned URL TTL is 300 seconds (5 minutes); even if intercepted, narrow window. R2 bucket itself is private (no public-read policy). |
| T-05-02-05 | Information disclosure | Cross-tenant photo access via key forgery | mitigate | POST /delivery-photo validates `key.startsWith(tenantId + "/")`; rejects 403 on mismatch (covered by deliveryPhoto.test.ts). Defense-in-depth — presigned URL was already tenant-scoped, but cheap to validate twice. |
| T-05-02-06 | Denial of service | DoS via location spam: courier device sends 1000s of points/sec | mitigate | agentLocationRateLimit (200 / 5min keyed by deviceId). Server-side dedup also short-circuits replays of the same idempotencyKey. |
| T-05-02-07 | Tampering | Outbox poisoning: adversary injects malicious LocationLog records via the SQLite outbox → repeated POSTs to backend | mitigate | (a) Backend rate-limit is per-deviceId; (b) Idempotency dedup means re-posting same point N times only writes 1 row; (c) deviceId-based scoping means a poisoned device only contaminates its own driver's track. |
| T-05-02-08 | Spoofing | Forged deviceId — attacker guesses another device's UUID | accept | Device.id is a UUID; guessing is computationally infeasible. Defense-in-depth via tenant scoping at every query layer. |
| T-05-02-09 | Information disclosure | R2 secret keys leaked into client bundle | mitigate | r2Service is backend-only; mobile sees presigned URLs only; .env.example includes R2_* with empty values (templates), production keys live in Vercel env vars. |
</threat_model>

<verification>
- `cd backend && npm test` PASS count is greater than pre-Phase-5 PASS count by exactly the number of Wave 0 RED tests now GREEN
- `cd backend && npm run lint:tenant` exits 0 (no new no-prisma-without-tenant violations)
- `cd backend && npx tsc --noEmit` exits 0 (no TypeScript regression)
- POST /api/agent/location with idempotencyKey on duplicate is dropped; integration test asserts via deduped count
- POST /api/agent/upload-url returns a real https URL when R2_* env vars are set in CI/dev (NOT in unit test — unit test mocks r2Service.presignPutUrl)
</verification>

<success_criteria>
- All 5 Wave 0 backend RED tests are GREEN
- 4 new backend env vars present in .env.example
- 2 new npm packages installed
- 2 new services + 1 new middleware + 2 new routes + 2 extended routes
- lint:tenant scope passes
- Pre-existing backend tests unaffected
</success_criteria>

<output>
After completion, create `.planning/phases/05-mobile-gps-beacon/05-02-SUMMARY.md` with:
- Wave 2 deliverables list
- Test counts: backend Wave 0 RED → Wave 2 GREEN
- The CourierOnlineSession upsert workaround (findFirst-then-update-or-create — composite unique index does not exist on the model)
- The idempotency Map design (in-process, 5-min TTL, single-instance assumption)
- R2 env vars that need to be set in production Vercel before Wave 4 deploy
</output>
