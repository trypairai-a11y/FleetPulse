---
phase: 05-mobile-gps-beacon
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - mobile/package.json
  - mobile/jest.config.js
  - mobile/__tests__/setup.ts
  - mobile/__tests__/mocks/expo-sqlite.ts
  - mobile/__tests__/mocks/expo-location.ts
  - mobile/__tests__/mocks/expo-image-manipulator.ts
  - mobile/__tests__/mocks/expo-camera.ts
  - mobile/__tests__/mocks/expo-battery.ts
  - mobile/__tests__/mocks/expo-secure-store.ts
  - mobile/__tests__/mocks/api-client.ts
  - mobile/__tests__/outbox.idempotency.test.ts
  - mobile/__tests__/outbox.flushSemantics.test.ts
  - mobile/__tests__/outbox.giveUp.test.ts
  - mobile/__tests__/locationService.permissionFlow.test.ts
  - mobile/__tests__/locationService.taskRegistration.test.ts
  - mobile/__tests__/photoService.compress.test.ts
  - mobile/__tests__/photoService.uploadDirect.test.ts
  - mobile/__tests__/platformGuess.lastTab.test.ts
  - mobile/__tests__/heartbeatService.battery.test.ts
  - mobile/__tests__/appJson.androidPermissions.test.ts
  - backend/package.json
  - backend/src/__tests__/agent/locationIngest.test.ts
  - backend/src/__tests__/agent/presignFlow.test.ts
  - backend/src/__tests__/agent/deliveryPhoto.test.ts
  - backend/src/__tests__/services/activePlatformAttribution.test.ts
  - backend/src/__tests__/middleware/agentRateLimit.test.ts
autonomous: true
requirements:
  - REQ-mobile-always-on-gps
  - REQ-mobile-active-platform-detection
  - REQ-mobile-delivery-photo-capture

must_haves:
  truths:
    - "Mobile codebase has a working jest-expo test runner (`cd mobile && npm test` exits 0 with N failing tests)"
    - "All Wave 1+ behaviors are described as failing test files (RED) before implementation begins"
    - "lint:tenant glob includes the new backend services/middleware/route files Phase 5 will add"
    - "Each failing test has a precise grep-able invariant that turns GREEN exactly when the corresponding Wave 1-3 implementation is correct"
  artifacts:
    - path: "mobile/jest.config.js"
      provides: "jest-expo preset configuration"
      contains: "preset.*jest-expo"
    - path: "mobile/package.json"
      provides: "test script + jest-expo + @testing-library/react-native + @types/jest"
      contains: "\"test\": \"jest\""
    - path: "mobile/__tests__/setup.ts"
      provides: "Module mocks bootstrap for expo-sqlite, expo-location, expo-image-manipulator, expo-camera, expo-battery, expo-secure-store, api/client"
    - path: "mobile/__tests__/outbox.idempotency.test.ts"
      provides: "RED: enqueue twice with same key inserts once"
    - path: "mobile/__tests__/outbox.flushSemantics.test.ts"
      provides: "RED: confirmed rows deleted; failed rows attempts++"
    - path: "mobile/__tests__/outbox.giveUp.test.ts"
      provides: "RED: rows with attempts >= 5 are not re-flushed"
    - path: "mobile/__tests__/locationService.permissionFlow.test.ts"
      provides: "RED: foreground denied → no background ask; foreground granted → background prompted"
    - path: "mobile/__tests__/locationService.taskRegistration.test.ts"
      provides: "RED: TaskManager.defineTask called once at module load with name 'darb-background-location'"
    - path: "mobile/__tests__/photoService.compress.test.ts"
      provides: "RED: compressed JPEG ≤ 200KB"
    - path: "mobile/__tests__/photoService.uploadDirect.test.ts"
      provides: "RED: presigned URL fetched, PUT issued with Content-Type image/jpeg, then metadata POST"
    - path: "mobile/__tests__/platformGuess.lastTab.test.ts"
      provides: "RED: setLastTab + getLastTab roundtrip; null after 30 min decay"
    - path: "mobile/__tests__/heartbeatService.battery.test.ts"
      provides: "RED: heartbeat reads expo-battery values, NOT hardcoded 1.0"
    - path: "mobile/__tests__/appJson.androidPermissions.test.ts"
      provides: "RED: app.json Android permissions array contains FOREGROUND_SERVICE_LOCATION"
    - path: "backend/src/__tests__/agent/locationIngest.test.ts"
      provides: "RED: 5 GPS POSTs → 5 LocationLog rows + CourierOnlineSession upsert with fresh lastGpsAt; duplicate idempotencyKey within window dedupes; rate limit returns 429 on 201st request"
    - path: "backend/src/__tests__/agent/presignFlow.test.ts"
      provides: "RED: /api/agent/upload-url issues a presigned URL only when orderEvent (or order ID) belongs to caller's tenant"
    - path: "backend/src/__tests__/agent/deliveryPhoto.test.ts"
      provides: "RED: /api/agent/delivery-photo writes OrderEvent (action=DELIVERY_PHOTO) with photoKey + lat/lng metadata; rejects 403 on key prefix tenant mismatch"
    - path: "backend/src/__tests__/services/activePlatformAttribution.test.ts"
      provides: "RED: Tier 1 OrderEvent → HIGH; Tier 2 IN_PROGRESS Shift → MEDIUM; Tier 3 Driver default → LOW (or MEDIUM with hint match); UNKNOWN when driver missing"
    - path: "backend/src/__tests__/middleware/agentRateLimit.test.ts"
      provides: "RED: agentLocationRateLimit returns 429 after 200 in 5min window keyed by deviceId"
    - path: "backend/package.json"
      provides: "lint:tenant script extended with new Phase 5 backend file globs"
      contains: "src/services/activePlatformAttribution.ts"
  key_links:
    - from: "mobile/__tests__/setup.ts"
      to: "mobile/jest.config.js"
      via: "setupFilesAfterEach: ['<rootDir>/__tests__/setup.ts']"
      pattern: "setupFilesAfterEach.*setup"
    - from: "backend/src/__tests__/services/activePlatformAttribution.test.ts"
      to: "backend/src/services/activePlatformAttribution.ts (Wave 2)"
      via: "import resolveActivePlatform from ../../services/activePlatformAttribution"
      pattern: "from.*services/activePlatformAttribution"
    - from: "backend/package.json lint:tenant"
      to: "src/services/activePlatformAttribution.ts, src/services/r2Service.ts, src/middleware/agentRateLimit.ts"
      via: "ESLint glob expansion"
      pattern: "lint:tenant.*activePlatformAttribution"
---

<objective>
Wave 0 — RED test scaffolding + jest-expo bootstrap. Establish the safety net BEFORE any implementation lands. Every behavior shipped in Waves 1-3 has a corresponding failing test that turns GREEN exactly when the behavior is correct. This is the Nyquist verification floor for Phase 5.

Purpose: Mobile has zero tests today (verified — `mobile/package.json` has no jest deps). Phase 5 ships the most critical mobile code in the codebase (always-on GPS, the data moat). Without a test runner the next 4 waves have no per-task verification command and the executor has no GREEN-bar to validate against.

Output:
- `mobile/jest.config.js` + `mobile/__tests__/setup.ts` + 10 mobile test files (all currently RED)
- 5 backend test files (all currently RED) covering locationIngest, presignFlow, deliveryPhoto, activePlatformAttribution, agentRateLimit
- `backend/package.json` `lint:tenant` script extended to cover Phase 5's new backend file globs
- `cd mobile && npm test` exits 0 with N failing tests (NOT a runtime crash)
- `cd backend && npm test` includes the 5 new RED files (failures expected)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-mobile-gps-beacon/05-RESEARCH.md
@CLAUDE.md
@mobile/package.json
@mobile/app.json
@mobile/src/services/locationService.ts
@mobile/src/api/client.ts
@mobile/app/(tabs)/dashboard.tsx
@backend/package.json
@backend/src/routes/agent.ts
@backend/prisma/schema.prisma
@backend/src/__tests__/setup.ts
@backend/src/__tests__/mocks/config.ts

<interfaces>
<!-- Backend test mocking pattern (existing — must be matched). -->

From backend/src/__tests__/setup.ts:
```typescript
export function getMockPrisma(): jest.Mocked<typeof prisma>;
export function resetAllMocks(): void;
```

Backend uses moduleNameMapper in jest.config.js to swap `../config` → `__tests__/mocks/config.ts`.
Phase 5 RED tests follow the SAME pattern — DO NOT introduce new mocking infrastructure.

From backend/prisma/schema.prisma (verified models — Phase 5 uses these, NO new models):
```
LocationLog { id, deviceId, driverId, latitude, longitude, accuracy, speed?, capturedAt }
Device { id, tenantId, driverId, imei, model, osVersion, batteryLevel, lastSeen, lastLatitude, lastLongitude, ... }
CourierOnlineSession { id, tenantId, driverId, startTime, endTime?, isOnline, lastGpsAt?, lastGpsLat?, lastGpsLng?, area? }
OrderEvent { id, tenantId, orderId, action, description, operator?, operatorId?, timestamp, metadata? Json }
Shift { id, tenantId, driverId, platform, status, scheduledStart, scheduledEnd, actualStart?, actualEnd?, ... }
Driver { id, tenantId, platform, ... }
```

NOTE: There is NO `model Order` in this schema. The "Order" referenced by RESEARCH.md examples
is fictional — Phase 5 records delivery photo metadata as an `OrderEvent` row with
`action="DELIVERY_PHOTO"` and `metadata={ photoKey, latitude, longitude }`. The orderId in
the request body refers to OrderEvent.orderId (a free-form string the mobile app derives from
its order list — at this stage, accept any string but require the OrderEvent's tenant matches
the caller's tenant if the OrderEvent already exists, OR allow creating a new OrderEvent
under the caller's tenant if it does not).

Mobile mocking pattern (NEW — Phase 5 establishes):

mobile/__tests__/setup.ts provides centralised jest.mock() calls for:
  - `expo-sqlite` → in-memory `_db` map keyed by SQL string
  - `expo-location` → mocked requestForegroundPermissionsAsync, requestBackgroundPermissionsAsync, startLocationUpdatesAsync, stopLocationUpdatesAsync, getCurrentPositionAsync
  - `expo-task-manager` → captures defineTask calls; isTaskRegisteredAsync returns last-registered
  - `expo-image-manipulator` → returns `{ uri: 'file://compressed.jpg', width: 1280, height: 960 }`
  - `expo-camera` → minimal mock; not exercised in Phase 5 unit tests
  - `expo-battery` → returns deterministic battery level
  - `expo-secure-store` → in-memory map
  - `../src/api/client` → all named exports mocked as jest.fn(); tests inject behaviour per-test

The api/client mock exposes `__mockApi` with the same shape as the real exports so
`mobile/__tests__/outbox.flushSemantics.test.ts` can do `mockApi.uploadLocations.mockResolvedValueOnce({ synced: 5 })`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 0: Bootstrap jest-expo + add Phase 5 RED test files (mobile)</name>
  <files>mobile/package.json, mobile/jest.config.js, mobile/__tests__/setup.ts, mobile/__tests__/mocks/expo-sqlite.ts, mobile/__tests__/mocks/expo-location.ts, mobile/__tests__/mocks/expo-task-manager.ts, mobile/__tests__/mocks/expo-image-manipulator.ts, mobile/__tests__/mocks/expo-camera.ts, mobile/__tests__/mocks/expo-battery.ts, mobile/__tests__/mocks/expo-secure-store.ts, mobile/__tests__/mocks/api-client.ts, mobile/__tests__/outbox.idempotency.test.ts, mobile/__tests__/outbox.flushSemantics.test.ts, mobile/__tests__/outbox.giveUp.test.ts, mobile/__tests__/locationService.permissionFlow.test.ts, mobile/__tests__/locationService.taskRegistration.test.ts, mobile/__tests__/photoService.compress.test.ts, mobile/__tests__/photoService.uploadDirect.test.ts, mobile/__tests__/platformGuess.lastTab.test.ts, mobile/__tests__/heartbeatService.battery.test.ts, mobile/__tests__/appJson.androidPermissions.test.ts</files>

  <read_first>
    - mobile/package.json (verify NO jest deps yet)
    - mobile/src/services/locationService.ts (current AsyncStorage outbox pattern — what we're replacing)
    - mobile/src/api/client.ts (named exports mobile tests will mock)
    - .planning/phases/05-mobile-gps-beacon/05-RESEARCH.md "Validation Architecture" + "Wave 0 Gaps" sections
    - backend/jest.config.js + backend/src/__tests__/setup.ts (the moduleNameMapper pattern Phase 5 mobile mirrors)
  </read_first>

  <action>
    Step 1 — Add devDependencies + test script to `mobile/package.json` (per A1 jest-expo + RESEARCH.md "Don't Hand-Roll" row mobile-testing-harness):
      "scripts": { "test": "jest", "test:watch": "jest --watch" }
      "devDependencies": {
        "@types/jest": "^30.0.0",
        "@testing-library/react-native": "^13.0.0",
        "jest": "^29.7.0",
        "jest-expo": "~52.0.0",
        "react-test-renderer": "18.3.1"
      }

    Run `cd mobile && npm install` so the lockfile + node_modules pick up the new packages. Do NOT use --no-save; the orchestrator commits package-lock.json.

    Step 2 — Create `mobile/jest.config.js`:
      module.exports = {
        preset: "jest-expo",
        setupFilesAfterEach: ["<rootDir>/__tests__/setup.ts"],
        testPathIgnorePatterns: ["/node_modules/", "/.expo/", "/dist/"],
        moduleNameMapper: {
          "^expo-sqlite$": "<rootDir>/__tests__/mocks/expo-sqlite.ts",
          "^expo-location$": "<rootDir>/__tests__/mocks/expo-location.ts",
          "^expo-task-manager$": "<rootDir>/__tests__/mocks/expo-task-manager.ts",
          "^expo-image-manipulator$": "<rootDir>/__tests__/mocks/expo-image-manipulator.ts",
          "^expo-camera$": "<rootDir>/__tests__/mocks/expo-camera.ts",
          "^expo-battery$": "<rootDir>/__tests__/mocks/expo-battery.ts",
          "^expo-secure-store$": "<rootDir>/__tests__/mocks/expo-secure-store.ts",
          "^.*api/client$": "<rootDir>/__tests__/mocks/api-client.ts"
        },
        transformIgnorePatterns: ["node_modules/(?!((jest-)?react-native|@react-native(-community)?|expo(nent)?|@expo(nent)?/.*|expo-modules-core|@expo-google-fonts/.*|react-clone-referenced-element|@react-navigation/.*|@unimodules/.*|sentry-expo|native-base|react-native-svg)/)"],
        clearMocks: true
      };

    Step 3 — Create the eight mocks under `mobile/__tests__/mocks/`. Each mock exports the same named functions as the real module but uses `jest.fn()` so tests can inject behaviour. The expo-sqlite mock is the most important: it MUST behave like a real SQLite for INSERT OR IGNORE (track unique constraints by idempotencyKey column). See spec snippet at the bottom of this action.

    Step 4 — Create `mobile/__tests__/setup.ts` that resets all mocks via beforeEach + clears the in-memory expo-sqlite store + clears expo-secure-store map.

    Step 5 — Write each of the 10 RED test files. Each MUST:
      - Import the (not-yet-existing) Wave-1 service file at the path Wave 1 will create.
      - Make the test FAIL today with a clear "Cannot find module ../src/services/outbox" error (or equivalent for the other test files).
      - Each test asserts a SINGLE narrowly-scoped behavior (per RESEARCH.md "Phase Requirements → Test Map" table).

    Specific test bodies to write (each test = one `test('...', async () => {...})`):

    File `mobile/__tests__/outbox.idempotency.test.ts`:
      - "INSERT OR IGNORE: enqueue same idempotency key twice → only 1 row stored":
        Call enqueueGpsPoint with the exact same lat/lng/capturedAt twice.
        Read `_countForTests()`. Expect 1.

    File `mobile/__tests__/outbox.flushSemantics.test.ts`:
      - "happy path: enqueue 5 → flush 5 → queue empty":
        mockApi.uploadLocations.mockResolvedValueOnce({ synced: 5 });
        Enqueue 5 unique points. flushPendingPoints(). Expect _countForTests()===0.
      - "failure path: server throws → rows remain + attempts++":
        mockApi.uploadLocations.mockRejectedValueOnce(new Error('500'));
        Enqueue 1 point. flushPendingPoints(). Expect _countForTests()===1, attempts==1.

    File `mobile/__tests__/outbox.giveUp.test.ts`:
      - "rows with attempts>=5 are not re-flushed":
        Manually insert a row with attempts=5 via the mock SQLite seam.
        flushPendingPoints(). Expect mockApi.uploadLocations NOT called.

    File `mobile/__tests__/locationService.permissionFlow.test.ts`:
      - "foreground denied → no background ask, returns ok:false reason:foreground_denied":
        mockLocation.requestForegroundPermissionsAsync.mockResolvedValueOnce({ status:'denied' });
        const result = await startBeacon();
        expect(mockLocation.requestBackgroundPermissionsAsync).not.toHaveBeenCalled();
        expect(result).toEqual({ ok:false, reason:'foreground_denied' });
      - "both granted → ok:true; startLocationUpdatesAsync called with required iOS keys":
        mockLocation.requestForegroundPermissionsAsync.mockResolvedValueOnce({ status:'granted' });
        mockLocation.requestBackgroundPermissionsAsync.mockResolvedValueOnce({ status:'granted' });
        mockTaskManager.isTaskRegisteredAsync.mockResolvedValueOnce(false);
        const result = await startBeacon();
        expect(result).toEqual({ ok:true });
        expect(mockLocation.startLocationUpdatesAsync).toHaveBeenCalledWith('darb-background-location', expect.objectContaining({
          accuracy: expect.any(Number),
          activityType: expect.any(Number),
          pausesUpdatesAutomatically: false,
          showsBackgroundLocationIndicator: true,
          deferredUpdatesInterval: 60000,
          foregroundService: expect.objectContaining({ killServiceOnDestroy: false })
        }));

    File `mobile/__tests__/locationService.taskRegistration.test.ts`:
      - "TaskManager.defineTask called once at module load with name 'darb-background-location'":
        Importing the locationService module triggers defineTask. Verify exactly 1 call with first arg === 'darb-background-location'.

    File `mobile/__tests__/photoService.compress.test.ts`:
      - "compresses to ≤200KB":
        Mock expo-image-manipulator to return `{ uri:'file://x.jpg' }`. Mock fetch(uri).blob() to return a Blob with size 195_000. Call uploadDeliveryPhoto. Expect ImageManipulator.manipulateAsync called with `{ compress: 0.7, format: SaveFormat.JPEG }` AND `[{ resize: { width: 1280 } }]`.

    File `mobile/__tests__/photoService.uploadDirect.test.ts`:
      - "calls /api/agent/upload-url, then PUTs to presigned URL with Content-Type image/jpeg, then POSTs metadata":
        mockApi.requestUploadUrl.mockResolvedValueOnce({ url:'https://r2/x', key:'tenantA/order1/dev1/123.jpg', expiresInSec:300 });
        global.fetch = jest.fn().mockResolvedValueOnce({ ok:true });
        mockApi.recordDeliveryPhotoMetadata.mockResolvedValueOnce({ ok:true });
        await uploadDeliveryPhoto({ orderId:'order1', uri:'file://orig.jpg', latitude:29.3, longitude:47.9 });
        expect(fetch).toHaveBeenCalledWith('https://r2/x', expect.objectContaining({ method:'PUT', headers: expect.objectContaining({ 'Content-Type':'image/jpeg' }) }));

    File `mobile/__tests__/platformGuess.lastTab.test.ts`:
      - "setLastTab/getLastTab roundtrip":
        setLastTab('KEETA'); expect(getLastTab()).toBe('KEETA');
      - "decay to null after 30 min":
        Use jest.useFakeTimers(); setLastTab('KEETA'); jest.advanceTimersByTime(31*60_000); expect(getLastTab()).toBeNull();

    File `mobile/__tests__/heartbeatService.battery.test.ts`:
      - "heartbeat reads expo-battery and forwards real value (NOT hardcoded 1.0)":
        mockBattery.getBatteryLevelAsync.mockResolvedValueOnce(0.42);
        mockApi.heartbeat.mockResolvedValueOnce({});
        await sendHeartbeat();
        expect(mockApi.heartbeat).toHaveBeenCalledWith(expect.objectContaining({ batteryLevel: 0.42 }));
        expect(mockApi.heartbeat).not.toHaveBeenCalledWith(expect.objectContaining({ batteryLevel: 1.0 }));

    File `mobile/__tests__/appJson.androidPermissions.test.ts`:
      - "android.permissions includes FOREGROUND_SERVICE_LOCATION":
        const j = require('../app.json');
        expect(j.expo.android.permissions).toContain('FOREGROUND_SERVICE_LOCATION');
      - "iOS NSLocationAlwaysAndWhenInUseUsageDescription present":
        expect(j.expo.ios.infoPlist.NSLocationAlwaysAndWhenInUseUsageDescription).toBeTruthy();
      - "expo-sqlite plugin declared":
        expect(j.expo.plugins).toEqual(expect.arrayContaining(['expo-sqlite']));

    NOTE — ALL mobile tests above are RED today: the imported modules (outbox, photoService, platformGuess, heartbeatService) do not yet exist, and `app.json` does not yet declare FOREGROUND_SERVICE_LOCATION. That is expected — the tests are the contracts; Waves 1-3 implement them.

    Step 6 — `cd mobile && npm test` should exit with NON-ZERO (failures) but the runner itself MUST start cleanly. If jest-expo crashes on first run because of a missing transformIgnorePattern or babel preset, fix the config until tests merely fail (RED), not crash.

    Self-check before commit:
      - All 10 mobile test files exist
      - `cd mobile && npm test 2>&1 | head -200` shows test names but FAIL output (no "Cannot find preset" or "Babel" crash)
  </action>

  <verify>
    <automated>cd /Users/mac/Documents/Darb/mobile && npm test --silent 2>&1 | tail -40 | grep -E "(FAIL|Tests:.*failed)" | head -5</automated>
  </verify>

  <done>
    - `mobile/package.json` has the test script + jest-expo + jest + @testing-library/react-native + @types/jest + react-test-renderer
    - `mobile/jest.config.js` exists; `cd mobile && npm test` runs (does not crash) and reports failing tests
    - 10 RED test files exist under `mobile/__tests__/`; each is a precise, narrow assertion
    - 7 module mocks exist under `mobile/__tests__/mocks/`
    - `mobile/__tests__/setup.ts` resets mocks beforeEach
  </done>
</task>

<task type="auto">
  <name>Task 1: Add Phase 5 backend RED tests + extend lint:tenant scope</name>
  <files>backend/src/__tests__/agent/locationIngest.test.ts, backend/src/__tests__/agent/presignFlow.test.ts, backend/src/__tests__/agent/deliveryPhoto.test.ts, backend/src/__tests__/services/activePlatformAttribution.test.ts, backend/src/__tests__/middleware/agentRateLimit.test.ts, backend/package.json</files>

  <read_first>
    - backend/src/__tests__/agent/scheduler/ (existing agent tests — pattern reference)
    - backend/src/__tests__/setup.ts (getMockPrisma, resetAllMocks)
    - backend/src/__tests__/mocks/config.ts (prisma mock shape — verify courierOnlineSession + orderEvent + locationLog + driver + shift + device delegates exist; if any are missing add them in this task)
    - backend/src/routes/agent.ts (existing endpoints to extend in Wave 2)
    - backend/jest.config.js (moduleNameMapper for `../config`)
    - backend/package.json `lint:tenant` (the script we're extending)
    - .planning/phases/05-mobile-gps-beacon/05-RESEARCH.md "Common Pitfalls" → Pitfall 5 (cross-tenant key forgery) — the test must enforce it
  </read_first>

  <action>
    Step 1 — Verify the prisma mock in `backend/src/__tests__/mocks/config.ts` exposes delegates we need:
      `prisma.locationLog.{create,createMany,findMany,findFirst,count}`
      `prisma.courierOnlineSession.{upsert,findFirst,update}`
      `prisma.orderEvent.{create,findFirst}`
      `prisma.shift.{findFirst}`
      `prisma.driver.{findUnique,findFirst}`
      `prisma.device.{findUnique,update}`
    If any delegate is absent, add it as `jest.fn()` placeholder. Do NOT change behaviour of existing delegates — this is purely additive.

    Step 2 — Create `backend/src/__tests__/agent/locationIngest.test.ts` per RESEARCH.md "Eval Harness — Smoke Walking-Skeleton" + "Phase Requirements → Test Map":

      describe("/api/agent/location", () => {
        beforeEach(() => resetAllMocks());

        test("walking skeleton: 5 GPS POSTs → 5 LocationLog rows + CourierOnlineSession upsert", async () => {
          // Mock the device + driver lookup chain
          const prisma = getMockPrisma();
          prisma.device.findUnique.mockResolvedValue({ id:'dev1', tenantId:'tenA', driverId:'drv1', driver:{ id:'drv1', tenantId:'tenA' } });
          prisma.locationLog.createMany.mockResolvedValue({ count: 5 });
          prisma.courierOnlineSession.upsert.mockResolvedValue({} as any);
          prisma.device.update.mockResolvedValue({} as any);

          const res = await request(app).post("/api/agent/location").send({
            deviceId:'dev1',
            driverId:'drv1',
            locations: Array.from({length:5}).map((_,i)=>({ latitude:29.3759+i*0.0001, longitude:47.9774, accuracy:10, capturedAt:new Date(t0+i*6000).toISOString(), idempotencyKey:`t0+${i}` })),
          });

          expect(res.status).toBe(200);
          expect(prisma.locationLog.createMany).toHaveBeenCalledTimes(1);
          expect(prisma.courierOnlineSession.upsert).toHaveBeenCalledWith(expect.objectContaining({
            where: expect.objectContaining({ tenantId_driverId_isOnline: expect.any(Object) }),
            update: expect.objectContaining({ lastGpsAt: expect.any(Date), lastGpsLat: expect.any(Object), lastGpsLng: expect.any(Object) }),
          }));
        });

        test("server-side dedup: same idempotencyKey within 5min window inserts only once", async () => {
          // Hand-roll: backend must use a Map keyed by deviceId+idempotencyKey OR a unique constraint
          // Implementation in Wave 2 will use an in-process Map (TTL 5min) since LocationLog has no unique index on capturedAt.
          // The test asserts: send same idempotencyKey twice; createMany called with N rows the FIRST time, 0 rows the second.
          // Until Wave 2 ships this dedup logic, this test fails. THAT IS THE POINT.
        });

        test("rate limit: 201st location POST in 5min returns 429", async () => {
          // For 200 successive successful posts, then assert next status === 429.
          // RED until Wave 2 attaches agentLocationRateLimit middleware to /api/agent/location.
        });

        test("rejects when device not found", async () => {
          const prisma = getMockPrisma();
          prisma.device.findUnique.mockResolvedValue(null);
          const res = await request(app).post("/api/agent/location").send({ deviceId:'unknown', driverId:'x', locations:[] });
          expect(res.status).toBe(404);
        });
      });

    Step 3 — Create `backend/src/__tests__/agent/presignFlow.test.ts`:

      describe("/api/agent/upload-url", () => {
        beforeEach(() => resetAllMocks());

        test("issues a presigned URL with key prefix tenantId/orderId/deviceId/<ts>.jpg", async () => {
          const prisma = getMockPrisma();
          prisma.device.findUnique.mockResolvedValue({ id:'dev1', tenantId:'tenA', driverId:'drv1', driver:{ id:'drv1', tenantId:'tenA' } });

          const res = await request(app).post("/api/agent/upload-url").send({
            deviceId:'dev1', orderId:'order1', contentType:'image/jpeg',
          });

          expect(res.status).toBe(200);
          expect(res.body).toEqual(expect.objectContaining({
            url: expect.stringContaining('http'),
            key: expect.stringMatching(/^tenA\/order1\/dev1\/\d+\.jpg$/),
            expiresInSec: 300,
          }));
        });

        test("rejects when contentType is not image/jpeg or image/png", async () => {
          // Wave 2 must validate contentType to prevent malware upload. RED today.
          const prisma = getMockPrisma();
          prisma.device.findUnique.mockResolvedValue({ id:'dev1', tenantId:'tenA', driverId:'drv1', driver:{ id:'drv1' } });
          const res = await request(app).post("/api/agent/upload-url").send({ deviceId:'dev1', orderId:'order1', contentType:'application/x-sh' });
          expect(res.status).toBe(400);
        });

        test("rate limit: 31st upload-url POST in 10min returns 429", async () => {
          // RED until Wave 2 attaches agentUploadRateLimit middleware.
        });
      });

    Step 4 — Create `backend/src/__tests__/agent/deliveryPhoto.test.ts`:

      describe("/api/agent/delivery-photo", () => {
        beforeEach(() => resetAllMocks());

        test("creates OrderEvent with action=DELIVERY_PHOTO + photoKey + lat/lng metadata", async () => {
          const prisma = getMockPrisma();
          prisma.device.findUnique.mockResolvedValue({ id:'dev1', tenantId:'tenA', driverId:'drv1', driver:{ id:'drv1', tenantId:'tenA' } });
          prisma.orderEvent.create.mockResolvedValue({ id:'oe1', tenantId:'tenA' } as any);

          const res = await request(app).post("/api/agent/delivery-photo").send({
            deviceId:'dev1',
            orderId:'order1',
            key:'tenA/order1/dev1/12345.jpg',
            capturedAt: new Date().toISOString(),
            latitude: 29.3759,
            longitude: 47.9774,
          });

          expect(res.status).toBe(200);
          expect(prisma.orderEvent.create).toHaveBeenCalledWith({
            data: expect.objectContaining({
              tenantId: 'tenA',
              orderId: 'order1',
              action: 'DELIVERY_PHOTO',
              metadata: expect.objectContaining({ photoKey:'tenA/order1/dev1/12345.jpg', latitude:29.3759, longitude:47.9774 }),
            }),
          });
        });

        test("rejects 403 when key prefix tenant mismatch (cross-tenant forgery)", async () => {
          const prisma = getMockPrisma();
          prisma.device.findUnique.mockResolvedValue({ id:'dev1', tenantId:'tenA', driverId:'drv1', driver:{ id:'drv1', tenantId:'tenA' } });

          const res = await request(app).post("/api/agent/delivery-photo").send({
            deviceId:'dev1',
            orderId:'order1',
            key:'tenB/order1/dev1/12345.jpg',  // tenant B prefix, caller is tenant A
            capturedAt: new Date().toISOString(),
            latitude: 29.3759,
            longitude: 47.9774,
          });

          expect(res.status).toBe(403);
          expect(prisma.orderEvent.create).not.toHaveBeenCalled();
        });

        test("rejects 400 when latitude out of range [-90,90]", async () => {
          const prisma = getMockPrisma();
          prisma.device.findUnique.mockResolvedValue({ id:'dev1', tenantId:'tenA', driverId:'drv1', driver:{ id:'drv1', tenantId:'tenA' } });
          const res = await request(app).post("/api/agent/delivery-photo").send({
            deviceId:'dev1', orderId:'order1', key:'tenA/order1/dev1/x.jpg',
            capturedAt: new Date().toISOString(), latitude: 999, longitude: 0,
          });
          expect(res.status).toBe(400);
        });
      });

    Step 5 — Create `backend/src/__tests__/services/activePlatformAttribution.test.ts`:

      describe("resolveActivePlatform", () => {
        beforeEach(() => resetAllMocks());

        test("Tier 1: HIGH/order_event when OrderEvent in last 30 min for this driver's order", async () => {
          // The Phase 5 implementation looks up:
          //   prisma.orderEvent.findFirst({ where:{ tenantId, timestamp:{gte}, metadata:{ path:['driverId'], equals: driverId } OR a join } })
          // Since OrderEvent does NOT have a driverId column directly, attribution joins to OrderLog to get the platform
          // OR matches on operatorId (mobile sends operatorId=driverId on delivery photo OrderEvents).
          // For the unit test we just assert the ORDER of evidence checks: orderEvent first, shift second, driver last.
          const prisma = getMockPrisma();
          prisma.orderEvent.findFirst.mockResolvedValue({ id:'oe1', tenantId:'tenA', orderId:'o1', timestamp:new Date(), metadata:{ platform:'KEETA' } } as any);
          // Wave 2 implementation derives platform from the orderId's OrderLog row OR from metadata.platform field.

          const result = await resolveActivePlatform({ tenantId:'tenA', driverId:'drv1', at:new Date(), mobileHint:null });
          expect(result.confidence).toBe('HIGH');
          expect(result.source).toBe('order_event');
          expect(result.platform).toBe('KEETA');
        });

        test("Tier 2: MEDIUM/shift when no recent OrderEvent but Shift IN_PROGRESS", async () => {
          const prisma = getMockPrisma();
          prisma.orderEvent.findFirst.mockResolvedValue(null);
          prisma.shift.findFirst.mockResolvedValue({ id:'s1', platform:'TALABAT', actualStart: new Date(Date.now() - 60_000) } as any);

          const result = await resolveActivePlatform({ tenantId:'tenA', driverId:'drv1', at:new Date(), mobileHint:null });
          expect(result).toEqual(expect.objectContaining({ confidence:'MEDIUM', source:'shift', platform:'TALABAT' }));
        });

        test("Tier 3: LOW/driver_default when no order, no shift", async () => {
          const prisma = getMockPrisma();
          prisma.orderEvent.findFirst.mockResolvedValue(null);
          prisma.shift.findFirst.mockResolvedValue(null);
          prisma.driver.findUnique.mockResolvedValue({ id:'drv1', tenantId:'tenA', platform:'DELIVEROO' } as any);

          const result = await resolveActivePlatform({ tenantId:'tenA', driverId:'drv1', at:new Date(), mobileHint:null });
          expect(result).toEqual(expect.objectContaining({ confidence:'LOW', source:'driver_default', platform:'DELIVEROO' }));
        });

        test("Tier 3 + matching mobileHint → MEDIUM/mobile_hint", async () => {
          const prisma = getMockPrisma();
          prisma.orderEvent.findFirst.mockResolvedValue(null);
          prisma.shift.findFirst.mockResolvedValue(null);
          prisma.driver.findUnique.mockResolvedValue({ id:'drv1', tenantId:'tenA', platform:'DELIVEROO' } as any);

          const result = await resolveActivePlatform({ tenantId:'tenA', driverId:'drv1', at:new Date(), mobileHint:'DELIVEROO' });
          expect(result).toEqual(expect.objectContaining({ confidence:'MEDIUM', source:'mobile_hint' }));
        });

        test("UNKNOWN when driver not in tenant", async () => {
          const prisma = getMockPrisma();
          prisma.orderEvent.findFirst.mockResolvedValue(null);
          prisma.shift.findFirst.mockResolvedValue(null);
          prisma.driver.findUnique.mockResolvedValue(null);

          const result = await resolveActivePlatform({ tenantId:'tenA', driverId:'unknown', at:new Date(), mobileHint:null });
          expect(result).toEqual(expect.objectContaining({ confidence:'UNKNOWN', source:'none', platform:null }));
        });

        test("cross-tenant leak guard: shift in tenantB does NOT count for tenantA", async () => {
          // Wave 2 implementation MUST scope all queries by tenantId. RED until enforced.
          const prisma = getMockPrisma();
          prisma.shift.findFirst.mockImplementation((args:any) => {
            if (args?.where?.tenantId === 'tenA') return Promise.resolve(null);
            return Promise.resolve({ id:'s-cross', platform:'KEETA' });
          });
          prisma.orderEvent.findFirst.mockResolvedValue(null);
          prisma.driver.findUnique.mockResolvedValue({ id:'drv1', tenantId:'tenA', platform:'DELIVEROO' });

          const result = await resolveActivePlatform({ tenantId:'tenA', driverId:'drv1', at:new Date(), mobileHint:null });
          expect(result.platform).toBe('DELIVEROO');  // falls through to driver default — NOT cross-tenant shift
        });
      });

    Step 6 — Create `backend/src/__tests__/middleware/agentRateLimit.test.ts`:

      describe("agentLocationRateLimit", () => {
        // Build a mini-app that mounts only the limiter + a 200 echo.
        let miniApp: express.Application;
        beforeAll(() => {
          miniApp = express();
          miniApp.use(express.json());
          miniApp.use(agentLocationRateLimit);
          miniApp.post("/x", (req, res) => res.json({ ok:true }));
        });

        test("permits 200 in 5min, blocks 201st", async () => {
          for (let i = 0; i < 200; i++) {
            const r = await request(miniApp).post("/x").send({ deviceId:'dev1' });
            expect(r.status).toBe(200);
          }
          const blocked = await request(miniApp).post("/x").send({ deviceId:'dev1' });
          expect(blocked.status).toBe(429);
        });

        test("keyGenerator scopes by deviceId — different deviceIds have independent buckets", async () => {
          for (let i = 0; i < 200; i++) {
            await request(miniApp).post("/x").send({ deviceId:'dev2' });
          }
          // dev2 exhausted; dev3 untouched
          const r = await request(miniApp).post("/x").send({ deviceId:'dev3' });
          expect(r.status).toBe(200);
        });
      });

    Step 7 — Extend `backend/package.json` `lint:tenant` script. Current script (verified) globs:
      src/agent/ src/__tests__/agent/ src/routes/aiChiefOfStaff.ts src/routes/ai.ts src/agent/tools/action/ src/services/decisions/ src/services/billing/ src/services/onboarding/ src/routes/decisions.ts src/routes/admin/ src/middleware/superAdmin.ts src/queues/onboardingBackwashWorker.ts

    Append the Phase 5 globs (file paths the next waves will create — lint:tenant uses `--no-error-on-unmatched-pattern` so it's safe to add upfront):
      src/services/activePlatformAttribution.ts
      src/services/r2Service.ts
      src/middleware/agentRateLimit.ts
      src/__tests__/services/activePlatformAttribution.test.ts

    Update is a single line — the script is one long string. Preserve the rest verbatim.

    Step 8 — Run `cd backend && npm test -- --testPathPattern="locationIngest|presignFlow|deliveryPhoto|activePlatformAttribution|agentRateLimit" 2>&1 | tail -60`. Expect:
      - Test runner starts cleanly
      - The new tests are FAILING (they import services/middleware that don't yet exist)
      - The PRE-EXISTING test suite is unaffected (don't break Phase 1/2 tests)

    Step 9 — Run `cd backend && npm run lint:tenant`. Expect: no output (clean) — the new globs don't match files yet but `--no-error-on-unmatched-pattern` keeps the script green.
  </action>

  <verify>
    <automated>cd /Users/mac/Documents/Darb/backend && npm test -- --testPathPattern="locationIngest|presignFlow|deliveryPhoto|activePlatformAttribution|agentRateLimit" 2>&1 | tail -20 | grep -E "(FAIL|Tests:.*failed)" | head -3 && cd /Users/mac/Documents/Darb/backend && npm run lint:tenant 2>&1 | tail -3</automated>
  </verify>

  <done>
    - 5 new backend RED test files exist
    - All 5 fail because they reference Wave 2/3 modules that don't yet exist (or unimplemented behaviour)
    - Pre-existing backend Phase 1/2 tests still pass (regression check: full `npm test` PASS count is ≥ pre-Phase-5 count)
    - `lint:tenant` script in backend/package.json includes 4 new Phase 5 file globs
    - `npm run lint:tenant` exits 0
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test code → mock | Test files run in node, but they mock prisma/express/expo modules entirely; no real DB or filesystem boundary crossed. |

## STRIDE Threat Register (Wave 0 scope)

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-00-01 | Tampering | Mock state leakage between tests → false-positive GREEN | mitigate | `setupFilesAfterEach` resets all mocks; mocks themselves do not persist data outside the test file |
| T-05-00-02 | Spoofing | RED tests pretend tenants exist, masking real-world tenant boundary bugs | mitigate | The "cross-tenant leak guard" test in activePlatformAttribution.test.ts asserts the boundary; same pattern carried into Wave 2 integration tests |
| T-05-00-03 | Information disclosure | Test fixtures contain hardcoded "PII" (lat/lng of Kuwait) | accept | Coordinates 29.3759, 47.9774 are the geographic centre of Kuwait — public knowledge, no real PII |
| T-05-00-04 | Denial of service | jest-expo install bloats the mobile bundle | accept | Dev-dependencies only; never shipped to production EAS builds |
| T-05-00-05 | Elevation of privilege | A malicious test-only mock exports a real DB connection | mitigate | Mocks live in `__tests__/mocks/` and only export jest.fn() seams; reviewed at PR time |
</threat_model>

<verification>
- `cd mobile && npm test` runs (does not crash)
- All 10 mobile tests fail because the modules they import do not exist yet (RED is correct)
- `cd backend && npm test` includes the 5 new files; pre-existing Phase 1/2 test count is unchanged
- `cd backend && npm run lint:tenant` exits 0 with the extended scope
- No production code modified in this wave
</verification>

<success_criteria>
- 10 mobile RED test files + 1 jest config + 1 setup file + 7 mocks
- 5 backend RED test files
- Mobile test runner starts cleanly (jest-expo bootstrapped)
- Backend lint:tenant scope extended to include Phase 5 files
- Wave 1+ executors have a precise GREEN-bar for every behaviour they ship
</success_criteria>

<output>
After completion, create `.planning/phases/05-mobile-gps-beacon/05-00-SUMMARY.md` with:
- Wave 0 deliverables list
- Test file → Wave/Task mapping
- jest-expo bootstrap notes (any transformIgnorePatterns issues hit + how resolved)
- lint:tenant scope before/after diff
</output>
