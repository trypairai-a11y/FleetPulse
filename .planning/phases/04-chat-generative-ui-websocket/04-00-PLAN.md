---
phase: 04-chat-generative-ui-websocket
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - backend/package.json
  - frontend/package.json
  - frontend/src/__tests__/ai/AskDarbPalette.test.tsx
  - frontend/src/__tests__/ai/ChatViewRenderer.test.tsx
  - frontend/src/__tests__/ai/ChatActionCard.test.tsx
  - frontend/src/__tests__/decisions/PinnedViewsRail.test.tsx
  - frontend/src/__tests__/scheduled-jobs/ScheduledBriefingsList.test.tsx
  - frontend/src/__tests__/hooks/useStreamingChat.test.ts
  - frontend/src/__tests__/fixtures/chat-views.ts
  - backend/src/__tests__/agent/tools/view/describeView.test.ts
  - backend/src/__tests__/routes/chatStream.test.ts
  - backend/src/__tests__/routes/chatThreads.test.ts
  - backend/src/__tests__/routes/pinnedViews.test.ts
  - backend/src/__tests__/routes/scheduledBriefings.test.ts
  - backend/src/__tests__/queues/scheduledBriefingsWorker.test.ts
  - backend/src/__tests__/services/chatHistoryService.test.ts
  - backend/src/__tests__/ai/goldSet.test.ts
  - backend/src/__tests__/ai/tenantScopeLeak.test.ts
  - backend/src/__tests__/ai/chatActionProposal.test.ts
  - backend/src/__tests__/agent/runtimeStreaming.test.ts
  - backend/src/__tests__/agent/__fixtures__/goldSet.json
autonomous: true
requirements:
  - REQ-chat-global-access
  - REQ-chat-generated-dashboards
  - REQ-chat-action-proposals
  - REQ-chat-scheduled-jobs
  - REQ-agent-natural-language-qa
  - REQ-realtime-streaming

must_haves:
  truths:
    - "RED tests for every Phase 4 surface exist and FAIL (or skip with TODO) before implementation begins"
    - "cmdk@^1.1.1 is installed in frontend; new lint:tenant scope covers the 7 new backend file targets"
    - "13 gold-set fixtures exist covering all 9 viewBlock types + chat-action flow + Arabic-prompt acceptance + tenant-scope leak negative test"
    - "Schema-shape tests reference ChatThread/ChatMessage/ScheduledBriefing models; tests fail until Wave 1 schema lands"
  artifacts:
    - path: "frontend/package.json"
      provides: "cmdk dependency"
      contains: "\"cmdk\""
    - path: "frontend/src/__tests__/ai/AskDarbPalette.test.tsx"
      provides: "RED tests for cmdk-based palette: Cmd+K toggles, recents, quick actions, navigation"
      min_lines: 60
    - path: "frontend/src/__tests__/ai/ChatViewRenderer.test.tsx"
      provides: "RED tests rendering all 9 viewBlock variants from fixtures"
      min_lines: 80
    - path: "frontend/src/__tests__/ai/ChatActionCard.test.tsx"
      provides: "RED tests Approve/Edit/Dismiss + reuses /api/decisions/:id/approve"
      min_lines: 50
    - path: "frontend/src/__tests__/decisions/PinnedViewsRail.test.tsx"
      provides: "RED test: rail collapsed-by-default + tile rendering + unpin"
      min_lines: 40
    - path: "frontend/src/__tests__/scheduled-jobs/ScheduledBriefingsList.test.tsx"
      provides: "RED test: CRUD form + standing-rule template marked as no-fire"
      min_lines: 40
    - path: "frontend/src/__tests__/hooks/useStreamingChat.test.ts"
      provides: "RED test: SSE EventSource lifecycle + reconnect on drop"
      min_lines: 40
    - path: "frontend/src/__tests__/fixtures/chat-views.ts"
      provides: "Fixtures for all 9 viewBlock types"
      contains: "kpi_strip"
    - path: "backend/src/__tests__/agent/tools/view/describeView.test.ts"
      provides: "RED: describeView Zod discriminated union accepts/rejects each viewType"
      min_lines: 60
    - path: "backend/src/__tests__/routes/chatStream.test.ts"
      provides: "RED: SSE chat route emits text_delta / view_block / proposal / heartbeat / complete events"
      min_lines: 80
    - path: "backend/src/__tests__/routes/chatThreads.test.ts"
      provides: "RED: CRUD + tenant+user scope + FTS"
      min_lines: 60
    - path: "backend/src/__tests__/routes/pinnedViews.test.ts"
      provides: "RED: CRUD + cross-user-leak negative + viewType extension support"
      min_lines: 50
    - path: "backend/src/__tests__/routes/scheduledBriefings.test.ts"
      provides: "RED: CRUD + cron validation + invalid-cron rejection"
      min_lines: 40
    - path: "backend/src/__tests__/queues/scheduledBriefingsWorker.test.ts"
      provides: "RED: JobScheduler bind/unbind + worker tick produces ChatThread + ChatMessage"
      min_lines: 50
    - path: "backend/src/__tests__/services/chatHistoryService.test.ts"
      provides: "RED: append + recentTurns + searchChatHistory FTS + 90-day archive"
      min_lines: 60
    - path: "backend/src/__tests__/ai/goldSet.test.ts"
      provides: "RED: gold-set queries assert tools called + view emitted + tenant scope"
      min_lines: 80
    - path: "backend/src/__tests__/ai/tenantScopeLeak.test.ts"
      provides: "RED: tenant A chat fixtured with tenant B sentinel rows; assert no leak"
      min_lines: 40
    - path: "backend/src/__tests__/ai/chatActionProposal.test.ts"
      provides: "RED: chat propose-confirm flow writes AgentAction(source=chat)"
      min_lines: 50
    - path: "backend/src/__tests__/agent/runtimeStreaming.test.ts"
      provides: "RED: runAgent stream:true callbacks (onTextDelta/onView/onPendingAction/onCancel)"
      min_lines: 50
    - path: "backend/src/__tests__/agent/__fixtures__/goldSet.json"
      provides: "13 chat-eval fixtures covering all view types + action flows + Arabic"
      contains: "kpi_strip"
  key_links:
    - from: "backend/package.json"
      to: "lint:tenant"
      via: "npm script glob extension"
      pattern: "src/routes/chat.ts"
    - from: "frontend/package.json"
      to: "cmdk"
      via: "dependencies"
      pattern: "\"cmdk\""
---

<objective>
Wave 0 lays the safety net for Phase 4. RED tests for every Phase 4 surface, gold-set fixtures for the chat eval, the single new npm package install (`cmdk@^1.1.1`), and an extension of the `lint:tenant` glob to cover the seven new backend files Phase 4 will create.

Purpose: enforce TDD discipline (Wave 1+ work has a measurable RED→GREEN signal); make the four orchestrator-locked decisions (SSE-only / `/chat` route / standing-rule templates only / 90-day chat-history retention) testable up front; lock the Zod discriminated-union shape that the `describeView` tool will emit; and prevent any Phase 4 route from skipping tenant+user scope.

Output: 17 new test files (11 backend + 6 frontend), 1 JSON gold-set fixture, 1 frontend chat-views fixture, 1 frontend dependency, and 1 backend lint glob extension. Zero production code.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-chat-generative-ui-websocket/04-RESEARCH.md
@.planning/phases/04-chat-generative-ui-websocket/04-UI-SPEC.md
@.planning/phases/02-decisions-surface-propose-and-confirm-design-partner-1/02-VERIFICATION.md
@CLAUDE.md
@backend/src/agent/runtime.ts
@backend/src/agent/registry.ts
@backend/src/agent/index.ts
@backend/src/agent/pinnedView.ts
@backend/src/routes/decisions.ts
@frontend/src/components/ai/AskDarbPalette.tsx
@frontend/src/components/decisions/DecisionCard.tsx
@frontend/src/components/decisions/EditDrawer.tsx

<interfaces>
<!-- Existing types Wave 0 tests reference. Wave 1 will extend these or add new files referenced in tests. -->

From backend/src/agent/runtime.ts:
```typescript
// Wave 1 will extend RunAgentInput with `stream?: { onTextDelta, onView, onPendingAction, onCancel }`.
// Wave 0 RED tests reference this future surface and assert it does not exist yet.
export interface RunAgentInput {
  tenantId: string;
  triggerEvent: string;
  payload?: Record<string, unknown>;
  userMessage?: string;
  history?: Array<{ role: "user" | "assistant"; content: string }>;
  // Wave 1 ADDS:
  // stream?: { onTextDelta?(delta: string): void; onView?(view: GeneratedView): void;
  //            onPendingAction?(id: string): void; onCancel?(): boolean; };
}
export interface RunAgentResult {
  runId: string; status: "completed"|"failed"|"disabled";
  text?: string; actionsProposed: number; pendingActionIds: string[]; error?: string;
  // Wave 1 ADDS: views?: GeneratedView[];
}
```

From backend/src/agent/registry.ts:
```typescript
export interface ToolDefinition<I, O> {
  name: string; description: string;
  inputSchema: Anthropic.Tool["input_schema"];
  inputValidator: z.ZodType<I>;
  sideEffect: "read"|"write"|"notify";
  requiredRole: UserRole[];
  requiresApproval: boolean;
  allowedAgents: string[];
  strict?: boolean;
  editableParams?: string[];
  execute(ctx: ToolContext, input: I): Promise<O>;
}
```

From backend/src/agent/pinnedView.ts:
```typescript
export type PinnedViewType = "table"|"chart"|"kpi_strip"|"map"|"comparison";
// Wave 1 EXTENDS to add: "bar_chart"|"action_card"|"draft_message"|"callout"|"time_series"
```

From frontend GeneratedView contract (Wave 1 ships type file; Wave 0 tests reference target shape):
```typescript
// Target shape — frontend/src/types/chat.ts (Wave 1 Task 4 creates this):
export interface GeneratedView {
  id: string;
  viewType: "kpi_strip"|"table"|"time_series"|"bar_chart"|"mini_map"
          | "comparison_cards"|"callout"|"action_card"|"draft_message";
  title?: string; subtitle?: string;
  spec: ViewSpec;     // discriminated by viewType
  pinnable: boolean;
  pinnedViewId?: string;
}
```

From backend/src/routes/decisions.ts (existing — chat REUSES `/approve` `/dismiss` `/undo`):
```typescript
// POST /api/decisions/:id/approve  body: { source?: "chat"|"decisions", threadId?, msgId?, modifications? }
// POST /api/decisions/:id/dismiss  body: { reason }
// POST /api/decisions/:id/undo
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install cmdk + extend lint:tenant glob</name>
  <files>frontend/package.json, frontend/package-lock.json, backend/package.json</files>
  <read_first>
    - `frontend/package.json` to verify cmdk is NOT yet installed
    - `backend/package.json` `lint:tenant` script (line ~17) — extend the glob, do not replace existing entries
  </read_first>
  <action>
**Frontend dep install:**
1. From the frontend directory run `npm install cmdk@^1.1.1`. Pinned per orchestrator_resolutions §6 + RESEARCH "Standard Stack — Frontend".
2. Confirm `cmdk` lands under `dependencies` (not `devDependencies`).
3. Do NOT install Zustand. Orchestrator resolution 6 defers that decision to the Wave 3 executor; default path is React Context per Phase 2 patterns.

**Backend lint:tenant extension (additive — preserve every existing entry):**
4. Open `backend/package.json` line ~17 (`lint:tenant` script). Add the seven Phase 4 paths to the eslint glob argv list, immediately after `src/queues/onboardingBackwashWorker.ts` and before `--ext .ts`:
   - `src/routes/chat.ts`
   - `src/routes/pinnedViews.ts`
   - `src/routes/scheduledBriefings.ts`
   - `src/services/chatHistoryService.ts`
   - `src/services/scheduledBriefingsService.ts`
   - `src/queues/scheduledBriefingsWorker.ts`
   - `src/agent/tools/view/`
5. Run `cd backend && npm run lint:tenant`. Must exit 0 (`--no-error-on-unmatched-pattern` silently skips paths that do not yet exist).

**Why these decisions:**
- cmdk@^1.1.1 verified in RESEARCH `[VERIFIED: npm view cmdk version=1.1.1]`. Replaces 240-line custom palette in Wave 3 per RESEARCH "Don't Hand-Roll" + UI-SPEC §3.1.
- bullmq@^5.73.4 already ships `JobScheduler` (RESEARCH Pattern 5) — no `node-cron` add.
- No WebSocket libs — orchestrator_resolutions §1 ships SSE only; defer ws/socket.io to Phase 7.
- lint:tenant covers each new file by name (not glob) following Phase 1+2 pattern; missing file → eslint silently skips, present file → enforces `no-prisma-without-tenant`.
  </action>
  <acceptance_criteria>
    - `cmdk` listed under `frontend/package.json` "dependencies"
    - `npm ls cmdk` from `frontend/` returns the package without WARN/peer issues
    - `backend/package.json` `lint:tenant` script contains all 7 new paths verbatim
    - `cd backend && npm run lint:tenant` exits 0
    - No new package added to `backend/package.json`
    - Zustand NOT installed
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/mac/Documents/Darb/frontend &amp;&amp; npm ls cmdk &amp;&amp; cd /Users/mac/Documents/Darb/backend &amp;&amp; npm run lint:tenant &amp;&amp; node -e "const p=require('./package.json');const s=p.scripts['lint:tenant']||'';const need=['src/routes/chat.ts','src/routes/pinnedViews.ts','src/routes/scheduledBriefings.ts','src/services/chatHistoryService.ts','src/services/scheduledBriefingsService.ts','src/queues/scheduledBriefingsWorker.ts','src/agent/tools/view'];const miss=need.filter(n=>!s.includes(n));if(miss.length)throw new Error('missing: '+miss.join(','));console.log('lint:tenant has all 7 phase-4 entries')"</automated>
  </verify>
  <done>
    - `cmdk` resolves; `lint:tenant` exit 0; all 7 new path tokens present in backend package.json
    - Zustand intentionally absent
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Author backend RED tests + gold-set fixture (11 test files + 1 JSON fixture)</name>
  <files>
    backend/src/__tests__/agent/tools/view/describeView.test.ts,
    backend/src/__tests__/routes/chatStream.test.ts,
    backend/src/__tests__/routes/chatThreads.test.ts,
    backend/src/__tests__/routes/pinnedViews.test.ts,
    backend/src/__tests__/routes/scheduledBriefings.test.ts,
    backend/src/__tests__/queues/scheduledBriefingsWorker.test.ts,
    backend/src/__tests__/services/chatHistoryService.test.ts,
    backend/src/__tests__/ai/goldSet.test.ts,
    backend/src/__tests__/ai/tenantScopeLeak.test.ts,
    backend/src/__tests__/ai/chatActionProposal.test.ts,
    backend/src/__tests__/agent/runtimeStreaming.test.ts,
    backend/src/__tests__/agent/__fixtures__/goldSet.json
  </files>
  <read_first>
    - `backend/src/__tests__/agent/scheduler.test.ts` for cron-tier test pattern
    - `backend/src/__tests__/decisions/approveFlow.test.ts` for propose-and-confirm test pattern Phase 4 chat-action mirrors
    - `backend/src/__tests__/agent/walkingSkeleton.test.ts` for the run-log integrity invariant pattern
    - `backend/src/__tests__/agent/dismissSuppression.test.ts` for the AgentMemory probe pattern
    - `backend/jest.config.cjs` to confirm path patterns and mock resolution
  </read_first>
  <behavior>
    Each test file MUST FAIL on first run (RED). Acceptable RED states:
    - referenced module/path does not exist yet → `Cannot find module '...'`
    - Wave 1 schema not yet migrated → `PrismaClient.chatThread is undefined`
    - Skipped via `it.todo` with comment naming the wave that turns it on

    Test counts (minimum):
    - describeView.test.ts: 10 cases — one per viewType (Zod accepts valid, rejects malformed)
    - chatStream.test.ts: 7 cases — auth, tenant+user scope, text_delta event, view_block event, proposal event, heartbeat 15s, error event closes stream
    - chatThreads.test.ts: 8 cases — POST creates, GET lists+grouping, GET search via FTS, GET :id, PATCH title/pinned, DELETE soft-archive, cross-user leak, cross-tenant leak
    - pinnedViews.test.ts: 6 cases — POST extends viewType to 9 values, GET/DELETE user-scoped, cross-user leak, refreshFrequency persists, soft cap warning at 24
    - scheduledBriefings.test.ts: 5 cases — POST validates cron whitelist, invalid cron rejected, list scoped, toggle active, delete unbinds JobScheduler
    - scheduledBriefingsWorker.test.ts: 4 cases — bindBriefing upserts JobScheduler, unbindBriefing removes, worker tick creates ChatThread + assistant ChatMessage, standing_rule_v3 type is no-op (per orchestrator_resolutions §3)
    - chatHistoryService.test.ts: 6 cases — appendMessage persists, recentTurns ordered, searchChatHistory FTS uses tsvector simple config (Pitfall 5), 90-day archivedAt cleanup, tenant+user scope, search returns relevance-ranked
    - goldSet.test.ts: loops over 13 fixtures from goldSet.json; each query asserts (a) expected tools called via AgentRunLog, (b) expected viewType in result.views, (c) tenant scope, (d) one Arabic-prompt acceptance case
    - tenantScopeLeak.test.ts: seeds tenant A + tenant B with sentinel `"SENTINEL_LEAK_FROM_B"`; runs chat agent under tenant A; assert response text + views[].spec contain ZERO sentinel substrings
    - chatActionProposal.test.ts: chat agent calls draftCourierMessage → registry stages PendingAgentAction → POST `/api/decisions/:id/approve {source:"chat", threadId, msgId}` → assert AgentAction row has `source="chat"`, chatThreadId, chatMessageId populated
    - runtimeStreaming.test.ts: 4 cases — onTextDelta invoked per text delta, onView invoked when describeView returns, onPendingAction invoked when registry stages PendingAgentAction, onCancel returning true mid-iteration aborts the loop

    All tests use `prisma` from existing test infrastructure; truncate-and-reseed pattern from approveFlow.test.ts.
  </behavior>
  <action>
**Step 1 — Create the gold-set fixture file:**

Write `backend/src/__tests__/agent/__fixtures__/goldSet.json` with 13 entries (orchestrator_resolutions §5). Schema per entry:
```json
{
  "id": "gs-01-revenue-drop",
  "userMessage": "Why did revenue drop yesterday in Hawally?",
  "expectsToolCalls": ["revenueByZone", "attendanceForPeriod"],
  "expectsViewTypes": ["kpi_strip", "bar_chart", "callout"],
  "language": "en",
  "expectsActionProposal": false
}
```

Required coverage (one fixture per row):
1. gs-01-revenue-drop — "Why did revenue drop yesterday in Hawally?" → kpi_strip + bar_chart + callout
2. gs-02-cash-exposure — "What's our cash exposure across platforms this week?" → kpi_strip + table
3. gs-03-late-drivers — "Which drivers were late or missed shifts today?" → table + action_card
4. gs-04-top-performers — "Top 5 performers this week, by completion rate" → table + comparison_cards
5. gs-05-where-is-mohamed — "Where is driver Mohamed Khaled right now?" → mini_map (single marker)
6. gs-06-driver-trend — "Show me Tariq's score trend for the last 30 days" → time_series
7. gs-07-warn-drivers — "Warn the worst 3 drivers from yesterday" → table + draft_message + ACTION PROPOSAL (draftCourierMessage)
8. gs-08-cash-vs-talabat — "Compare Talabat vs Keeta cash exposure this month" → comparison_cards + bar_chart
9. gs-09-rejection-spikes — "Who's rejecting orders today?" → table + callout
10. gs-10-pin-revenue — "Pin yesterday's revenue snapshot" — assert kpi_strip emitted with pinnable=true
11. gs-11-arabic-prompt — "أعطني ملخص اليوم" (Arabic input — assert agent responds in English with at least one viewType, English response acceptable per orchestrator_resolutions §5)
12. gs-12-out-of-scope — "Delete driver record for Bader" — assert callout(info) "out of scope for v1" + zero PendingAgentAction rows
13. gs-13-no-data — "Revenue from Saturn" — assert empty kpi_strip + callout(info), no error state

**Step 2 — Write each test file:**

For each test file, follow the Phase 1/2 jest pattern (`describe → beforeEach truncate → it cases`). Tests should:
- Use `setupTestEnv()` helpers from `backend/src/__tests__/_lib/` if they exist; otherwise follow inline truncate pattern from `approveFlow.test.ts`.
- Reference future Wave 1+ modules by their target paths (e.g. `require("../../routes/chat")`). When import would crash Jest at parse time (typed import of a not-yet-shipped Prisma model), use `it.todo("…")` instead of `it("…", …)` so the file still parses.
- Each test file's first describe block MUST log a console banner identifying which Wave turns it green.

Example shape for `chatStream.test.ts`:
```typescript
/**
 * @wave 2 (Wave 0 RED — flips GREEN in Wave 2 Task 2)
 * SSE chat stream route: text_delta / view_block / proposal / heartbeat events.
 */
import request from "supertest";
import { prisma } from "../../config";
import "../../server";
describe("GET /api/ai/chat/stream (Wave 0 RED)", () => {
  beforeEach(async () => {
    // RED: chatMessage doesn't exist until Wave 1 schema migration
  });
  it.todo("authenticates user; rejects 401 without cookie");
  it.todo("emits text_delta SSE chunks while assistant streams");
  it.todo("emits view_block event when describeView tool returns");
  it.todo("emits proposal event when registry stages PendingAgentAction");
  it.todo("emits :heartbeat\\n\\n every 15s during tool-loop");
  it.todo("rejects cross-user thread access");
  it.todo("rejects cross-tenant thread access");
});
```

Each test file gets at least the listed minimum case count, in either `it()` (truly red — module-not-found ok) OR `it.todo()` (when missing schema/import would crash parsing).

**Step 3 — Verify they FAIL or are skipped:**

Run `cd backend && npm test -- --testPathPatterns=chatStream` → expect either RED (module-not-found ok) OR all `.todo` (count visible as pending). Repeat for each file. If any file's import crashes Jest at parse time, downgrade the failing import to dynamic `require()` inside the it body, or wrap in `// @ts-nocheck`.

**Why these decisions:**
- 13 gold-set fixtures = orchestrator_resolutions §5 floor. Cover all 9 viewTypes + 1 ACTION proposal flow + 1 Arabic + 1 out-of-scope + 1 empty-data state.
- `tenantScopeLeak.test.ts` is the regression mitigation for RESEARCH Pitfall 6.
- `chatActionProposal.test.ts` mirrors approveFlow.test.ts (Phase 2) — chat is just another way to file a PendingAgentAction; this test guarantees no divergence.
- `runtimeStreaming.test.ts` locks the Wave 1 `runAgent({stream})` contract before implementation — prevents API drift.
- Pure test scaffolding: do not write production code in this task.
  </action>
  <acceptance_criteria>
    - 11 test files exist under `backend/src/__tests__/`, each with a `@wave N` header comment naming the wave that flips it GREEN
    - 1 fixture file at `backend/src/__tests__/agent/__fixtures__/goldSet.json` contains 13 entries
    - Every test file includes the minimum case count from the behavior block (either `it()` RED or `it.todo`)
    - `cd backend && npm test -- --testPathPatterns="(chatStream|chatThreads|pinnedViews|scheduledBriefings|chatHistoryService|describeView|goldSet|tenantScopeLeak|chatActionProposal|runtimeStreaming|scheduledBriefingsWorker)"` runs to completion (failures or skips OK; parse errors NOT OK)
    - No production .ts file under `backend/src/` (outside __tests__) modified
    - `goldSet.json` parses (`node -e 'JSON.parse(...)'` exits 0)
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/mac/Documents/Darb/backend &amp;&amp; npm test -- --testPathPatterns="(chatStream|chatThreads|pinnedViews|scheduledBriefings|chatHistoryService|describeView|goldSet|tenantScopeLeak|chatActionProposal|runtimeStreaming|scheduledBriefingsWorker)" --listTests | grep -c "__tests__" &amp;&amp; node -e "const e=JSON.parse(require('fs').readFileSync('src/__tests__/agent/__fixtures__/goldSet.json','utf8'));if(!Array.isArray(e)||e.length<13)throw new Error('goldSet missing entries: '+e.length);console.log('goldSet has '+e.length+' entries')" &amp;&amp; ls src/__tests__/agent/__fixtures__/goldSet.json src/__tests__/routes/chatStream.test.ts src/__tests__/agent/runtimeStreaming.test.ts</automated>
  </verify>
  <done>
    - All 11 test files discovered by Jest (no parse errors)
    - goldSet.json parses with >= 13 entries
    - Tests are RED or .todo — production code unchanged
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Author frontend RED tests + viewBlock fixtures (6 test files + 1 fixture)</name>
  <files>
    frontend/src/__tests__/ai/AskDarbPalette.test.tsx,
    frontend/src/__tests__/ai/ChatViewRenderer.test.tsx,
    frontend/src/__tests__/ai/ChatActionCard.test.tsx,
    frontend/src/__tests__/decisions/PinnedViewsRail.test.tsx,
    frontend/src/__tests__/scheduled-jobs/ScheduledBriefingsList.test.tsx,
    frontend/src/__tests__/hooks/useStreamingChat.test.ts,
    frontend/src/__tests__/fixtures/chat-views.ts
  </files>
  <read_first>
    - `frontend/src/__tests__/decisions/DecisionsList.test.tsx` for the RTL+jsdom pattern Phase 2 used
    - `frontend/src/__tests__/admin/DarbsReadReport.test.tsx` for fixture-mocking pattern
    - `frontend/vitest.config.*` for the test runner config
    - `frontend/src/components/ai/AskDarbPalette.tsx` (existing — REWRITTEN in Wave 3)
    - `frontend/src/components/decisions/DecisionCard.tsx` for the propose-card shape ChatActionCard mirrors
  </read_first>
  <behavior>
    Each test file MUST fail RED on first run (component path doesn't yet exist for Wave 3 components → `import` resolution fails) OR uses `it.todo` for cases that depend on Wave 1 schema or Wave 2 routes.

    Required minimum cases:
    - **AskDarbPalette.test.tsx (5 cases):** renders, Cmd+K opens, Cmd+K closes when open, Esc closes, type-and-Enter routes to `/chat?q={q}`, recent thread click routes to `/chat?thread={id}`. Mock `useRouter` from next/navigation.
    - **ChatViewRenderer.test.tsx (10 cases):** renders all 9 viewType variants from chat-views fixture (kpi_strip, table, time_series, bar_chart, mini_map, comparison_cards, callout, action_card, draft_message) + 1 case for unknown viewType graceful fallback.
    - **ChatActionCard.test.tsx (6 cases):** renders proposal data, Approve calls onApprove, Edit opens drawer, Dismiss calls onDismiss, optimistic UI flips to "approved" with 5s undo, server 409 reverts to pending state.
    - **PinnedViewsRail.test.tsx (4 cases):** empty state hides rail entirely, ≤6 tiles render in a row, "View all" appears with >6 tiles, unpin confirm fires DELETE.
    - **ScheduledBriefingsList.test.tsx (4 cases):** renders briefings list, create form posts, standing-rule template (type=standing_rule_v3) shows "Phase 12 — won't fire yet" badge, delete confirms.
    - **useStreamingChat.test.ts (4 cases):** EventSource opens with credentials, dispatches text_delta to setter, reconnects after error with backoff, returns abort handle.

    All tests use Vitest + RTL + msw for HTTP mocks. EventSource mocking via the existing `useSSE.ts` mock pattern if available.
  </behavior>
  <action>
**Step 1 — Create the viewBlock fixtures file:**

Write `frontend/src/__tests__/fixtures/chat-views.ts` exporting one fixture per viewType:
```typescript
import type { GeneratedView } from "@/types/chat";  // path Wave 1 ships
export const fixtureKpiStrip: GeneratedView = {
  id: "fx-kpi-1", viewType: "kpi_strip", title: "Yesterday's revenue",
  spec: { tiles: [
    { label: "Revenue", value: "KD 1,247", delta: { pct: -12, direction: "down" }, tone: "danger" },
    { label: "Coverage", value: "18 / 22", delta: { pct: -18, direction: "down" }, tone: "warning" },
    { label: "Reject %", value: "4.2%", delta: { pct: 1.1, direction: "up" }, tone: "warning" },
  ]},
  pinnable: true,
};
// repeat for: fixtureTable, fixtureTimeSeries, fixtureBarChart, fixtureMiniMap,
// fixtureComparisonCards, fixtureCallout, fixtureActionCard, fixtureDraftMessage
```

Each fixture is a complete spec per UI-SPEC §3.2.4 (variant 1 through 9). Use realistic Kuwait-domain data (Hawally, Talabat/Keeta/Deliveroo platforms, KD currency).

If `@/types/chat` import would crash test discovery, add a local `type GeneratedView = any` alias at the top of the fixture file with a TODO comment naming Wave 1 Task 4.

**Step 2 — Write each test file with RTL + Vitest:**

Use the Phase 2 pattern (DecisionsList.test.tsx) as template. Each test file imports the future Wave 3+ component path; tests fail RED until the component ships. Targets:
- `@/components/ai/AskDarbPalette` (path EXISTS today; rewritten in Wave 3)
- `@/components/chat/ChatViewRenderer` (NEW — Wave 3)
- `@/components/chat/ChatActionCard` (NEW — Wave 3)
- `@/components/decisions/PinnedViewsRail` (NEW — Wave 4)
- `@/components/scheduled-jobs/ScheduledBriefingsList` (NEW — Wave 5)
- `@/hooks/useStreamingChat` (NEW — Wave 3)

Each test file MUST include a header comment naming the wave that turns it green:
```typescript
/**
 * @wave 3 (Wave 0 RED — flips GREEN in Wave 3 Task 4)
 * Tests cmdk-based AskDarbPalette: ⌘K toggle, recent threads, quick actions.
 */
```

**Step 3 — Verify RED state:**

Run `cd frontend && npm run test:run -- src/__tests__/ai/AskDarbPalette.test.tsx`. Expect either parse errors with module-not-found (acceptable RED) OR test failures with import errors. Repeat for each test file.

**Why these decisions:**
- ChatActionCard naming over UI-SPEC's "ChatProposalCard" — RESEARCH consistently uses ChatActionCard; one consistent name reduces cognitive load. UI-SPEC §3.2.5 component name is aliased — Wave 3 ships ChatActionCard.
- 9 viewType fixtures (not 7) — UI-SPEC §3.2.4 lists 9 (callout + bar_chart added vs RESEARCH); orchestrator quality_gate requires all 9.
- `useStreamingChat` over `useSSE` — RESEARCH §"Recommended Project Structure" places SSE chat in its own hook to keep notification SSE intact.
- Pure test scaffolding: do not write production code in this task.
  </action>
  <acceptance_criteria>
    - 6 test files exist under `frontend/src/__tests__/`, each with a `@wave N` header comment naming the wave that flips it GREEN
    - 1 fixture file at `frontend/src/__tests__/fixtures/chat-views.ts` exports 9 fixtures (one per viewType)
    - Every test file includes the minimum case count from behavior block
    - `cd frontend && npm run test:run -- src/__tests__/ai/AskDarbPalette.test.tsx src/__tests__/ai/ChatViewRenderer.test.tsx src/__tests__/ai/ChatActionCard.test.tsx src/__tests__/decisions/PinnedViewsRail.test.tsx src/__tests__/scheduled-jobs/ScheduledBriefingsList.test.tsx src/__tests__/hooks/useStreamingChat.test.ts` runs to completion (errors OK; parse errors NOT OK)
    - No frontend production .tsx/.ts file under `frontend/src/` (outside __tests__) modified
    - `chat-views.ts` parses
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/mac/Documents/Darb/frontend &amp;&amp; ls src/__tests__/ai/AskDarbPalette.test.tsx src/__tests__/ai/ChatViewRenderer.test.tsx src/__tests__/ai/ChatActionCard.test.tsx src/__tests__/decisions/PinnedViewsRail.test.tsx src/__tests__/scheduled-jobs/ScheduledBriefingsList.test.tsx src/__tests__/hooks/useStreamingChat.test.ts src/__tests__/fixtures/chat-views.ts &amp;&amp; node -e "const fs=require('fs');const f=['src/__tests__/ai/AskDarbPalette.test.tsx','src/__tests__/ai/ChatViewRenderer.test.tsx','src/__tests__/ai/ChatActionCard.test.tsx','src/__tests__/decisions/PinnedViewsRail.test.tsx','src/__tests__/scheduled-jobs/ScheduledBriefingsList.test.tsx','src/__tests__/hooks/useStreamingChat.test.ts'];const miss=f.filter(p=>!fs.readFileSync(p,'utf8').includes('@wave'));if(miss.length)throw new Error('missing @wave: '+miss.join(','));console.log('all 6 frontend tests have @wave headers')"</automated>
  </verify>
  <done>
    - 6 frontend RED test files + 1 fixture file exist
    - All 6 test files contain a `@wave` annotation
    - Tests fail RED (component-not-found) or `.todo` — production code unchanged
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| eslint glob → backend source | The `lint:tenant` script's argv list IS the trust boundary that catches missing tenant-scope on new agent surfaces. Adding the 7 new paths in this wave guarantees Wave 1+ files cannot land without scope enforcement. |
| Wave 0 fixtures → Wave 1+ test runs | The gold-set JSON fixture is consumed by the chat agent's eval — its shape is the contract. Bad fixtures = bad signal in later waves. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-W0-01 | Tampering | Wave 1+ chat routes ship without tenant scope | mitigate | Extend `lint:tenant` glob in Task 1 to cover all 7 new file targets; eslint custom rule `no-prisma-without-tenant` blocks merge if any new chat route omits `where: { tenantId }` |
| T-04-W0-02 | Information disclosure | Cross-tenant data leak via chat output | mitigate | Author `tenantScopeLeak.test.ts` in Task 2: seed tenant B with sentinel string, run chat under tenant A, assert sentinel never appears in response. Wave 2+ implementation must keep this test green. |
| T-04-W0-03 | Information disclosure | Cross-user chat history leak | mitigate | Author `chatThreads.test.ts` in Task 2 with explicit cross-user negative test (user A cannot read user B's thread). Wave 1 schema MUST scope by both tenantId AND userId. |
| T-04-W0-04 | Repudiation | Chat-source AgentAction rows masquerade as direct human approvals | mitigate | Author `chatActionProposal.test.ts` in Task 2: assert `AgentAction.source = "chat"` + chatThreadId + chatMessageId populated. Audit log can distinguish chat-origin from /decisions-origin. |
| T-04-W0-05 | Denial of service | Scheduled briefing cron runaway costs | mitigate | Author `scheduledBriefings.test.ts` in Task 2: invalid cron rejected (only `0 6 * * *`, `0 7 * * *` allowed by default, custom requires admin role) — UI-SPEC §9 + RESEARCH Pitfall 7 |
| T-04-W0-06 | Tampering | LLM prompt injection via chat input exfiltrates secrets | accept | Phase 2's `monitor.md` defense pattern is reused via the existing system prompt path. Wave 0 only authors `goldSet.test.ts` `gs-12-out-of-scope` to assert agent refuses delete/dangerous operations. Full prompt-injection hardening accrues over time. |
| T-04-W0-07 | Information disclosure | Generative-UI XSS via viewBlock spec | mitigate | Author `ChatViewRenderer.test.tsx` (Task 3) — every renderer goes through React text nodes (escaped); no `dangerouslySetInnerHTML`. Discriminated-union Zod schema is the contract; tests assert unknown viewType falls back to a safe error renderer. |
</threat_model>

<verification>
- `cd backend && npm test` runs full suite — Wave 0 tests are pending/RED but no parse errors, and ALL existing Phase 1+2 tests still GREEN (188 of 188).
- `cd frontend && npm run test:run` — 6 new RED tests + existing GREEN tests; total green count from Phase 2 (36/36) preserved.
- `cd backend && npm run lint:tenant` exit 0.
- `cd frontend && npm ls cmdk` finds the package.
</verification>

<success_criteria>
- 11 backend RED test files + 1 backend gold-set JSON fixture exist
- 6 frontend RED test files + 1 frontend chat-views fixture exist
- cmdk@^1.1.1 in frontend/package.json
- backend/package.json `lint:tenant` script extended with 7 new path tokens
- All Phase 1+2 tests still GREEN (no regression)
- No production code modified outside package.json files
- Every Phase 4 REQ-ID has at least one Wave 0 test marker — REQ-chat-global-access (AskDarbPalette + chatThreads search), REQ-chat-generated-dashboards (ChatViewRenderer + describeView + goldSet), REQ-chat-action-proposals (ChatActionCard + chatActionProposal), REQ-chat-scheduled-jobs (scheduledBriefings + scheduledBriefingsWorker), REQ-agent-natural-language-qa (goldSet + tenantScopeLeak), REQ-realtime-streaming (chatStream + runtimeStreaming + useStreamingChat)
</success_criteria>

<output>
After completion, create `.planning/phases/04-chat-generative-ui-websocket/04-00-SUMMARY.md` capturing:
- File counts (RED tests added; fixtures added; package.json deltas)
- The three founder-level decisions locked in fixtures (90-day retention via chatHistoryService.test.ts archive case; standing_rule_v3 no-op via worker test; SSE-only via runtimeStreaming + chatStream test contracts)
- Any deferrals (e.g. Zustand decision deferred to Wave 3 executor)
- The number of `it.todo` vs `it()` per test file (so Wave 1+ executors know what to flip GREEN first)
</output>
