---
phase: 02-decisions-surface-propose-and-confirm-design-partner-1
plan: 05
type: execute
wave: 5
depends_on:
  - 02-03
  - 02-04
files_modified:
  - frontend/src/lib/adminApi.ts
  - frontend/src/types/admin.ts
  - frontend/src/components/admin/OnboardingStepper.tsx
  - frontend/src/components/admin/onboarding/TenantInfoStep.tsx
  - frontend/src/components/admin/onboarding/CourierImportStep.tsx
  - frontend/src/components/admin/onboarding/PlatformCredentialsStep.tsx
  - frontend/src/components/admin/onboarding/BackwashStep.tsx
  - frontend/src/components/admin/onboarding/ReportPreview.tsx
  - frontend/src/components/admin/DarbsReadReport.tsx
  - frontend/src/components/admin/OverrideToggle.tsx
  - frontend/src/app/(dashboard)/admin/onboarding/page.tsx
  - frontend/src/app/(dashboard)/admin/onboarding/[tenantId]/report/page.tsx
  - frontend/src/app/(dashboard)/admin/billing/page.tsx
  - frontend/src/app/(dashboard)/admin/billing/[tenantId]/page.tsx
  - backend/prisma/migrations/20260510000000_decisions_billing_admin_models/migration.sql
autonomous: false
requirements:
  - REQ-pricing-model
  - REQ-gtm-onboarding

must_haves:
  truths:
    - "/admin/onboarding renders a 5-step wizard with stepper + per-step forms wired to Wave 4 endpoints"
    - "Step 4 (Backwash) polls GET backwash-status every 5 seconds and renders per-platform progress bars"
    - "Step 5 (Report Preview) renders DarbsReadReport with all 9 sections from buildOnboardingReport API"
    - "DarbsReadReport renders with bg-white background (no gradient bg, no watermark per feedback_invoices_white_background) — turns DarbsReadReport.test.tsx GREEN"
    - "Print-to-PDF action exists but does NOT auto-fire (per feedback_invoices_no_pdf — only fires on explicit user click)"
    - "/admin/billing renders DataTable of tenants with Active / Bill / Override / Trial columns + KPI strip (Total tenants / MRR / Active couriers across fleets)"
    - "/admin/billing/[tenantId] renders single-tenant detail with override toggle + past 6 months chart + past invoices table"
    - "Override toggle: enabling fires PATCH /override with reason; written to audit"
    - "Admin pages 403 for non-super-admin users (server-side enforced via requireSuperAdmin)"
    - "[BLOCKING] prisma migrate dev creates additive migration with 0 destructive operations"
    - "After migration: prisma migrate status reports 'Database schema is up to date!'"
    - "Phase 2 verification: design partner #1 dry-run completes — wizard runs end-to-end against a seeded fake tenant in dev, report renders with all 9 sections, KD 100/month override applied"
  artifacts:
    - path: frontend/src/lib/adminApi.ts
      provides: "Typed axios wrappers for /api/admin/onboarding/* and /api/admin/billing/*"
      contains: "approveDecision"
    - path: frontend/src/components/admin/DarbsReadReport.tsx
      provides: "9-section HTML report with white background, print-friendly"
      contains: "bg-white"
    - path: frontend/src/components/admin/OnboardingStepper.tsx
      provides: "5-pill stepper with current/completed/future state"
      contains: "currentStep"
    - path: frontend/src/components/admin/onboarding/TenantInfoStep.tsx
      provides: "Step 1 form: tenant name + owner contact + fleet size + tenant type radio"
      contains: "designPartner"
    - path: frontend/src/components/admin/onboarding/CourierImportStep.tsx
      provides: "Step 2 drag-drop XLSX/CSV upload with validation chip"
      contains: "civilId"
    - path: frontend/src/components/admin/onboarding/PlatformCredentialsStep.tsx
      provides: "Step 3 platform cards (Keeta/Talabat/Deliveroo/Americana) with test-connection buttons"
      contains: "Test connection"
    - path: frontend/src/components/admin/onboarding/BackwashStep.tsx
      provides: "Step 4 progress poller (5s interval) with per-platform bars"
      contains: "5_000"
    - path: frontend/src/components/admin/onboarding/ReportPreview.tsx
      provides: "Step 5 report preview + Send report + Mark as design partner buttons"
      contains: "start-trial"
    - path: frontend/src/components/admin/OverrideToggle.tsx
      provides: "Toggle UI for design-partner override on /admin/billing/[tenantId]"
      contains: "monthlyOverrideKd"
    - path: frontend/src/app/(dashboard)/admin/onboarding/page.tsx
      provides: "Multi-step wizard container, super-admin only"
      contains: "OnboardingStepper"
    - path: frontend/src/app/(dashboard)/admin/billing/page.tsx
      provides: "Billing dashboard with tenant table + KPI strip"
      contains: "monthlyBillKd"
    - path: backend/prisma/migrations/20260510000000_decisions_billing_admin_models/migration.sql
      provides: "Additive migration: 4 ALTER TABLE ADD COLUMN statements; 0 destructive ops"
      contains: "ALTER TABLE"
  key_links:
    - from: frontend/src/app/(dashboard)/admin/onboarding/page.tsx
      to: frontend/src/lib/adminApi.ts
      via: "step-by-step calls to onboarding endpoints"
      pattern: "createTenant|importCouriers|setPlatformCredentials|runBackwash|getBackwashStatus|getReport|startTrial"
    - from: frontend/src/components/admin/DarbsReadReport.tsx
      to: frontend/src/components/shared/StatCard.tsx
      via: "section 2 renders 5 StatCards"
      pattern: "StatCard"
    - from: frontend/src/components/admin/OverrideToggle.tsx
      to: frontend/src/lib/adminApi.ts
      via: "PATCH /override on toggle save"
      pattern: "patchOverride"
    - from: frontend/src/app/(dashboard)/admin/billing/page.tsx
      to: backend/src/routes/admin/billing.ts
      via: "GET /api/admin/billing/tenants"
      pattern: "/admin/billing/tenants"

threat_model:
  trust_boundaries:
    - "founder browser -> /admin/onboarding 5-step wizard"
    - "founder browser -> /admin/billing override toggle"
    - "production DB <- prisma migrate dev migration SQL"
  threats:
    - id: T-02-27
      category: Tampering
      component: "wizard step skipping (e.g., POST start-trial before step 4 completes)"
      disposition: mitigate
      mitigation: "Backend POST start-trial does NOT depend on prior steps for correctness — sets tenant fields atomically. Frontend wizard validates step preconditions client-side (Step 5 requires reportId from Step 4 response) but server is the gate. Bypassing the wizard cannot create an inconsistent tenant."
    - id: T-02-28
      category: Information disclosure
      component: "report HTML downloaded by founder leaks PII to email recipient"
      disposition: accept
      mitigation: "Report contains only first names + zone names + aggregate financials; no phone / civilId / address (T-02-26). Founder is responsible for distribution. Sharable expiring link is deferred (orchestrator decision #6)."
    - id: T-02-29
      category: Denial of service
      component: "[BLOCKING] migration on production DB during deploy"
      disposition: mitigate
      mitigation: "Migration is additive (4 ALTER TABLE ADD COLUMN with DEFAULT values + nullable columns). PostgreSQL applies these as metadata-only ops on PG12+ (no full table rewrite). Verified: migration SQL contains 0 destructive ops (DROP TABLE | DROP COLUMN | ALTER COLUMN .. DROP). Production deploy via Vercel postinstall `prisma migrate deploy` runs the same SQL."
    - id: T-02-30
      category: Tampering
      component: "OverrideToggle reason field bypass via curl"
      disposition: mitigate
      mitigation: "Backend PATCH /override route validates body.reason (400 if missing or empty after trim). Frontend disables save until reason is non-empty. Server is the gate."

---

<objective>
Wave 5 — Close out Phase 2:

1. **Frontend admin surfaces** built on top of Wave 4 backend:
   - `/admin/onboarding` 5-step wizard (TenantInfo / CourierImport / PlatformCredentials / Backwash / ReportPreview)
   - `/admin/onboarding/[tenantId]/report` standalone report page
   - `/admin/billing` dashboard
   - `/admin/billing/[tenantId]` per-tenant detail with override toggle
2. **DarbsReadReport** component — 9-section HTML, bg-white, print-friendly. Turns `DarbsReadReport.test.tsx` GREEN (Wave 0).
3. **[BLOCKING] Prisma migration** — `prisma migrate dev --name decisions_billing_admin_models` runs at the end of this wave. Migration must be additive (4 ALTER TABLE ADD COLUMN statements). Match Phase 1's pattern: blocking migration ships at phase close-out.
4. **Design partner #1 dry-run** — checkpoint task running the wizard end-to-end against a seeded fake tenant in dev, verifying:
   - Wizard completes without errors
   - Report renders all 9 sections
   - KD 100/month override applied
   - 14-day trial set
   - AgentAction audit row written for the override

This is the final Phase 2 plan. After this, the founder onboards design partner #1 in person, charging KD 100/month against the KD 200 floor.

Output: 14 frontend files + 1 migration file. Wave 0's `DarbsReadReport.test.tsx` turns from RED to GREEN. Phase 2 verification can run.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-decisions-surface-propose-and-confirm-design-partner-1/02-RESEARCH.md
@.planning/phases/02-decisions-surface-propose-and-confirm-design-partner-1/02-VALIDATION.md
@.planning/phases/02-decisions-surface-propose-and-confirm-design-partner-1/02-UI-SPEC.md
@.planning/phases/02-decisions-surface-propose-and-confirm-design-partner-1/02-04-SUMMARY.md

# Existing components to REUSE
@frontend/src/components/shared/SlidePanel.tsx
@frontend/src/components/shared/ConfirmModal.tsx
@frontend/src/components/shared/DataTable.tsx
@frontend/src/components/shared/FilterBar.tsx
@frontend/src/components/shared/StatCard.tsx
@frontend/src/components/shared/Skeleton.tsx
@frontend/src/components/shared/ErrorState.tsx
@frontend/src/components/shared/Toast.tsx

# Existing patterns to MIRROR
@frontend/src/lib/api.ts
@frontend/src/components/layout/SidebarV2.tsx
@frontend/tailwind.config.ts
@frontend/src/app/globals.css
@frontend/src/__tests__/admin/DarbsReadReport.test.tsx

# Backend contracts (Wave 4)
@backend/src/routes/admin/onboarding.ts
@backend/src/routes/admin/billing.ts
@backend/src/services/billing/billingService.ts
@backend/src/services/onboarding/onboardingReport.ts

# Migration reference
@backend/prisma/schema.prisma
@backend/prisma/migrations/20260509180000_add_agent_spine_models/migration.sql

<interfaces>
<!-- Backend admin API responses (Wave 4 contracts the frontend consumes). -->

POST /api/admin/onboarding/tenants -> { tenantId: string, ownerUserId: string, tempPassword: string }
POST /api/admin/onboarding/tenants/:tid/couriers/import -> { totalRows, valid, invalid: { missingPhone, duplicateCivilId }, created }
POST /api/admin/onboarding/tenants/:tid/platform-credentials -> { connected: boolean, error?: string }
POST /api/admin/onboarding/tenants/:tid/run-backwash -> { jobId: string }
GET  /api/admin/onboarding/tenants/:tid/backwash-status?jobId=X -> { jobId, state, progress: { step, totalSteps, message } }
GET  /api/admin/onboarding/tenants/:tid/report -> ReportData (9 sections)
POST /api/admin/onboarding/tenants/:tid/start-trial -> { tenantId, trialEndsAt, designPartner, monthlyOverrideKd, audit }

GET  /api/admin/billing/tenants?month=YYYY-MM -> { tenants: BillingTenant[], totals: { tenantCount, mrrKd, activeCouriersAcrossFleets } }
GET  /api/admin/billing/tenants/:tid -> { bill, past6Months, pastInvoices }
PATCH /api/admin/billing/tenants/:tid/override { override, reason } -> { tenantId, override, audit }

ReportData shape (matches Wave 4 buildOnboardingReport return):
{
  cover: { tenantName, fleetSize, dateRange: {from, to}, founderSignatureLine },
  topLineNumbers: { totalOrders, totalRevenueKd, courierCount, totalOnlineHours, completionRate },
  top5Performers: Array<{ driverId, driverName, score, ordersCompleted }>,
  bottom5Performers: Array<{ driverId, driverName, score, ordersCompleted, agentCritique }>,
  cashExposure: { totalOutstandingKd, byPlatform, top3RiskyReceivables },
  violations: { countByType, mostCommonPattern },
  whatDarbWouldHaveDone: Array<DecisionCardData>,
  whatThisCosts: { fleetSize, computedKd, floorKd, overrideKd?, netKd, breakdown },
  footer: { contactEmail, signatureLine, trialStartButtonHref }
}
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build admin types + API client + DarbsReadReport (turns DarbsReadReport.test.tsx green)</name>
  <files>
    frontend/src/lib/adminApi.ts
    frontend/src/types/admin.ts
    frontend/src/components/admin/DarbsReadReport.tsx
  </files>
  <read_first>
    backend/src/routes/admin/onboarding.ts (Wave 4 — exact response shapes)
    backend/src/routes/admin/billing.ts
    backend/src/services/onboarding/onboardingReport.ts (ReportData shape)
    frontend/src/lib/decisionsApi.ts (Wave 3 — pattern to mirror)
    frontend/src/components/shared/StatCard.tsx (used in section 2 of report)
    frontend/src/components/decisions/DecisionCard.tsx (read-only variant for section 7)
    frontend/src/__tests__/admin/DarbsReadReport.test.tsx (Wave 0 RED test)
    .planning/phases/02-decisions-surface-propose-and-confirm-design-partner-1/02-UI-SPEC.md (§3.4.3 9 sections)
  </read_first>
  <action>
**types/admin.ts** — Export TS interfaces matching the Wave 4 API responses (verbatim — see <interfaces> above):
- TenantInfoFormData, CreateTenantResponse, ImportSummary, PlatformCredsResponse, BackwashStatus, ReportData (with all 9 nested section types), BillingTenant, BillingTotals, BillingDetail, OverrideUpdateRequest, etc.

**lib/adminApi.ts** — Typed axios wrappers:
- createTenant(data) -> Promise<CreateTenantResponse>
- importCouriers(tenantId, file: File) -> Promise<ImportSummary>
- setPlatformCredentials(tenantId, data) -> Promise<PlatformCredsResponse>
- runBackwash(tenantId, body?) -> Promise<{jobId}>
- getBackwashStatus(tenantId, jobId) -> Promise<BackwashStatus>
- getReport(tenantId) -> Promise<ReportData>
- startTrial(tenantId, body) -> Promise<{tenantId, trialEndsAt, designPartner, monthlyOverrideKd, audit}>
- listBilling(month?) -> Promise<{tenants, totals}>
- getBillingDetail(tenantId) -> Promise<BillingDetail>
- patchOverride(tenantId, body) -> Promise<{tenantId, override, audit}>

**DarbsReadReport.tsx** — Props: `{ data: ReportData; printMode?: boolean }`.

Layout (per UI-SPEC §3.4.3):
- Outer: bg-white text-slate-900 max-w-[8.5in] mx-auto p-12 (A4-ish). NO gradient, NO watermark (per feedback_invoices_white_background user memory). border border-sand-200 print:border-0.
- Section 1 — Cover: large h1 (font-display text-[40px]) tenant name + subtitle date range + founder signature line in italics.
- Section 2 — Top-line numbers: 5 StatCards in a 5-column grid (responsive: 2-column on mobile). Each StatCard shows label + value + small unit.
- Section 3 — Top 5 performers: ranked table (rank, driverName, score, ordersCompleted). Driver name only — NO PHONE/CIVILID per T-02-26.
- Section 4 — Bottom 5 performers: same table + agentCritique column rendered as italic text.
- Section 5 — Cash exposure: total in big number; per-platform breakdown as 4 small tiles (Keeta yellow / Talabat orange / Deliveroo teal / Americana blue per platform tag colours in UI-SPEC §7.3); top 3 risky receivables as a small list.
- Section 6 — Violations: countByType as a horizontal bar chart (use a simple inline SVG; no chart library); mostCommonPattern as a callout box bg-amber-50 border border-amber-200.
- Section 7 — What Darb would have done: render up to 10 read-only DecisionCard variants (use DecisionCard with `state="approved"` and pass a special `readOnly` prop OR render a simplified version inline — preferred: inline simpler variant to avoid the optimistic-UI baggage).
- Section 8 — What this costs: large number `KD ${data.whatThisCosts.netKd.toFixed(3)}/month` + breakdown text below.
- Section 9 — Footer: contactEmail, signatureLine, "Start 14-day trial" button (link to start-trial endpoint trigger).
- Print-to-PDF affordance: a small button at the top-right "Print to PDF" that triggers `window.print()` ONLY on explicit click. Do NOT auto-fire on mount (per feedback_invoices_no_pdf).
- @media print: hide the Print button + page footer; keep content.

Run `cd frontend && npx vitest run src/__tests__/admin/DarbsReadReport.test.tsx`. Expect 3/3 GREEN (renders 9 sections, bg-white present, no auto-PDF). Use the direct vitest binary — the non-zero exit code is the gate.
  </action>
  <verify>
    <automated>cd frontend && ls src/lib/adminApi.ts src/types/admin.ts src/components/admin/DarbsReadReport.tsx | wc -l | xargs -I {} test {} -eq 3 && cd frontend && grep -c "createTenant\|importCouriers\|runBackwash\|patchOverride" src/lib/adminApi.ts | xargs -I {} test {} -ge 4 && cd frontend && grep -c "ReportData" src/types/admin.ts | xargs -I {} test {} -ge 1 && cd frontend && grep -c "bg-white" src/components/admin/DarbsReadReport.tsx | xargs -I {} test {} -ge 1 && cd frontend && grep -c "window.print\|print()" src/components/admin/DarbsReadReport.tsx | xargs -I {} test {} -ge 1 && cd frontend && npx vitest run src/__tests__/admin/DarbsReadReport.test.tsx && cd frontend && npx tsc --noEmit 2>&1 | grep -c "error TS" | xargs -I {} test {} -eq 0</automated>
  </verify>
  <done>
    types/admin.ts + lib/adminApi.ts + DarbsReadReport.tsx exist. DarbsReadReport renders with bg-white, no auto-PDF, all 9 sections. Wave-0 DarbsReadReport.test.tsx (3 tests) GREEN. TypeScript 0 errors.
  </done>
</task>

<task type="auto">
  <name>Task 2: Build 5 onboarding step components + Stepper + OverrideToggle</name>
  <files>
    frontend/src/components/admin/OnboardingStepper.tsx
    frontend/src/components/admin/onboarding/TenantInfoStep.tsx
    frontend/src/components/admin/onboarding/CourierImportStep.tsx
    frontend/src/components/admin/onboarding/PlatformCredentialsStep.tsx
    frontend/src/components/admin/onboarding/BackwashStep.tsx
    frontend/src/components/admin/onboarding/ReportPreview.tsx
    frontend/src/components/admin/OverrideToggle.tsx
  </files>
  <read_first>
    frontend/src/lib/adminApi.ts (just built)
    frontend/src/types/admin.ts
    frontend/src/components/admin/DarbsReadReport.tsx (used inside ReportPreview)
    frontend/src/components/shared/ConfirmModal.tsx (OverrideToggle uses for confirm dialog)
    frontend/src/components/shared/DataTable.tsx (CourierImportStep preview table)
    .planning/phases/02-decisions-surface-propose-and-confirm-design-partner-1/02-UI-SPEC.md (§3.4.2 5-step wizard + §3.5.3 override toggle)
  </read_first>
  <action>
**OnboardingStepper.tsx:** Props: `{ currentStep: 1 | 2 | 3 | 4 | 5, steps: Array<{key,label}> }`. Row of 5 pills. Each pill:
- Future: bg-sand-100 text-sand-500
- Current: bg-primary text-white
- Completed: bg-primary/10 text-primary with Check icon
Connecting lines between pills (1px sand-300; primary when both ends completed).

**TenantInfoStep.tsx:** Props: `{ onNext: (data: CreateTenantResponse) => void }`. Form fields per UI-SPEC §3.4.2 Step 1:
- name (input, required, min 3)
- ownerName (input, required)
- ownerEmail (input, email validation, required)
- ownerPhone (input, Kuwait format `+965 9XXX XXXX`, required, regex /^\+965 \d{4} \d{4}$/)
- fleetSizeEstimate (number input, min 50 max 500, required)
- tenantType radio: Standard (KD 200/mo floor) / Design partner (KD 100/mo override). Default Standard.

Submit button "Save & continue" (disabled until valid). On submit: call createTenant(data), pass response to onNext. Show error toast on backend failure.

**CourierImportStep.tsx:** Props: `{ tenantId: string, onNext: () => void, onSkip: () => void }`. UI per UI-SPEC §3.4.2 Step 2:
- Drag-drop zone for XLSX/CSV (use existing pattern from keeta/import — check codebase for shared file-drop component; if none, build inline).
- Required columns: name, phone, civilId, platformId, vehicleType, contractType.
- Live preview using DataTable (max 25 rows visible; client-side parse via xlsx library if available, else just upload and let server parse).
- After upload: show validation chip `✓ {valid} valid · ⚠ {invalid.missingPhone} missing phone · ✗ {invalid.duplicateCivilId} duplicate civilId`.
- Import button calls importCouriers(tenantId, file); on success move to onNext.
- Skip button (always available) advances to Step 3.

**PlatformCredentialsStep.tsx:** Props: `{ tenantId: string, onNext: () => void }`. 4 cards (Keeta / Talabat / Deliveroo / Americana). Each card:
- Toggle "Enable {platform}"
- If enabled: 2 inputs (username + password) + "Test connection" button
- Status chip per platform: ✓ Connected / ✗ Failed: {reason} / — Not tested
- Note: per DEC-scrapers-as-adapter-layer mobile-app-as-first-source preferred; copy: "Until your couriers install the Darb mobile app, we'll use scraping. We'll switch automatically when the app rolls out."

Continue button calls setPlatformCredentials per enabled platform; advance.

**BackwashStep.tsx:** Props: `{ tenantId: string, onComplete: (reportId: string) => void }`. UI per UI-SPEC §3.4.2 Step 4:
- Single big primary CTA "Run Darb's read on your fleet (30 days)".
- On click: call runBackwash(tenantId), store jobId.
- Poll getBackwashStatus(tenantId, jobId) every 5_000 ms.
- Render per-platform progress bars based on response.progress.message; estimated time displayed "~4–8 minutes".
- Once status === "completed": show "Continue to report" button -> onComplete(jobId or whatever the returned reportId is).
- On status === "failed": show ErrorState with retry button.

**ReportPreview.tsx:** Props: `{ tenantId: string, onComplete: () => void }`. UI per UI-SPEC §3.4.2 Step 5:
- Calls getReport(tenantId) on mount; shows Skeleton during load.
- Renders <DarbsReadReport data={reportData} /> in a scrollable container.
- 2 buttons:
  - "Send report to owner" (Phase 2 stub: calls a placeholder endpoint or just shows toast "Email sending ships in Phase 9 — print and share manually for now").
  - "Mark as design partner & start trial" -> opens ConfirmModal: "Set Tenant.designPartner=true, monthlyOverrideKd=100, trialEndsAt=now+14d?" -> on confirm calls startTrial(tenantId, { designPartner: true, overrideKd: 100 }) -> shows success toast `"Tenant provisioned. Decisions inbox will populate within 1 hour."` -> onComplete().

**OverrideToggle.tsx:** Props: `{ tenantId: string; currentOverride: number | null; onSave: (override: number | null, reason: string) => void }`. UI per UI-SPEC §3.5.3:
- Switch labeled "Design partner override".
- When enabled: input for override amount (number, KD 3-decimal format) + textarea for "Override reason (audit-only)" (required, min 10 chars).
- Save button: disabled until reason is non-empty. Opens ConfirmModal "Apply override KD ${override} for tenant?" -> on confirm calls onSave + shows toast.
- When override > floor (KD 200): show warning toast "Override raises bill above standard floor. Confirm?".
- Disable button calls onSave(null, "Override removed: {reason}"). Reason still required for audit.

All step components must show clear error states when API calls fail (use existing Toast + ErrorState).
  </action>
  <verify>
    <automated>cd frontend && ls src/components/admin/OnboardingStepper.tsx src/components/admin/onboarding/TenantInfoStep.tsx src/components/admin/onboarding/CourierImportStep.tsx src/components/admin/onboarding/PlatformCredentialsStep.tsx src/components/admin/onboarding/BackwashStep.tsx src/components/admin/onboarding/ReportPreview.tsx src/components/admin/OverrideToggle.tsx | wc -l | xargs -I {} test {} -eq 7 && cd frontend && grep -c "5_000\|5000" src/components/admin/onboarding/BackwashStep.tsx | xargs -I {} test {} -ge 1 && cd frontend && grep -c "designPartner" src/components/admin/onboarding/TenantInfoStep.tsx | xargs -I {} test {} -ge 1 && cd frontend && grep -c "DarbsReadReport" src/components/admin/onboarding/ReportPreview.tsx | xargs -I {} test {} -ge 1 && cd frontend && grep -c "monthlyOverrideKd\|currentOverride" src/components/admin/OverrideToggle.tsx | xargs -I {} test {} -ge 1 && cd frontend && npx tsc --noEmit 2>&1 | grep -c "error TS" | xargs -I {} test {} -eq 0</automated>
  </verify>
  <done>
    7 admin components built. BackwashStep polls every 5s. TenantInfoStep enforces Kuwait phone format. ReportPreview embeds DarbsReadReport. OverrideToggle requires reason for audit. TypeScript 0 errors.
  </done>
</task>

<task type="auto">
  <name>Task 3: Build 4 admin pages (onboarding, onboarding/[tid]/report, billing, billing/[tid])</name>
  <files>
    frontend/src/app/(dashboard)/admin/onboarding/page.tsx
    frontend/src/app/(dashboard)/admin/onboarding/[tenantId]/report/page.tsx
    frontend/src/app/(dashboard)/admin/billing/page.tsx
    frontend/src/app/(dashboard)/admin/billing/[tenantId]/page.tsx
  </files>
  <read_first>
    frontend/src/components/admin/OnboardingStepper.tsx + 5 step components (just built)
    frontend/src/components/admin/DarbsReadReport.tsx
    frontend/src/components/admin/OverrideToggle.tsx
    frontend/src/lib/adminApi.ts
    frontend/src/components/shared/DataTable.tsx (billing table)
    frontend/src/components/shared/StatCard.tsx (KPI strip)
    .planning/phases/02-decisions-surface-propose-and-confirm-design-partner-1/02-UI-SPEC.md (§3.4 onboarding + §3.5 billing)
  </read_first>
  <action>
**`app/(dashboard)/admin/onboarding/page.tsx`** (super-admin only — server-side enforced; client checks `useAuth().user.isSuperAdmin` for graceful redirect on mount):

- Renders <OnboardingStepper currentStep={state.step} steps={[{key:"tenant",label:"Tenant"},{key:"couriers",label:"Couriers"},{key:"platforms",label:"Platforms"},{key:"backwash",label:"Backwash"},{key:"report",label:"Report"}]} />
- State machine: { step: 1, tenantId?: string, jobId?: string, completedSteps: number[] }
- Conditional render of step component:
  - step=1 -> <TenantInfoStep onNext={(res) => setState({step:2, tenantId: res.tenantId})} />
  - step=2 -> <CourierImportStep tenantId={state.tenantId} onNext={()=>setState({step:3})} onSkip={()=>setState({step:3})} />
  - step=3 -> <PlatformCredentialsStep tenantId={state.tenantId} onNext={()=>setState({step:4})} />
  - step=4 -> <BackwashStep tenantId={state.tenantId} onComplete={(jobId)=>setState({step:5,jobId})} />
  - step=5 -> <ReportPreview tenantId={state.tenantId} onComplete={()=>router.push(`/admin/billing/${state.tenantId}`)} />
- Top-right "Cancel" link navigates back to /admin/onboarding (resets state).
- Page guarded: if !user.isSuperAdmin, replace content with "403 — Super-admin access required" + Link back to /decisions.

**`app/(dashboard)/admin/onboarding/[tenantId]/report/page.tsx`**:
- Server component fetches getReport(params.tenantId).
- Renders <DarbsReadReport data={reportData} /> standalone (no wizard chrome, just the report). Useful for sharing print.
- 403 wrapper for non-super-admin.

**`app/(dashboard)/admin/billing/page.tsx`** (per UI-SPEC §3.5.2):
- Header: h1 "Billing" + subtitle.
- KPI strip: 3 StatCards (Total tenants / MRR (KD) / Active couriers across fleets) from listBilling().totals.
- Month picker (default current month).
- DataTable with columns: Tenant (name with link to detail), Plan, Active (couriers), Bill (KD), Override (chip if applied), Trial (days remaining if trialEndsAt).
- Click row -> /admin/billing/{tenantId}.

**`app/(dashboard)/admin/billing/[tenantId]/page.tsx`** (per UI-SPEC §3.5.3):
- Header: tenant name + plan + designPartner badge.
- This-month bill section: big number (KD 3-decimal) + breakdown text from billingDetail.bill.breakdown (already shaped server-side).
- Active-courier definition tooltip: "A courier is 'active' if they were online for ≥ 4 hours in any single day this month."
- <OverrideToggle tenantId={params.tenantId} currentOverride={bill.override} onSave={(override, reason) => patchOverride(params.tenantId, {override, reason})} />.
- Past 6 months chart: simple inline SVG bar chart (no library) showing trailing 6 months of netKd.
- Past invoices: DataTable; each row has download buttons "HTML" and "Make PDF" (Make PDF only triggers window.print on a hidden iframe per feedback_invoices_no_pdf — only on explicit click).
- 403 wrapper.
  </action>
  <verify>
    <automated>cd frontend && ls "src/app/(dashboard)/admin/onboarding/page.tsx" "src/app/(dashboard)/admin/onboarding/[tenantId]/report/page.tsx" "src/app/(dashboard)/admin/billing/page.tsx" "src/app/(dashboard)/admin/billing/[tenantId]/page.tsx" | wc -l | xargs -I {} test {} -eq 4 && cd frontend && grep -c "OnboardingStepper" "src/app/(dashboard)/admin/onboarding/page.tsx" | xargs -I {} test {} -ge 1 && cd frontend && grep -c "DarbsReadReport" "src/app/(dashboard)/admin/onboarding/[tenantId]/report/page.tsx" | xargs -I {} test {} -ge 1 && cd frontend && grep -c "OverrideToggle" "src/app/(dashboard)/admin/billing/[tenantId]/page.tsx" | xargs -I {} test {} -ge 1 && cd frontend && grep -c "isSuperAdmin\|403" "src/app/(dashboard)/admin/onboarding/page.tsx" | xargs -I {} test {} -ge 1 && cd frontend && npx tsc --noEmit 2>&1 | grep -c "error TS" | xargs -I {} test {} -eq 0 && cd frontend && npm run build 2>&1 | grep -c "Failed to compile" | xargs -I {} test {} -eq 0</automated>
  </verify>
  <done>
    4 admin pages built. Onboarding page is the 5-step wizard with state machine. Billing page lists tenants with KPI strip. Per-tenant detail page has OverrideToggle. Standalone report page renders for sharing. All pages graceful-degrade for non-super-admin. TypeScript 0 errors. Frontend build passes.
  </done>
</task>

<task type="auto">
  <name>Task 4: [BLOCKING] Run prisma migrate dev for the additive schema delta</name>
  <files>
    backend/prisma/migrations/20260510000000_decisions_billing_admin_models/migration.sql
  </files>
  <read_first>
    backend/prisma/schema.prisma (Wave 1 added 4 columns; verify they're still present)
    backend/prisma/migrations/20260509180000_add_agent_spine_models/migration.sql (Phase 1 reference: idempotent additive pattern)
    .planning/phases/01-backend-agent-spine-data-architecture/01-04-PLAN.md (Wave 4 — Phase 1 [BLOCKING] migration handover precedent)
    .planning/phases/01-backend-agent-spine-data-architecture/deferred-items.md (DI-01-02 — pre-existing migration-history defect; may force the same workaround pattern)
  </read_first>
  <action>
[BLOCKING] Run the migration. Match Phase 1's pattern from 01-04-PLAN.md.

**Step 1 — Pre-check:** verify schema.prisma still has the 4 Wave-1 fields:
```
cd backend && grep -E "designPartner|monthlyOverrideKd|trialEndsAt|isSuperAdmin" prisma/schema.prisma | wc -l
```
Must equal 4. If not, abort and re-add per Wave 1 Task 1.

**Step 2 — Try `prisma migrate dev` first:**
```
cd backend && npx prisma migrate dev --name decisions_billing_admin_models --create-only
```

If this succeeds (migration file generated; no shadow-DB error), Prisma will produce a migration directory at `backend/prisma/migrations/{timestamp}_decisions_billing_admin_models/migration.sql`. Inspect the generated SQL: it MUST be 4 ALTER TABLE ADD COLUMN statements, all additive, no DROPs.

If `prisma migrate dev` fails because of the pre-existing PlatformSettings/Notification baseline defect (DI-01-02), fall back to Phase 1's hand-crafted pattern:

**Step 3 — Hand-crafted migration (fallback):**
```
mkdir -p backend/prisma/migrations/20260510000000_decisions_billing_admin_models
```

Create `backend/prisma/migrations/20260510000000_decisions_billing_admin_models/migration.sql` with the following SQL:

```sql
-- Phase 2 — Decisions / Billing / Admin schema delta
-- Additive only. Zero destructive operations. PostgreSQL 12+ applies these as
-- metadata-only ops (no full table rewrite) for nullable columns and DEFAULT-ed
-- BOOLEAN columns.

-- Tenant table additions
ALTER TABLE "Tenant" ADD COLUMN IF NOT EXISTS "designPartner" BOOLEAN NOT NULL DEFAULT false;
ALTER TABLE "Tenant" ADD COLUMN IF NOT EXISTS "monthlyOverrideKd" DECIMAL(10, 3);
ALTER TABLE "Tenant" ADD COLUMN IF NOT EXISTS "trialEndsAt" TIMESTAMP(3);

-- User table addition
ALTER TABLE "User" ADD COLUMN IF NOT EXISTS "isSuperAdmin" BOOLEAN NOT NULL DEFAULT false;
```

Then apply via `prisma db push` and register as applied via `prisma migrate resolve --applied 20260510000000_decisions_billing_admin_models`:
```
cd backend && npx prisma db push --skip-generate
cd backend && npx prisma migrate resolve --applied 20260510000000_decisions_billing_admin_models
```

**Step 4 — Verify clean state:**
```
cd backend && npx prisma migrate status
cd backend && npx prisma generate
cd backend && grep -nE "DROP TABLE|DROP COLUMN|ALTER COLUMN.*DROP" prisma/migrations/20260510*/migration.sql
```

Expected:
- `prisma migrate status` reports "Database schema is up to date!"
- `prisma generate` succeeds.
- The `grep` for destructive ops returns 0 matches (exit code 1).

**Step 5 — Run the full test suite end-to-end:**
```
cd backend && npm test
```
All Phase-1 + Phase-2 backend test suites must remain GREEN. Number of suites should now equal Phase 1's 17 + Phase-2 new ones (decisions/3 + monitor/4 + scheduler/1 + billing/2 + queues/1 + onboarding/2 + middleware/1 ≈ 14 new = 31 suites).

**Step 6 — Frontend build sanity:**
```
cd frontend && npm run build
```
Expect successful compile (no TS errors, no missing imports).

**Production deploy note:** The Vercel postinstall hook (`prisma generate && prisma migrate deploy || echo skipped`) auto-applies `prisma migrate deploy` against the production DB on the next deploy. The migration file we shipped is exactly what runs there. Additive ALTER TABLE ADD COLUMN with DEFAULT is metadata-only on PG12+, so no production downtime.
  </action>
  <verify>
    <automated>cd backend && npx prisma migrate status 2>&1 | grep -c "Database schema is up to date" | xargs -I {} test {} -ge 1 && cd backend && ls prisma/migrations/20260510000000_decisions_billing_admin_models/migration.sql && cd backend && grep -nE "DROP TABLE|DROP COLUMN|ALTER COLUMN.*DROP" prisma/migrations/20260510000000_decisions_billing_admin_models/migration.sql | wc -l | xargs -I {} test {} -eq 0 && cd backend && grep -cE "ALTER TABLE.*ADD COLUMN" prisma/migrations/20260510000000_decisions_billing_admin_models/migration.sql | xargs -I {} test {} -ge 4 && cd backend && npm test 2>&1 | grep -E "Test Suites:.*passed" | grep -v failed && cd backend && npx tsc --noEmit 2>&1 | grep -c "error TS" | xargs -I {} test {} -eq 0 && cd frontend && npx tsc --noEmit 2>&1 | grep -c "error TS" | xargs -I {} test {} -eq 0</automated>
  </verify>
  <done>
    Migration file exists with 4 ALTER TABLE ADD COLUMN statements (zero destructive ops). prisma migrate status reports clean. All Phase-1 + Phase-2 backend tests GREEN. Frontend + backend TypeScript 0 errors. Production deploy will apply automatically via Vercel postinstall.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Phase 2 is complete:
    - Backend: monitor agent + 3 propose tools + listAgentMemory + tiered cron + /api/decisions/* + /api/audit/agent-actions + /api/admin/onboarding/* + /api/admin/billing/* + onboardingBackwashWorker
    - Frontend: /decisions inbox + audit log + permalink + sidebar update + 5-step admin onboarding wizard + admin billing dashboard + DarbsReadReport
    - Schema: 4 additive columns migrated (Tenant.designPartner / monthlyOverrideKd / trialEndsAt + User.isSuperAdmin)
    - 21+ tests GREEN (backend + frontend)
    Design partner #1 dry-run is the final gate.
  </what-built>
  <how-to-verify>
    **Setup:**
    1. Ensure backend running (`cd backend && npm run dev`). Ensure frontend running (`cd frontend && npm run dev`).
    2. Have a User with isSuperAdmin=true in your dev DB. If not: in prisma studio, set isSuperAdmin=true on your founder user. Confirm sidebar shows the "Admin" footer section after refresh.

    **Design Partner #1 Dry-Run:**

    1. **Run the wizard end-to-end** with a fake tenant:
       - Sign in as super-admin. Click sidebar "Onboarding".
       - Step 1 (Tenant): name="Acme Fleet (DRY RUN)", ownerName="Test Owner", ownerEmail=`dryrun-{ts}@darb.kw`, ownerPhone="+965 9999 9999", fleetSizeEstimate=150, tenantType=Design partner. Click Save & continue. Confirm a temp password is shown.
       - Step 2 (Couriers): SKIP for dry-run.
       - Step 3 (Platforms): toggle Keeta only, leave creds blank, hit Continue (or skip).
       - Step 4 (Backwash): click "Run Darb's read on your fleet". Wait for the polling indicator to advance. Job may complete quickly with empty data — that's OK for dry-run.
       - Step 5 (Report): wait for report to render. Verify all 9 sections present:
         a. Cover with tenant name "Acme Fleet (DRY RUN)" + 30-day date range
         b. 5 StatCards (totals can be 0)
         c. Top 5 performers (may be empty list — acceptable)
         d. Bottom 5 performers
         e. Cash exposure 4-platform tiles
         f. Violations countByType
         g. What Darb would have done (may be 0 cards if no PendingAgentAction in dev DB)
         h. What this costs — should show "150 active couriers × KD 2 = KD 300; override applied: KD 100; **net KD 100.000**" because designPartner=true was selected in Step 1.
         i. Footer with "Start 14-day trial" button.
       - Click "Mark as design partner & start trial". Confirm modal appears. Click confirm. Toast: "Tenant provisioned. Decisions inbox will populate within 1 hour."

    2. **Verify DB state:**
       - Open prisma studio: `cd backend && npx prisma studio`
       - Find the new Tenant row: designPartner=true, monthlyOverrideKd=100, trialEndsAt=now+14d.
       - Find the new User row: role=ADMIN, isSuperAdmin=false.
       - Find an AgentAction row: toolName="admin.startTrial", proposer="Darb", approverId=your-user-id, subjectType="Tenant", subjectId=new tenant id.

    3. **Verify billing dashboard:**
       - Navigate /admin/billing. The new tenant appears in the list with Bill=KD 100.000, Override=✓ chip, Trial=14 days.
       - Click row to /admin/billing/{tenantId}. Verify big number "KD 100.000/month" + breakdown text + override toggle showing override is applied.

    4. **Override audit trail:**
       - On /admin/billing/{tenantId}, toggle override OFF (set to null) with reason "Dry-run cleanup".
       - Verify Tenant.monthlyOverrideKd is null in DB; AgentAction row written for the change with reason="Dry-run cleanup".

    5. **Cross-tenant isolation check:**
       - Confirm that any OTHER tenant's billing row shows their actual computed bill (not KD 100). The override is scoped only to the dry-run tenant.

    6. **Cleanup (optional):**
       - Delete the dry-run tenant from prisma studio (cascades to User + AgentAction).

    **Phase 2 verification ready when:**
    - All 6 steps above completed without errors
    - Migration applied (check `npx prisma migrate status` says "up to date")
    - All test suites green (`cd backend && npm test && cd frontend && npx vitest run`)
    - Sign off in this checkpoint by typing "approved"
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
- 14 frontend admin files (lib + types + 9 components + 4 pages) created
- DarbsReadReport renders with bg-white + 9 sections + no auto-PDF
- 5-step wizard wires through Wave 4 endpoints
- /admin/billing dashboard + per-tenant detail with override toggle
- [BLOCKING] migration applied: 4 ALTER TABLE ADD COLUMN, zero destructive ops, prisma migrate status clean
- All Phase 1 + Phase 2 tests GREEN (backend + frontend)
- TypeScript 0 errors (both backend + frontend)
- lint:tenant 0 errors
- Design partner #1 dry-run completes end-to-end with audit trail correct
</verification>

<success_criteria>
- [ ] All 14 frontend files created
- [ ] DarbsReadReport.test.tsx GREEN (Wave 0 RED -> GREEN)
- [ ] cd frontend && npx vitest run all GREEN (exits 0)
- [ ] cd frontend && npx tsc --noEmit 0 errors
- [ ] cd frontend && npm run build succeeds
- [ ] Migration file exists at prisma/migrations/20260510000000_decisions_billing_admin_models/migration.sql
- [ ] Migration is purely additive (zero DROP / ALTER COLUMN DROP)
- [ ] cd backend && npx prisma migrate status reports "up to date"
- [ ] cd backend && npm test all GREEN (Phase 1 + Phase 2)
- [ ] cd backend && npx tsc --noEmit 0 errors
- [ ] cd backend && npm run lint:tenant 0 errors
- [ ] Human checkpoint passes — design partner #1 dry-run end-to-end clean
</success_criteria>

<output>
Create .planning/phases/02-decisions-surface-propose-and-confirm-design-partner-1/02-05-SUMMARY.md documenting:
- 14 frontend files added (admin surface)
- DarbsReadReport visual contract (bg-white, 9 sections, print-on-explicit-click)
- 5-step wizard state machine
- /admin/billing dashboard + override toggle audit trail
- [BLOCKING] migration outcome (4 columns, additive, applied)
- Design partner #1 dry-run results
- Phase 2 closure: ready for /gsd-verify-work and design partner #1 onboarding (charge KD 100/mo)
- Hand-off note: "Phase 3 (Driver File) — see ROADMAP. Driver name links from Decision cards become live in Phase 3."
</output>
