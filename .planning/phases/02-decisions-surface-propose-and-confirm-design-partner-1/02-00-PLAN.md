---
phase: 02-decisions-surface-propose-and-confirm-design-partner-1
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - backend/package.json
  - backend/src/__tests__/decisions/cardProjector.test.ts
  - backend/src/__tests__/decisions/approveFlow.test.ts
  - backend/src/__tests__/decisions/dismissFlow.test.ts
  - backend/src/__tests__/agent/monitor/monitoringSmoke.test.ts
  - backend/src/__tests__/agent/monitor/neverExecutes.test.ts
  - backend/src/__tests__/agent/monitor/dismissSuppression.test.ts
  - backend/src/__tests__/agent/monitor/promptRegression.test.ts
  - backend/src/__tests__/agent/monitor/fixtures/01-late-clockins.ts
  - backend/src/__tests__/agent/monitor/fixtures/02-gps-stale.ts
  - backend/src/__tests__/agent/monitor/fixtures/03-rejection-cluster.ts
  - backend/src/__tests__/agent/monitor/fixtures/04-cash-mismatch.ts
  - backend/src/__tests__/agent/monitor/fixtures/05-perf-regression.ts
  - backend/src/__tests__/agent/monitor/fixtures/06-dismissed-suppression.ts
  - backend/src/__tests__/agent/monitor/fixtures/07-empty-state.ts
  - backend/src/__tests__/agent/monitor/fixtures/08-multi-anomaly-courier.ts
  - backend/src/__tests__/agent/monitor/fixtures/09-disabled-tenant.ts
  - backend/src/__tests__/agent/monitor/fixtures/10-deleted-courier.ts
  - backend/src/__tests__/agent/monitor/fixtures/index.ts
  - backend/src/__tests__/agent/scheduler/monitorTier.test.ts
  - backend/src/__tests__/billing/billingService.test.ts
  - backend/src/__tests__/billing/overrideIsolation.test.ts
  - backend/src/__tests__/queues/onboardingBackwashWorker.test.ts
  - backend/src/__tests__/onboarding/backwashProgress.test.ts
  - backend/src/__tests__/onboarding/reportRender.test.ts
  - backend/src/__tests__/middleware/superAdmin.test.ts
  - frontend/vitest.config.ts
  - frontend/src/__tests__/decisions/DecisionsList.test.tsx
  - frontend/src/__tests__/admin/DarbsReadReport.test.tsx
autonomous: true
requirements:
  - REQ-decisions-proposal-inbox
  - REQ-agent-continuous-monitoring
  - REQ-agent-action-drafting
  - REQ-agent-propose-confirm
  - REQ-pricing-model
  - REQ-gtm-onboarding

must_haves:
  truths:
    - "Every Wave 1-5 task has a failing test waiting under __tests__/ that it will turn green"
    - "10 gold-set fixtures (one per anomaly class + suppression / empty / multi / disabled / deleted-courier) live under __tests__/agent/monitor/fixtures/"
    - "lint:tenant script scope extended to backend/src/agent/tools/action/ + backend/src/services/decisions/ + backend/src/services/billing/ + backend/src/services/onboarding/ + backend/src/routes/decisions.ts + backend/src/routes/admin/ + backend/src/middleware/superAdmin.ts"
    - "Frontend Vitest config exists at frontend/vitest.config.ts and runs cd frontend && npm test"
    - "promptRegression.test.ts asserts forbiddenToolNames includes applyPenalty and suspendDriver (Phase 8 tools must NOT appear)"
    - "neverExecutes.test.ts proves monitor agent's tool-loop NEVER reaches the .execute() body of any write tool (only .invoke() approval-gate path)"
  artifacts:
    - path: backend/src/__tests__/decisions/cardProjector.test.ts
      provides: "RED tests for projectPendingAction shape (CON-decisions-card-shape compliance)"
      contains: "projectPendingAction"
    - path: backend/src/__tests__/decisions/approveFlow.test.ts
      provides: "6 RED tests asserting approve route writes AgentAction with proposer=Darb + approverId=userId + outcome=success + originalProposal, AND a concurrency race test verifying the optimistic-lock pattern (two simultaneous approves: first 200, second 409)"
      contains: "writeAgentAction"
    - path: backend/src/__tests__/decisions/dismissFlow.test.ts
      provides: "RED tests asserting dismiss route writes AgentMemory with key=dismissed:* and value{reason,dismissedBy,dismissedAt,originalProposal}"
      contains: "dismissed:"
    - path: backend/src/__tests__/agent/monitor/monitoringSmoke.test.ts
      provides: "RED end-to-end test: cron tick -> runAgent('monitor') -> read tools called -> write tool called -> PendingAgentAction created"
      contains: "PendingAgentAction"
    - path: backend/src/__tests__/agent/monitor/neverExecutes.test.ts
      provides: "RED test enforcing monitor never executes a write tool (no notification.create, no penalty.create, etc.)"
      contains: "never executes"
    - path: backend/src/__tests__/agent/monitor/dismissSuppression.test.ts
      provides: "RED test: prior dismissed:* memory <7 days suppresses identical proposal on next monitor run"
      contains: "7 days"
    - path: backend/src/__tests__/agent/monitor/promptRegression.test.ts
      provides: "RED snapshot eval harness consuming the 10 gold-set fixtures"
      contains: "gold-set"
    - path: backend/src/__tests__/agent/monitor/fixtures/index.ts
      provides: "Aggregator exporting all 10 fixtures as GoldFixture[]"
      contains: "GoldFixture"
    - path: backend/src/__tests__/agent/scheduler/monitorTier.test.ts
      provides: "RED test: monitorTick(hot) at 1m, monitorTick(warm) at 15m, monitorTick(cold) at 1h schedules"
      contains: "monitorTick"
    - path: backend/src/__tests__/billing/billingService.test.ts
      provides: "RED tests: 150 active -> KD 300; 50 active -> KD 200 floor; override applies"
      contains: "computeMonthlyBill"
    - path: backend/src/__tests__/billing/overrideIsolation.test.ts
      provides: "RED test: tenant-A override does NOT affect tenant-B bill"
      contains: "override leakage"
    - path: backend/src/__tests__/queues/onboardingBackwashWorker.test.ts
      provides: "RED test: 30 days chunked into 5-day windows; per-platform progress events"
      contains: "5-day"
    - path: backend/src/__tests__/onboarding/backwashProgress.test.ts
      provides: "RED test: GET /backwash-status?jobId=... returns progress + step + totalSteps + message"
      contains: "totalSteps"
    - path: backend/src/__tests__/onboarding/reportRender.test.ts
      provides: "RED snapshot test: buildOnboardingReport returns ReportData with all 9 sections present (cover, topLineNumbers, top5Performers, bottom5Performers, cashExposure, violations, whatDarbWouldHaveDone, whatThisCosts, footer). whatDarbWouldHaveDone is asserted to be an array of 0..10 ReportCard objects each conforming to {action, reasoning, courierName, dateRange} — shape-based, not length-based, because production reports return whatever PendingAgentAction rows are available. The seed-design-partner-fixture script ensures the dry-run sees ≥10 rows so the production report renders the full 10."
      contains: "9 sections"
    - path: backend/src/__tests__/middleware/superAdmin.test.ts
      provides: "RED test: requireSuperAdmin returns 403 for User.isSuperAdmin=false; passes through for true"
      contains: "isSuperAdmin"
    - path: frontend/vitest.config.ts
      provides: "Vitest config so cd frontend && npm test runs the component tests"
      contains: "vitest"
    - path: frontend/src/__tests__/decisions/DecisionsList.test.tsx
      provides: "RED component test: 3 cards render via mocked /api/decisions, Cmd+Enter approves focused card"
      contains: "DecisionsList"
    - path: frontend/src/__tests__/admin/DarbsReadReport.test.tsx
      provides: "RED component test: report renders with white background (no gradient bg, per feedback_invoices_white_background)"
      contains: "white background"
    - path: backend/package.json
      provides: "lint:tenant script scope expanded to cover all Phase 2 surfaces"
      contains: "src/agent/tools/action"
  key_links:
    - from: backend/src/__tests__/agent/monitor/promptRegression.test.ts
      to: backend/src/__tests__/agent/monitor/fixtures/index.ts
      via: "import GoldFixture[] from ./fixtures"
      pattern: "from.*fixtures"
    - from: backend/package.json
      to: backend/src/agent/tools/action
      via: "lint:tenant glob includes src/agent/tools/action/"
      pattern: "src/agent/tools/action"
    - from: frontend/src/__tests__/decisions/DecisionsList.test.tsx
      to: frontend/vitest.config.ts
      via: "vitest config picks up test files under src/__tests__/"
      pattern: "include.*__tests__"

threat_model:
  trust_boundaries: []  # No new trust boundaries — Wave 0 ships tests, no production code
  threats: []           # Test scaffolding poses no STRIDE threat surface

---

<objective>
Wave 0 — Build the safety net BEFORE any Phase 2 feature code lands. This plan ships:

1. **Backend test scaffolding** (RED tests) covering every Phase 2 deliverable: card projector, approve/dismiss flows, monitoring smoke, never-executes invariant, dismiss suppression, prompt regression eval harness with 10 gold-set fixtures, monitor tier scheduling, billing math, billing override isolation, onboarding backwash chunking, onboarding progress polling, "Darb's read" report rendering, and super-admin middleware.
2. **Frontend Vitest scaffolding** + 2 RED component tests (DecisionsList, DarbsReadReport).
3. **lint:tenant scope extension** so every new Phase 2 surface is gated by the existing `no-prisma-without-tenant` ESLint rule from day one.

Per the Nyquist rule (`<verify>` MUST include `<automated>`), every Wave 1-5 task has a failing test waiting for it. Wave 0 produces the failing tests; Waves 1-5 turn them green.

Purpose: Phase 2 is the wedge phase. The agent now drafts proposals (REQ-agent-action-drafting) and the propose-and-confirm contract (REQ-agent-propose-confirm) is the v1 trust mechanism. Every regression must be caught before it reaches the design partner.

Output: 17 RED test files + 10 gold-set fixtures + extended `lint:tenant` script. `cd backend && npm run test:agent` shows failing tests for monitor + decisions + billing + onboarding scopes. `cd frontend && npm test` shows failing component tests. `cd backend && npm run lint:tenant` exits clean (the new scope contains no source files yet).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-decisions-surface-propose-and-confirm-design-partner-1/02-RESEARCH.md
@.planning/phases/02-decisions-surface-propose-and-confirm-design-partner-1/02-VALIDATION.md
@.planning/phases/02-decisions-surface-propose-and-confirm-design-partner-1/02-UI-SPEC.md
@.planning/intel/constraints.md

# Existing patterns the executor will mirror
@backend/jest.config.js
@backend/package.json
@backend/src/__tests__/agent/walkingSkeleton.test.ts
@backend/src/__tests__/agent/ledger.test.ts
@backend/src/__tests__/agent/memory.test.ts
@backend/src/__tests__/agent/tools/strict.test.ts
@backend/src/__tests__/agent/tools/tenantIsolation.test.ts
@backend/src/__tests__/mocks/agentMocks.ts
@backend/src/__tests__/mocks/config.ts

# Phase 1 patterns to study
@backend/src/agent/registry.ts
@backend/src/agent/runtime.ts
@backend/src/agent/scheduler.ts
@backend/src/agent/ledger.ts
@backend/src/agent/memory.ts

<interfaces>
<!-- Key Phase-1 contracts the executor will reference. Extracted from codebase. -->

From backend/src/agent/registry.ts:
```typescript
export type ToolSideEffect = "read" | "write" | "notify";
export interface ToolContext {
  tenantId: string;
  agentId: string;
  runId: string;
  actorRole: UserRole;
  userId?: string;  // set ONLY when a human approves
}
export interface ToolDefinition<I, O> {
  name: string;
  description: string;
  inputSchema: Anthropic.Tool["input_schema"];
  inputValidator: z.ZodType<I>;
  sideEffect: ToolSideEffect;
  requiredRole: UserRole[];
  requiresApproval: boolean;
  allowedAgents: string[];
  strict?: boolean;
  execute: (ctx: ToolContext, input: I) => Promise<O>;
}
export interface InvokeResult<O> {
  status: "executed" | "pending_approval" | "forbidden" | "error";
  output?: O;
  pendingActionId?: string;
  error?: string;
}
// invoke() writes PendingAgentAction when requiresApproval && !ctx.userId
```

From backend/src/agent/runtime.ts:
```typescript
export type AgentId = "triage" | "reconciliation" | "narrator" | "chat";
// Phase 2 will EXTEND this to include "monitor"
export interface RunAgentInput {
  tenantId: string;
  triggerEvent: string;
  payload?: Record<string, unknown>;
  userMessage?: string;
  history?: Array<{ role: "user" | "assistant"; content: string }>;
}
export interface RunAgentResult {
  runId: string;
  status: "completed" | "failed" | "disabled";
  text?: string;
  actionsProposed: number;
  pendingActionIds: string[];
  error?: string;
}
```

From backend/src/agent/index.ts (re-exports):
```typescript
export { writeAgentAction } from "./ledger";
export { upsertAgentMemory, latestMemoryByKey, listMemoriesByPrefix } from "./memory";
```

From backend/prisma/schema.prisma (existing models Phase 2 reads/writes):
- PendingAgentAction (existing) — Phase 2 fills via monitor agent + reads via /api/decisions
- AgentAction (existing) — Phase 2 writes via approve route
- AgentMemory (existing) — Phase 2 writes via dismiss route with key=dismissed:*
- AgentRunLog (existing) — every monitor invocation persists one row
- Tenant (existing) — Phase 2 ADDS columns: designPartner, monthlyOverrideKd, trialEndsAt
- User (existing) — Phase 2 ADDS column: isSuperAdmin
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create 14 backend RED test files + 10 gold-set fixtures</name>
  <files>
    backend/src/__tests__/decisions/cardProjector.test.ts,
    backend/src/__tests__/decisions/approveFlow.test.ts,
    backend/src/__tests__/decisions/dismissFlow.test.ts,
    backend/src/__tests__/agent/monitor/monitoringSmoke.test.ts,
    backend/src/__tests__/agent/monitor/neverExecutes.test.ts,
    backend/src/__tests__/agent/monitor/dismissSuppression.test.ts,
    backend/src/__tests__/agent/monitor/promptRegression.test.ts,
    backend/src/__tests__/agent/monitor/fixtures/index.ts,
    backend/src/__tests__/agent/monitor/fixtures/01-late-clockins.ts,
    backend/src/__tests__/agent/monitor/fixtures/02-gps-stale.ts,
    backend/src/__tests__/agent/monitor/fixtures/03-rejection-cluster.ts,
    backend/src/__tests__/agent/monitor/fixtures/04-cash-mismatch.ts,
    backend/src/__tests__/agent/monitor/fixtures/05-perf-regression.ts,
    backend/src/__tests__/agent/monitor/fixtures/06-dismissed-suppression.ts,
    backend/src/__tests__/agent/monitor/fixtures/07-empty-state.ts,
    backend/src/__tests__/agent/monitor/fixtures/08-multi-anomaly-courier.ts,
    backend/src/__tests__/agent/monitor/fixtures/09-disabled-tenant.ts,
    backend/src/__tests__/agent/monitor/fixtures/10-deleted-courier.ts,
    backend/src/__tests__/agent/scheduler/monitorTier.test.ts,
    backend/src/__tests__/billing/billingService.test.ts,
    backend/src/__tests__/billing/overrideIsolation.test.ts,
    backend/src/__tests__/queues/onboardingBackwashWorker.test.ts,
    backend/src/__tests__/onboarding/backwashProgress.test.ts,
    backend/src/__tests__/onboarding/reportRender.test.ts,
    backend/src/__tests__/middleware/superAdmin.test.ts
  </files>
  <read_first>
    backend/src/__tests__/agent/walkingSkeleton.test.ts (existing pattern for E2E tests),
    backend/src/__tests__/agent/ledger.test.ts (Phase-1 pattern for writeAgentAction tests),
    backend/src/__tests__/agent/memory.test.ts (Phase-1 pattern for upsertAgentMemory tests),
    backend/src/__tests__/agent/tools/tenantIsolation.test.ts (two-tenant fixture pattern),
    backend/src/__tests__/mocks/agentMocks.ts (Jest mocks for prisma.agentAction, prisma.agentMemory),
    backend/src/__tests__/mocks/config.ts (prisma mock surface),
    backend/src/agent/registry.ts (ToolContext + InvokeResult shapes),
    backend/src/agent/runtime.ts (runAgent signature + AgentId type),
    backend/src/agent/scheduler.ts (existing setInterval cron pattern),
    .planning/phases/02-decisions-surface-propose-and-confirm-design-partner-1/02-RESEARCH.md (Pattern 1-6, gold-set fixture spec lines 1180-1206),
    .planning/phases/02-decisions-surface-propose-and-confirm-design-partner-1/02-VALIDATION.md (Test infrastructure §1, Wave 0 Requirements, gold-set list)
  </read_first>
  <action>
Create 14 RED test files + 10 fixture files + 1 fixtures index.

**Each test file MUST end with `expect(true).toBe(false); // RED — turned green by Wave N`** so Jest reports it as failing until the implementing wave lands. Use `describe.skip` is NOT allowed — these must run and fail until the feature exists.

**Fixture shape (TypeScript interface, exported from `fixtures/index.ts`):**
```typescript
export interface GoldFixture {
  name: string;
  tenantId: string;
  seed: {
    drivers?: Array<{ id: string; name: string; status: string; phone?: string }>;
    shifts?: Array<{ driverId: string; date: string; isLate: boolean; actualHoursMinutes?: number }>;
    cashRecords?: Array<{ driverId: string; salesAmount: number; collectionAmount: number; pendingDues: number; date: string }>;
    onlineSessions?: Array<{ driverId: string; isOnline: boolean; lastGpsAt: string | null }>;
    aiScores?: Array<{ driverId: string; date: string; compositeScore: number; trend: string }>;
    memoryRows?: Array<{ key: string; value: unknown; createdAt: string }>;
    tenantDisabled?: boolean;
  };
  triggerTier: "hot" | "warm" | "cold";
  expect: {
    minProposals: number;
    requiredToolNames: string[];      // these MUST appear in PendingAgentAction.toolName
    forbiddenToolNames: string[];     // applyPenalty/suspendDriver MUST be in this list for Phase 2
    proposalShouldMention: string[];  // key strings the reasoning must contain
  };
}
export const GOLD_FIXTURES: GoldFixture[] = [/* import all 10 */];
```

**Fixture list (each file exports a single `GoldFixture` named export):**
1. `01-late-clockins.ts` — driver "Mohamed" with 3 isLate=true shifts in 7 days. tier=warm. Expects ≥1 proposal, requiredToolNames=["draftCourierMessage"], forbiddenToolNames=["applyPenalty","suspendDriver"], proposalShouldMention=["3","late","this week"].
2. `02-gps-stale.ts` — driver "Ali" with onlineSession.isOnline=true and lastGpsAt 12 minutes ago. tier=hot. Expects ≥1 proposal, requiredToolNames=["draftCourierMessage"] OR ["flagForReview"], proposalShouldMention=["GPS","stale" OR "10 min"].
3. `03-rejection-cluster.ts` — driver with 3 orderLog rows status=REJECTED in 2 hours. tier=hot. Expects ≥1 proposal, requiredToolNames=["draftCourierMessage"] OR ["flagForReview"], proposalShouldMention=["rejection","3"].
4. `04-cash-mismatch.ts` — cashRecord with pendingDues=KD 28.500. tier=cold. Expects ≥1 proposal, requiredToolNames=["proposeCashReminder"] OR ["draftCourierMessage"], proposalShouldMention=["cash","KD"].
5. `05-perf-regression.ts` — aiScore weekly trend DOWN, compositeScore drop ≥8 points. tier=cold. Expects ≥1 proposal, requiredToolNames=["draftCourierMessage"] OR ["flagForReview"], proposalShouldMention=["score","regression" OR "down"].
6. `06-dismissed-suppression.ts` — same seed as fixture 01 PLUS memoryRow{key:"dismissed:draftCourierMessage:Driver:drv_xy12", createdAt: 2 days ago}. tier=warm. Expects minProposals=0 (suppression).
7. `07-empty-state.ts` — drivers=[], no anomalies. tier=hot. Expects minProposals=0.
8. `08-multi-anomaly-courier.ts` — single driver with both late clock-in AND GPS-stale. tier=warm. Expects minProposals=1 + maxProposalsPerCourier=1 (rate-limit invariant).
9. `09-disabled-tenant.ts` — tenantDisabled=true. tier=hot. Expects minProposals=0.
10. `10-deleted-courier.ts` — drivers=[] but shifts reference a non-existent driverId. tier=warm. Expects minProposals=0 + no thrown errors (graceful skip per Pitfall 3).

**Test file specs (each file MUST follow Phase 1 conventions: jest.mock("../../config", () => ({ prisma: ... })) + a `describe(req-id, ...)` per requirement):**

1. `decisions/cardProjector.test.ts` — `describe("REQ-decisions-proposal-inbox: projectPendingAction")` with 4 RED tests:
   - "maps PendingAgentAction.toolName=draftCourierMessage to tag=warn"
   - "loads driverName from prisma.driver.findFirst({where:{id:subjectId,tenantId:ctx.tenantId}})"
   - "sets toolIsLive=true ONLY for draftCourierMessage in Phase 2"
   - "computes state=pending|approved|dismissed from resolvedAt + resolution"

2. `decisions/approveFlow.test.ts` — `describe("REQ-agent-propose-confirm: POST /api/decisions/:id/approve")` with 6 RED tests:
   - "404 when PendingAgentAction does not exist"
   - "409 when PendingAgentAction.resolvedAt is already set"
   - "writes AgentAction with proposer='Darb', approverId=req.user.userId, originalProposal=pa.input"
   - "for live tool draftCourierMessage: re-invokes registry with ctx.userId set, asserts InvokeResult.status='executed'"
   - "for non-live tool flagForReview: writes audit-only row, no side effect, asserts no notification.create call"
   - "concurrent approve race: two simultaneous approve calls on the same PendingAgentAction → first returns 200 with resolution=approved; second returns 409 (count=0 from updateMany where {id, resolvedAt: null}). Verifies the optimistic-lock pattern in Plan 02 Task 2 (approve route uses prisma.pendingAgentAction.updateMany where {id, resolvedAt: null} → if updateMany.count !== 1, returns 409 to the second writer)"

3. `decisions/dismissFlow.test.ts` — `describe("REQ-agent-propose-confirm: POST /api/decisions/:id/dismiss")` with 3 RED tests:
   - "writes AgentMemory with key=`dismissed:${toolName}:${subjectType}:${subjectId}` and value{reason,dismissedBy,dismissedAt,originalProposal,pendingActionId}"
   - "updates PendingAgentAction.resolvedAt + resolution='rejected' + overrideReason=reason"
   - "400 when reason is missing or empty string"

4. `agent/monitor/monitoringSmoke.test.ts` — `describe("REQ-agent-continuous-monitoring: monitor end-to-end")` with 1 RED test:
   - "runAgent('monitor') with seed fixture 01 calls ≥1 read tool, calls draftCourierMessage as a write tool, results in PendingAgentAction.create with toolName='draftCourierMessage', tenantId, runId, agentId='monitor'"

5. `agent/monitor/neverExecutes.test.ts` — `describe("REQ-agent-propose-confirm: monitor never executes write tools")` with 2 RED tests:
   - "monitor's tool-loop with fixture 01: prisma.notification.create is NEVER called (only prisma.pendingAgentAction.create)"
   - "runtime assertion: ctx.userId is undefined for the entire monitor run; if a write tool's execute body sees ctx.userId set, fail"

6. `agent/monitor/dismissSuppression.test.ts` — `describe("REQ-agent-propose-confirm: 7-day dismiss suppression")` with 2 RED tests:
   - "fixture 06 (prior dismissal 2 days ago): minProposals=0 — monitor reads listMemoriesByPrefix('dismissed:') and skips identical proposal"
   - "fixture 06 with prior dismissal 8 days ago: ≥1 proposal — suppression window is 7 days, not 8+"

7. `agent/monitor/promptRegression.test.ts` — `describe("REQ-agent-action-drafting: gold-set prompt eval")` with 1 it.each test parameterized over GOLD_FIXTURES — for each fixture, run monitor, assert minProposals + requiredToolNames + forbiddenToolNames + proposalShouldMention. **Snapshot the rendered headline + reasoning for each fixture so prompt diffs are visible in PR.**

8. `agent/scheduler/monitorTier.test.ts` — `describe("REQ-agent-continuous-monitoring: tiered cadence")` with 3 RED tests:
   - "monitorTick('hot') is invoked at 60_000 ms intervals"
   - "monitorTick('warm') is invoked at 900_000 ms intervals"
   - "monitorTick('cold') is invoked at 3_600_000 ms intervals"
   - Use `jest.useFakeTimers()` + spy on `setInterval` calls in `startAgentScheduler()`.

9. `billing/billingService.test.ts` — `describe("REQ-pricing-model: computeMonthlyBill")` with 5 RED tests:
   - "150 active couriers, no override -> netKd=300.000"
   - "50 active couriers, no override -> netKd=200.000 (floor)"
   - "150 active couriers, monthlyOverrideKd=100 -> netKd=100.000 (override beats computed)"
   - "courier with one shift actualHoursMinutes=240 in month -> counts as 1 active"
   - "courier with all shifts actualHoursMinutes<240 -> NOT active"

10. `billing/overrideIsolation.test.ts` — `describe("REQ-pricing-model: override does not leak across tenants")` with 1 RED test:
    - "Tenant A monthlyOverrideKd=100, Tenant B no override: computeMonthlyBill(B,...) returns max(active*2,200), NOT 100"

11. `queues/onboardingBackwashWorker.test.ts` — `describe("REQ-gtm-onboarding: backwash worker chunking")` with 3 RED tests:
    - "30-day window split into 6 chunks of 5 days each per platform"
    - "per-platform progress is reported via job.updateProgress with {step,totalSteps,message}"
    - "concurrency capped at 2 platforms in flight (avoid scraper hammering)"

12. `onboarding/backwashProgress.test.ts` — `describe("REQ-gtm-onboarding: backwash status polling")` with 1 RED test:
    - "GET /api/admin/onboarding/tenants/:tid/backwash-status?jobId=X returns {state,progress:{step,totalSteps,message}}"

13. `onboarding/reportRender.test.ts` — `describe("REQ-gtm-onboarding: Darb's read report")` with 1 RED test:
    - "buildOnboardingReport returns ReportData with all 9 sections present: cover, topLineNumbers, top5Performers, bottom5Performers, cashExposure, violations, whatThisCosts, footer (8 fixed sections always present), and whatDarbWouldHaveDone (an array of 0..10 ReportCard objects). Each card in whatDarbWouldHaveDone conforms to the shape `{ action: string, reasoning: string, courierName: string, dateRange: { from: string, to: string } }`. The seed-design-partner-fixture script (Plan 04) ensures ≥10 PendingAgentAction rows exist for the design-partner-1 dry-run, so the production report returns exactly 10."

14. `middleware/superAdmin.test.ts` — `describe("admin route gate: requireSuperAdmin")` with 3 RED tests:
    - "User.isSuperAdmin=false -> 403"
    - "User.isSuperAdmin=true -> next() called"
    - "no req.user -> 401 (auth precondition)"

**RED-discipline note:** Each test must run a real expectation. Do NOT use `it.todo` or `describe.skip`. The tests should fail because the imported modules don't exist yet OR the assertion fails — Jest's exit code MUST be non-zero. Imports of files that don't yet exist (e.g. `../../services/decisions/cardProjector`) will cause module-not-found failures, which is acceptable RED state.
  </action>
  <verify>
    <automated>cd backend && npx jest --listTests --testPathPattern='(decisions|monitor|scheduler|billing|queues|onboarding|middleware/superAdmin)' 2>&1 | grep -c "\.test\.ts$" | xargs -I {} test {} -ge 14 && cd backend && ls src/__tests__/agent/monitor/fixtures/*.ts | grep -v index.ts | wc -l | xargs -I {} test {} -eq 10</automated>
  </verify>
  <done>
    14 RED test files + 10 fixture files + fixtures/index.ts exist. `cd backend && npx jest --testPathPattern='(decisions|monitor|scheduler|billing|queues|onboarding|middleware/superAdmin)'` produces a non-zero exit code (failing — modules not yet implemented). Phase 1 tests still pass: `cd backend && npm run test:agent` shows the existing 9 Phase-1 suites green AND the new monitor/etc suites red.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create frontend Vitest config + 2 RED component test files</name>
  <files>
    frontend/vitest.config.ts,
    frontend/src/__tests__/decisions/DecisionsList.test.tsx,
    frontend/src/__tests__/admin/DarbsReadReport.test.tsx,
    frontend/package.json
  </files>
  <read_first>
    frontend/package.json (check existing scripts + deps),
    frontend/next.config.mjs (TS path aliases),
    frontend/tsconfig.json (path aliases for @/),
    frontend/src/components/shared/StatCard.tsx (existing component pattern to reference),
    .planning/phases/02-decisions-surface-propose-and-confirm-design-partner-1/02-UI-SPEC.md (§3.4.3 nine sections of the report; §3.1.2 card anatomy)
  </read_first>
  <action>
**Step 1 — Add Vitest + jsdom + Testing Library to `frontend/package.json` if not present:**

Check `frontend/package.json` devDependencies. If `vitest`, `@vitejs/plugin-react`, `jsdom`, `@testing-library/react`, `@testing-library/jest-dom`, `@testing-library/user-event` are missing, add them via:
```
cd frontend && npm install --save-dev vitest @vitejs/plugin-react jsdom @testing-library/react @testing-library/jest-dom @testing-library/user-event
```

Add a `"test": "vitest run"` script (or `"vitest"` if present already use that). Keep existing `dev`, `build`, `start`.

**Step 2 — Create `frontend/vitest.config.ts`:**
```typescript
import { defineConfig } from "vitest/config";
import react from "@vitejs/plugin-react";
import path from "path";

export default defineConfig({
  plugins: [react()],
  resolve: {
    alias: {
      "@": path.resolve(__dirname, "./src"),
    },
  },
  test: {
    environment: "jsdom",
    globals: true,
    include: ["src/__tests__/**/*.test.{ts,tsx}"],
    setupFiles: ["./src/__tests__/setup.ts"],
  },
});
```

Create `frontend/src/__tests__/setup.ts`:
```typescript
import "@testing-library/jest-dom/vitest";
```

**Step 3 — Create `frontend/src/__tests__/decisions/DecisionsList.test.tsx` (RED):**

Tests:
- "REQ-decisions-proposal-inbox: renders 3 cards via mocked /api/decisions response"
- "REQ-decisions-proposal-inbox: Cmd+Enter on focused card invokes onApprove with card.id"
- "REQ-decisions-proposal-inbox: empty state appears when cards array is []"

Imports `DecisionsList` from `@/components/decisions/DecisionsList` (does not exist yet — produces module-not-found RED state). Use `render(<DecisionsList cards={mockCards} loading={false} onApprove={onApprove} onDismiss={vi.fn()} onEdit={vi.fn()} />)`. Mock cards reference the `DecisionCardData` shape from UI-SPEC §4.5.

**Step 4 — Create `frontend/src/__tests__/admin/DarbsReadReport.test.tsx` (RED):**

Tests:
- "REQ-gtm-onboarding: report renders all 9 sections (per UI-SPEC §3.4.3)"
- "feedback_invoices_white_background: report root container has bg-white class (no gradient bg, no watermark)"
- "feedback_invoices_no_pdf: 'Print to PDF' button is present but does NOT auto-fire a download on mount"

Imports `DarbsReadReport` from `@/components/admin/DarbsReadReport` (does not exist yet — RED). Mock data: a `ReportData` object with all 9 sections populated.
  </action>
  <verify>
    <automated>cd frontend && cat vitest.config.ts | grep -c "jsdom" && cd frontend && ls src/__tests__/decisions/DecisionsList.test.tsx src/__tests__/admin/DarbsReadReport.test.tsx src/__tests__/setup.ts | wc -l | xargs -I {} test {} -eq 3 && cd frontend && npm test 2>&1 | grep -E "FAIL|failed" | head -1</automated>
  </verify>
  <done>
    `frontend/vitest.config.ts` exists with `environment: "jsdom"`. `frontend/src/__tests__/setup.ts` imports `@testing-library/jest-dom/vitest`. Both component test files exist. `cd frontend && npm test` exits non-zero (RED — components not yet built). `package.json` test script runs vitest.
  </done>
</task>

<task type="auto">
  <name>Task 3: Extend lint:tenant scope to all Phase 2 backend surfaces</name>
  <files>
    backend/package.json
  </files>
  <read_first>
    backend/package.json (current lint:tenant script line 17),
    backend/eslint-rules/no-prisma-without-tenant.js (the rule definition),
    .planning/phases/02-decisions-surface-propose-and-confirm-design-partner-1/02-RESEARCH.md (Architectural Responsibility Map: which new files need lint coverage)
  </read_first>
  <action>
Edit the existing `lint:tenant` script in `backend/package.json` to extend the file glob list. Current script:

```
eslint src/agent/ src/__tests__/agent/ src/routes/aiChiefOfStaff.ts src/routes/ai.ts ...
```

Replace with (preserve all existing flags `--ext .ts --no-eslintrc --resolve-plugins-relative-to . --rulesdir eslint-rules --parser @typescript-eslint/parser --parser-options=ecmaVersion:2022,sourceType:module --rule "{\"no-prisma-without-tenant\":\"error\"}" --no-error-on-unmatched-pattern`):

```
eslint src/agent/ src/__tests__/agent/ src/routes/aiChiefOfStaff.ts src/routes/ai.ts src/agent/tools/action/ src/services/decisions/ src/services/billing/ src/services/onboarding/ src/routes/decisions.ts src/routes/admin/ src/middleware/superAdmin.ts src/queues/onboardingBackwashWorker.ts ...
```

Order matters less than completeness — every new Phase 2 backend surface that touches Prisma is covered.

After saving, run `cd backend && npm run lint:tenant`. Expected output: 0 errors (the new directories don't exist yet, and `--no-error-on-unmatched-pattern` ensures empty globs don't trip the script).

**Why:** Phase 2 surfaces (decisions routes, billing, onboarding, monitor) all touch Prisma. Phase 1's success criterion 4 ("automated check catches any new agent-touching route that omits tenant scoping") must hold for Phase 2's new surfaces from day one. Extending the scope NOW means Wave 1+ executors immediately fail the gate if they forget tenant filters.
  </action>
  <verify>
    <automated>cd backend && grep -E "src/agent/tools/action|src/services/decisions|src/services/billing|src/services/onboarding|src/routes/decisions\.ts|src/routes/admin|src/middleware/superAdmin|src/queues/onboardingBackwashWorker" package.json | wc -l | xargs -I {} test {} -ge 1 && cd backend && npm run lint:tenant 2>&1 | tee /tmp/lint-tenant-out.txt && grep -c "error" /tmp/lint-tenant-out.txt | xargs -I {} test {} -eq 0</automated>
  </verify>
  <done>
    `backend/package.json` lint:tenant glob includes all 8 new Phase 2 paths. `cd backend && npm run lint:tenant` exits 0 with no errors emitted. The rule is now armed for Wave 1+.
  </done>
</task>

</tasks>

<verification>
- All 14 backend RED test files exist under `__tests__/decisions/`, `__tests__/agent/monitor/`, `__tests__/agent/scheduler/`, `__tests__/billing/`, `__tests__/queues/`, `__tests__/onboarding/`, `__tests__/middleware/`.
- All 10 gold-set fixtures exist under `__tests__/agent/monitor/fixtures/` plus an `index.ts` aggregator.
- `cd backend && npx jest --testPathPattern='(decisions|monitor|scheduler|billing|queues|onboarding|middleware/superAdmin)'` exits non-zero (RED).
- `cd backend && npm run test:agent` continues to show all Phase-1 suites green.
- `frontend/vitest.config.ts` exists with `environment: "jsdom"`.
- `frontend/src/__tests__/setup.ts` imports `@testing-library/jest-dom/vitest`.
- 2 frontend component test files exist (DecisionsList, DarbsReadReport).
- `cd frontend && npm test` exits non-zero (RED — components not built yet).
- `backend/package.json` lint:tenant script covers all 8 new Phase 2 paths.
- `cd backend && npm run lint:tenant` exits 0 (no source files in new paths yet).
- Wave 1 executor can immediately turn red tests green by implementing the named modules.
</verification>

<success_criteria>
- [ ] 14 RED backend test files committed (each fails when run, with import-not-found OR assertion failure)
- [ ] 10 gold-set fixtures + index.ts committed under fixtures/
- [ ] frontend/vitest.config.ts + setup.ts committed
- [ ] 2 RED frontend component tests committed
- [ ] backend/package.json lint:tenant scope expanded
- [ ] cd backend && npx jest --testPathPattern='(decisions|monitor|scheduler|billing|queues|onboarding)' exits non-zero
- [ ] cd backend && npm run test:agent shows existing Phase-1 suites still green
- [ ] cd frontend && npm test exits non-zero
- [ ] cd backend && npm run lint:tenant exits 0
- [ ] No TypeScript errors introduced into existing Phase-1 code
</success_criteria>

<output>
After completion, create `.planning/phases/02-decisions-surface-propose-and-confirm-design-partner-1/02-00-SUMMARY.md` documenting:
- Test files created (count + paths)
- Fixture coverage (10 anomaly classes, suppression invariant, edge cases)
- lint:tenant scope expansion (before/after diff)
- Confirmation that all Phase-1 tests still pass
- Hand-off note for Wave 1: "Implement backend/src/agent/tools/action/draftCourierMessage.ts to turn neverExecutes.test.ts and monitoringSmoke.test.ts green."
</output>
