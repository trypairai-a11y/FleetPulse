---
phase: 06-ingest-adapter-layer
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - backend/package.json
  - backend/src/__tests__/services/ingest/types.test.ts
  - backend/src/__tests__/services/ingest/composite.test.ts
  - backend/src/__tests__/services/ingest/registry.test.ts
  - backend/src/__tests__/services/ingest/audit.test.ts
  - backend/src/__tests__/services/ingest/compositeFetchCash.test.ts
  - backend/src/__tests__/services/ingest/normalize.test.ts
  - backend/src/__tests__/services/ingest/orderSourceMobileGps.test.ts
  - backend/src/__tests__/services/ingest/keeta/keetaAdapter.test.ts
  - backend/src/__tests__/services/ingest/talabat/talabatAdapter.test.ts
  - backend/src/__tests__/services/ingest/deliveroo/deliverooAdapter.test.ts
  - backend/src/__tests__/services/ingest/americana/americanaAdapter.test.ts
  - backend/src/__tests__/routes/talabatImport.test.ts
  - backend/src/__tests__/routes/deliverooImport.test.ts
  - backend/src/__tests__/queues/pullChunkPhase6.test.ts
  - backend/src/__tests__/services/ingest/_lint_negative.fixture.ts.txt
  - backend/src/__tests__/services/ingest/_lintNegative.test.ts
  - backend/src/__tests__/services/ingest/fixtures/keetaSample.xlsx.ts
  - backend/src/__tests__/services/ingest/fixtures/talabatSample.xlsx.ts
  - backend/src/__tests__/services/ingest/fixtures/deliverooSample.xlsx.ts
  - backend/src/__tests__/services/ingest/fixtures/americanaSample.xlsx.ts
  - backend/src/__tests__/services/ingest/fixtures/index.ts
autonomous: true
requirements:
  - REQ-ingest-adapter-layer

must_haves:
  truths:
    - "Every Wave 1-4 task has a failing Jest test waiting that it will turn GREEN"
    - "13 RED test files exist under backend/src/__tests__/services/ingest/ + backend/src/__tests__/routes/ + backend/src/__tests__/queues/ (11 original + compositeFetchCash + pullChunkPhase6) — plus a lint negative fixture + its Jest assertion test"
    - "lint:tenant scope extended to include backend/src/services/ingest/"
    - "Existing onboardingBackwashWorker.test.ts (Phase 2) and walkingSkeleton.test.ts (Phase 1) still pass — Phase 6 must not regress them"
    - "OrderSource.MOBILE_GPS RED test asserts the new enum value exists once Wave 1's prisma migrate runs"
    - "Talabat + Deliveroo XLSX route tests assert the canonical {success, rowsIn, rowsOk, errors} shape from CON-xlsx-fallback"
  artifacts:
    - path: backend/src/__tests__/services/ingest/types.test.ts
      provides: "RED tests asserting IngestAdapter interface, NormalizedRow<T> shape, NotAvailable exception class"
      contains: "NotAvailable"
    - path: backend/src/__tests__/services/ingest/composite.test.ts
      provides: "RED tests for CompositeAdapter precedence + NotAvailable fallthrough + non-NotAvailable propagation"
      contains: "MOBILE_GPS"
    - path: backend/src/__tests__/services/ingest/registry.test.ts
      provides: "RED test: getAdapter(platform, ctx) returns CompositeAdapter for each of KEETA|TALABAT|DELIVEROO|AMERICANA"
      contains: "getAdapter"
    - path: backend/src/__tests__/services/ingest/audit.test.ts
      provides: "RED test: writeIngestRun({tenantId, platform, source, status, rowsIn, rowsOk, errorLog, startedAt, finishedAt}) creates IngestRun row tenant-scoped"
      contains: "writeIngestRun"
    - path: backend/src/__tests__/services/ingest/compositeFetchCash.test.ts
      provides: "RED test pinning the XLSX-import-only contract: for each of KEETA|TALABAT|DELIVEROO|AMERICANA, mock all tier adapters and assert composite.fetchCash(...) returns []. Then assert pullChunkPhase6 records cashRows: 0 in the IngestRun row with note 'XLSX-import-only'."
      contains: "fetchCash"
    - path: backend/src/__tests__/queues/pullChunkPhase6.test.ts
      provides: "RED test driving Wave 4 Task 1: (1) calls getAdapter(platform).fetchOrders/fetchShifts/fetchViolations for each capability, (2) NotAvailable from one capability does NOT abort the chunk, (3) writes 1 IngestRun row per chunk with source='BACKWASH' and tenantId, (4) returns {rowsOk} where rowsOk = upserted-row-count excluding cashRows."
      contains: "pullChunkPhase6"
    - path: backend/src/__tests__/services/ingest/_lint_negative.fixture.ts.txt
      provides: "Deliberately-broken adapter snippet (.txt extension so it does not compile) for the lint negative test: prisma.driver.findFirst({where: {platformDriverId: 'x'}}) — no tenantId filter."
      contains: "platformDriverId"
    - path: backend/src/__tests__/services/ingest/_lintNegative.test.ts
      provides: "Jest test asserting `npm run lint:tenant -- --rulesdir ./eslint-rules <fixture>` exits non-zero — proves the no-prisma-without-tenant rule fires on the new services/ingest/** scope."
      contains: "lint:tenant"
    - path: backend/src/__tests__/services/ingest/normalize.test.ts
      provides: "RED tests for shared normalizers (parseLocalDate, parseMoneyKwd, normaliseDriverName)"
      contains: "normalize"
    - path: backend/src/__tests__/services/ingest/orderSourceMobileGps.test.ts
      provides: "RED test asserting Prisma OrderSource enum includes MOBILE_GPS — turns GREEN after Wave 1 migration"
      contains: "MOBILE_GPS"
    - path: backend/src/__tests__/services/ingest/keeta/keetaAdapter.test.ts
      provides: "RED tests: KeetaXlsxAdapter.ingestXlsx upserts KeetaDailyMetrics (idempotent), KeetaScraperAdapter preserves existing FAILED-without-creds + PARTIAL-with-creds behavior, KeetaMobileAdapter.isAvailable false on empty LocationLog"
      contains: "KeetaXlsxAdapter"
    - path: backend/src/__tests__/services/ingest/talabat/talabatAdapter.test.ts
      provides: "RED tests: TalabatXlsxAdapter parses MVP shape {date, driver_id, orders_count, online_minutes, attendance_status} → upserts TalabatDailyMetrics; TalabatScraperAdapter returns NotAvailable unconditionally; rejects file with missing required headers (HTTP 400)"
      contains: "TalabatXlsxAdapter"
    - path: backend/src/__tests__/services/ingest/deliveroo/deliverooAdapter.test.ts
      provides: "RED tests: DeliverooXlsxAdapter same MVP shape; DeliverooScraperAdapter returns NotAvailable; idempotent re-import"
      contains: "DeliverooXlsxAdapter"
    - path: backend/src/__tests__/services/ingest/americana/americanaAdapter.test.ts
      provides: "RED tests: AmericanaXlsxAdapter wraps existing parseAmericanaDailyXlsx + processIngestionRows preserves side-effects (violations + attendance); AmericanaEmailAdapter is a thin re-export shim of services/americanaInboxWatcher.ts"
      contains: "AmericanaXlsxAdapter"
    - path: backend/src/__tests__/routes/talabatImport.test.ts
      provides: "RED test: POST /api/talabat/import with valid XLSX returns 200 {success, rowsIn, rowsOk, errors}; missing file returns 400; cross-tenant driver match rejected; same XLSX uploaded twice produces same row count (idempotent)"
      contains: "POST /api/talabat/import"
    - path: backend/src/__tests__/routes/deliverooImport.test.ts
      provides: "RED test: POST /api/deliveroo/import same shape + idempotent + cross-tenant rejection"
      contains: "POST /api/deliveroo/import"
    - path: backend/src/__tests__/services/ingest/fixtures/index.ts
      provides: "Aggregator exporting buildKeetaXlsxBuffer / buildTalabatXlsxBuffer / buildDeliverooXlsxBuffer / buildAmericanaXlsxBuffer helpers"
      contains: "buildKeetaXlsxBuffer"
    - path: backend/package.json
      provides: "lint:tenant script scope extended to cover services/ingest/"
      contains: "src/services/ingest"
  key_links:
    - from: backend/src/__tests__/services/ingest/composite.test.ts
      to: backend/src/__tests__/services/ingest/types.test.ts
      via: "import { NotAvailable, IngestAdapter } from '../../../services/ingest/types'"
      pattern: "from.*services/ingest/types"
    - from: backend/src/__tests__/services/ingest/registry.test.ts
      to: backend/src/__tests__/services/ingest/composite.test.ts
      via: "registry returns instances of CompositeAdapter"
      pattern: "CompositeAdapter"
    - from: backend/package.json
      to: backend/src/services/ingest
      via: "lint:tenant glob includes src/services/ingest/"
      pattern: "src/services/ingest"
    - from: backend/src/__tests__/services/ingest/fixtures/index.ts
      to: backend/src/__tests__/services/ingest/keeta/keetaAdapter.test.ts
      via: "test imports buildKeetaXlsxBuffer"
      pattern: "buildKeetaXlsxBuffer"

threat_model:
  trust_boundaries: []  # Wave 0 ships tests + lint scope — no production code, no new trust boundaries
  threats: []           # Test scaffolding poses no STRIDE threat surface
---

<objective>
Wave 0 — Build the safety net BEFORE any Phase 6 production code lands. This plan ships:

1. **11 backend RED test files** covering every Phase 6 deliverable: the IngestAdapter interface contract, CompositeAdapter precedence chain, per-platform registry lookup, IngestRun audit writer, shared normalizers, the OrderSource.MOBILE_GPS enum addition (Wave 1 migration), all 4 platform adapter test suites (Keeta XLSX/scraper/mobile, Talabat XLSX/scraper, Deliveroo XLSX/scraper, Americana XLSX/email), and the 2 NEW XLSX import routes (Talabat + Deliveroo).
2. **4 XLSX fixture builders** (one per platform) that programmatically generate valid + invalid XLSX buffers using the existing `xlsx@0.18.5` package (no binary file commit).
3. **lint:tenant scope extension** to gate `services/ingest/**` with the existing `no-prisma-without-tenant` ESLint rule from day one — no Pitfall 3 (cross-tenant data leak) regressions sneak in during Wave 2's per-platform adapter implementation.

Per the Nyquist rule, every Wave 1-4 task has a failing test waiting for it. Wave 0 produces the failing tests; Waves 1-4 turn them green.

Purpose: Phase 6 is a refactor + harness phase. The four existing ingest pathways (Keeta scraper scaffold, Keeta XLSX, Americana XLSX, Americana email) work today; the refactor must NOT break them. RED tests for the existing behaviors (Keeta XLSX response shape; Keeta scraper IngestRun audit pattern; Americana side-effects) lock the contract before the refactor begins. RED tests for the new code (Talabat/Deliveroo XLSX adapters + routes; CompositeAdapter precedence) drive Wave 2-3 implementation deterministically.

Output: 11 RED test files + 4 XLSX fixture builders + extended `lint:tenant` script. `cd backend && npx jest --testPathPattern='services/ingest|routes/(talabat|deliveroo)Import'` produces a non-zero exit (RED — modules don't yet exist). `cd backend && npm run lint:tenant` passes (the new scope contains no source files yet — the rule applies once Wave 1 adds files).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-ingest-adapter-layer/06-RESEARCH.md
@.planning/intel/decisions.md
@.planning/intel/constraints.md

# Existing patterns the executor will mirror
@backend/jest.config.js
@backend/package.json
@backend/src/__tests__/queues/onboardingBackwashWorker.test.ts
@backend/src/__tests__/agent/walkingSkeleton.test.ts
@backend/src/__tests__/mocks/agentMocks.ts
@backend/src/__tests__/mocks/config.ts

# Existing files Phase 6 wraps (do NOT modify in Wave 0)
@backend/src/queues/keetaPortalScraperWorker.ts
@backend/src/queues/onboardingBackwashWorker.ts
@backend/src/queues/americanaIngestWorker.ts
@backend/src/services/keetaXlsxParser.ts
@backend/src/services/americanaDailyParser.ts
@backend/src/services/americanaInboxWatcher.ts
@backend/src/routes/keeta.ts
@backend/src/routes/americanaIngest.ts
@backend/prisma/schema.prisma
@backend/eslint-rules/no-prisma-without-tenant.js

<interfaces>
<!-- Key contracts the RED tests are asserting against. Extracted from RESEARCH.md §"Architecture Patterns". -->
<!-- These types are NOT yet implemented — the test imports cause RED-state module-not-found errors until Wave 1 ships them. -->

From backend/src/services/ingest/types.ts (NEW — Wave 1):
```typescript
export type Platform = "KEETA" | "TALABAT" | "DELIVEROO" | "AMERICANA";

export type AdapterSource =
  | "MOBILE_GPS"      // own-app data (Phase 5+); preferred per CON-scraper-replaceable
  | "PORTAL_SCRAPER"  // Keeta portal scrape (Playwright scaffold; real impl Phase 11)
  | "XLSX_IMPORT"     // user uploads file (permanent fallback per CON-xlsx-fallback)
  | "OCR_MOBILE"      // courier mobile-app screenshot OCR
  | "OCR_WEB"         // operator web-side screenshot OCR
  | "EMAIL_INBOX";    // Americana daily email feed

export class NotAvailable extends Error {
  constructor(public readonly reason: string) {
    super(reason);
    this.name = "NotAvailable";
  }
}

export interface DateRange {
  from: Date;        // inclusive
  to: Date;          // exclusive
}

export interface NormalizedRow<T> {
  source: AdapterSource;
  tenantId: string;
  platform: Platform;
  data: T;
  raw?: unknown;
}

export interface IngestAdapter {
  readonly platform: Platform;
  readonly source: AdapterSource;
  isAvailable(tenantId: string): Promise<boolean>;
  fetchOrders?(tenantId: string, range: DateRange): Promise<NormalizedRow<unknown>[]>;
  fetchShifts?(tenantId: string, range: DateRange): Promise<NormalizedRow<unknown>[]>;
  fetchAttendance?(tenantId: string, range: DateRange): Promise<NormalizedRow<unknown>[]>;
  fetchCash?(tenantId: string, range: DateRange): Promise<NormalizedRow<unknown>[]>;
  fetchViolations?(tenantId: string, range: DateRange): Promise<NormalizedRow<unknown>[]>;
  ingestXlsx?(tenantId: string, buffer: Buffer): Promise<{
    rowsIn: number;
    rowsOk: number;
    errors: string[];
  }>;
}
```

From backend/src/services/ingest/registry.ts (NEW — Wave 1):
```typescript
export function getAdapter(platform: Platform, ctx: { tenantId: string }): CompositeAdapter;
```

From backend/src/services/ingest/audit.ts (NEW — Wave 1):
```typescript
export interface WriteIngestRunArgs {
  tenantId: string;
  platform: Platform;
  source: string;     // matches IngestRun.source String column (existing)
  status: "SUCCESS" | "PARTIAL" | "FAILED";
  startedAt: Date;
  finishedAt?: Date;
  rowsIn?: number;
  rowsOk?: number;
  errorLog?: string | null;
}
export async function writeIngestRun(args: WriteIngestRunArgs): Promise<{ id: string }>;
```

From backend/prisma/schema.prisma (existing — DO NOT modify in Wave 0):
- IngestRun model at line 1307 — fields: tenantId, platform, source (String), status (String), startedAt, finishedAt, rowsIn, rowsOk, errorLog
- OrderSource enum at line 76 — values: MANUAL | SCREENSHOT_OCR | EXCEL_IMPORT | AGENT_CAPTURE | WHATSAPP. Wave 1 adds MOBILE_GPS.
- KeetaDailyMetrics has @@unique([tenantId, driverId, date]) — XLSX adapter upserts against this
- TalabatDailyMetrics + DeliverooDailyMetrics have similar compound unique constraints (verify exact field names during Wave 0 RED test write — read schema.prisma)
- LocationLog at line 964 keyed by driverId+deviceId — NOT directly tenant-scoped; mobile adapters MUST join through Driver.tenantId (Pitfall 5)

From backend/package.json (existing line 17 — extend in Task 3):
```
"lint:tenant": "eslint src/agent/ src/__tests__/agent/ src/routes/aiChiefOfStaff.ts ... src/queues/onboardingBackwashWorker.ts --ext .ts ..."
```
Wave 0 appends: `src/services/ingest/`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create 11 backend RED test files + 4 XLSX fixture builders</name>
  <files>
    backend/src/__tests__/services/ingest/types.test.ts,
    backend/src/__tests__/services/ingest/composite.test.ts,
    backend/src/__tests__/services/ingest/registry.test.ts,
    backend/src/__tests__/services/ingest/audit.test.ts,
    backend/src/__tests__/services/ingest/normalize.test.ts,
    backend/src/__tests__/services/ingest/orderSourceMobileGps.test.ts,
    backend/src/__tests__/services/ingest/keeta/keetaAdapter.test.ts,
    backend/src/__tests__/services/ingest/talabat/talabatAdapter.test.ts,
    backend/src/__tests__/services/ingest/deliveroo/deliverooAdapter.test.ts,
    backend/src/__tests__/services/ingest/americana/americanaAdapter.test.ts,
    backend/src/__tests__/routes/talabatImport.test.ts,
    backend/src/__tests__/routes/deliverooImport.test.ts,
    backend/src/__tests__/queues/pullChunkPhase6.test.ts,
    backend/src/__tests__/services/ingest/compositeFetchCash.test.ts,
    backend/src/__tests__/services/ingest/_lint_negative.fixture.ts.txt,
    backend/src/__tests__/services/ingest/_lintNegative.test.ts,
    backend/src/__tests__/services/ingest/fixtures/index.ts,
    backend/src/__tests__/services/ingest/fixtures/keetaSample.xlsx.ts,
    backend/src/__tests__/services/ingest/fixtures/talabatSample.xlsx.ts,
    backend/src/__tests__/services/ingest/fixtures/deliverooSample.xlsx.ts,
    backend/src/__tests__/services/ingest/fixtures/americanaSample.xlsx.ts
  </files>
  <read_first>
    backend/src/__tests__/queues/onboardingBackwashWorker.test.ts (existing Phase-2 RED-pattern reference: short, behavioral, asserts on injected fakes — 84 lines total),
    backend/src/__tests__/agent/walkingSkeleton.test.ts (existing E2E test pattern with prisma mock),
    backend/src/__tests__/mocks/agentMocks.ts (jest.mock("../../config", ...) pattern + prisma mock surface),
    backend/src/__tests__/mocks/config.ts (prisma mock fields),
    backend/src/services/keetaXlsxParser.ts (existing parser — adapter wraps; tests must NOT re-test parser internals, only the adapter's wrap-and-upsert behavior),
    backend/src/queues/keetaPortalScraperWorker.ts (existing scraper scaffold — Keeta scraper adapter test asserts identical IngestRun audit-row pattern: status="PARTIAL" + errorLog="Scaffold only — Playwright selectors pending" when creds present, status="FAILED" + errorLog="No Keeta portal credentials configured for tenant" when absent),
    backend/src/queues/americanaIngestWorker.ts (existing Americana flow with processIngestionRows + scanIngestionForViolations — americana XLSX adapter test asserts side-effects preserved),
    backend/src/services/americanaInboxWatcher.ts (existing IMAP watcher — americana email adapter test asserts thin-wrap re-export shim, NOT a relocation; per orchestrator resolution #5),
    backend/src/routes/keeta.ts (lines 376-470: existing /import route response shape `{success: true, imported, errors}` — tests assert the new factory route returns the same keys remapped to `{success, rowsIn, rowsOk, errors}` per RESEARCH.md Pattern 5),
    backend/prisma/schema.prisma (lines 76-82: OrderSource enum; lines 1307-1321: IngestRun model; KeetaDailyMetrics / TalabatDailyMetrics / DeliverooDailyMetrics @@unique constraints — read EXACT field names),
    .planning/phases/06-ingest-adapter-layer/06-RESEARCH.md (Pattern 1-5, Pitfalls 1-10, Validation Architecture §"Wave 0 Gaps")
  </read_first>
  <action>
Create 11 RED Jest test files + 4 XLSX fixture builders + 1 fixtures index.

**RED-discipline rule:** Each test runs a real expectation. Imports of files that don't exist (e.g. `../../../services/ingest/types`) cause Jest module-not-found failures — that IS the RED state. Do NOT use `it.todo` or `describe.skip`. Per Wave 0 convention from Phase 2, end each describe block with a comment `// RED — turned GREEN by Wave N` so reviewers see intent.

**Fixture builders (programmatic XLSX generation — NO binary commits):**

Each fixture file exports a `buildXxxXlsxBuffer(opts?)` helper using the existing `xlsx@0.18.5` package. Pattern:

```typescript
// backend/src/__tests__/services/ingest/fixtures/keetaSample.xlsx.ts
import * as XLSX from "xlsx";

export function buildKeetaXlsxBuffer(opts?: {
  rows?: Array<{
    date: string;            // "2026-05-01"
    courierPlatformId: string;
    firstName: string;
    lastName: string;
    onShift?: number;
    deliveredTasks?: number;
    /* ... 27 columns total per parseKeetaXlsx */
  }>;
  badHeaders?: boolean;       // emit malformed header row to trigger HTTP 400
}): Buffer {
  const rows = opts?.rows ?? [
    { date: "2026-05-01", courierPlatformId: "K-001", firstName: "Mohamed", lastName: "Ali",
      onShift: 1, deliveredTasks: 24, /* fill remaining columns with sensible defaults */ },
    { date: "2026-05-02", courierPlatformId: "K-002", firstName: "Hassan", lastName: "Said",
      onShift: 1, deliveredTasks: 18, /* ... */ },
  ];
  const headers = opts?.badHeaders
    ? ["wrong", "header", "shape"]
    : ["Date","Courier ID","First Name","Last Name","On Shift","Delivered", /* 27 total */];
  const aoa = [
    ["Some title row"],          // parseKeetaXlsx skips first 2 rows; row 3+ is data per parser at line 88-144
    headers,
    ...rows.map(r => [r.date, r.courierPlatformId, r.firstName, r.lastName, r.onShift, r.deliveredTasks /* ... */]),
  ];
  const ws = XLSX.utils.aoa_to_sheet(aoa);
  const wb = XLSX.utils.book_new();
  XLSX.utils.book_append_sheet(wb, ws, "Sheet1");
  return XLSX.write(wb, { type: "buffer", bookType: "xlsx" });
}
```

Replicate the same shape for Talabat / Deliveroo / Americana. The MVP Talabat + Deliveroo schema (per orchestrator resolution #2) is `{date, driver_id, orders_count, online_minutes, attendance_status}` — 5 columns. Americana fixture mirrors `parseAmericanaDailyXlsx`'s expected daily shape.

`fixtures/index.ts` re-exports all 4 builders + a `buildBadKeetaXlsx()` / `buildBadTalabatXlsx()` / etc. that returns malformed buffers for HTTP-400 schema-rejection tests.

**Test file specs (each file ends with the existing pattern from `__tests__/queues/onboardingBackwashWorker.test.ts`):**

1. **`services/ingest/types.test.ts`** — `describe("Phase 6 / REQ-ingest-adapter-layer: IngestAdapter contract")` with 4 RED tests:
   - "NotAvailable extends Error and carries .reason"
   - "NormalizedRow<T> has source/tenantId/platform/data/raw fields"
   - "IngestAdapter type compiles with optional fetch* methods + required platform/source/isAvailable"
   - "AdapterSource union includes MOBILE_GPS, PORTAL_SCRAPER, XLSX_IMPORT, OCR_MOBILE, OCR_WEB, EMAIL_INBOX"
   - Use TypeScript-only assertions (e.g., `expect((new NotAvailable("x")).reason).toBe("x")`); no DB.

2. **`services/ingest/composite.test.ts`** — `describe("Phase 6 / REQ-ingest-adapter-layer: CompositeAdapter precedence")` with 6 RED tests (per Pitfall 4 — composite must NOT swallow real errors):
   - "fetchOrders dispatches to first tier; tier-1 returns 3 rows → composite returns 3 rows; tier-2 NOT called"
   - "fetchOrders tier-1 throws NotAvailable → composite calls tier-2; tier-2 returns 2 rows → composite returns 2 rows"
   - "fetchOrders all tiers throw NotAvailable → composite returns []"
   - "fetchOrders tier-1 throws non-NotAvailable Error('boom') → composite re-throws Error('boom') (does NOT fall through)"
   - "isAvailable returns true if ANY tier.isAvailable returns true"
   - "isAvailable returns false if ALL tiers return false"
   - Use mock IngestAdapter instances built inline; no DB.

3. **`services/ingest/registry.test.ts`** — `describe("Phase 6 / REQ-ingest-adapter-layer: getAdapter factory")` with 4 RED tests:
   - "getAdapter('KEETA', {tenantId: 't-1'}) returns CompositeAdapter with platform='KEETA'"
   - "getAdapter('TALABAT', {tenantId: 't-1'}) returns CompositeAdapter with platform='TALABAT'"
   - "getAdapter('DELIVEROO', {tenantId: 't-1'}) returns CompositeAdapter with platform='DELIVEROO'"
   - "getAdapter('AMERICANA', {tenantId: 't-1'}) returns CompositeAdapter with platform='AMERICANA'"
   - Each platform's CompositeAdapter has tiers in the precedence order from RESEARCH.md Pattern 3 (MOBILE → SCRAPER → OCR → XLSX → EMAIL where applicable). Inspect `composite.tiers.map(t => t.source)`.

4. **`services/ingest/audit.test.ts`** — `describe("Phase 6 / REQ-ingest-adapter-layer: writeIngestRun audit helper")` with 4 RED tests:
   - "writeIngestRun({tenantId, platform: 'KEETA', source: 'XLSX_IMPORT', status: 'SUCCESS', rowsIn: 10, rowsOk: 10, startedAt, finishedAt}) calls prisma.ingestRun.create with tenantId-scoped data"
   - "writeIngestRun status='FAILED' with errorLog string ≤4000 chars persists errorLog"
   - "writeIngestRun trims errorLog over 4000 chars to 4000-char prefix"
   - "writeIngestRun returns {id: string}"
   - Use `jest.mock("../../config", () => ({ prisma: { ingestRun: { create: jest.fn() } } }))` pattern.

5. **`services/ingest/normalize.test.ts`** — `describe("Phase 6 / REQ-ingest-adapter-layer: shared normalizers")` with 6 RED tests:
   - "parseLocalDate('2026-05-01') returns Date with UTC midnight"
   - "parseLocalDate(45000) returns new Date(Date.UTC(2023, 2, 15)) — XLSX serial 45000 = 2023-03-15 (pinned per WARNING 6)"
   - "parseLocalDate(null) throws explanatory Error"
   - "parseMoneyKwd('KD 12.500') returns 12.5"
   - "parseMoneyKwd('12.500') returns 12.5"
   - "normaliseDriverName('  Mohamed  Ali  ') returns 'mohamed ali' (lowercased + collapsed whitespace) for case-insensitive driver matching"

6. **`services/ingest/orderSourceMobileGps.test.ts`** — `describe("Phase 6 / REQ-ingest-adapter-layer: OrderSource.MOBILE_GPS")` with 1 RED test:
   - "Prisma OrderSource enum includes MOBILE_GPS"
   - Test imports `OrderSource` from `@prisma/client` (or generated path) and asserts `OrderSource.MOBILE_GPS === 'MOBILE_GPS'`. Will FAIL until Wave 1's `prisma migrate dev --name add_mobile_gps_order_source` runs and `prisma generate` regenerates the type. This is the dedicated migration verification gate.

7. **`services/ingest/keeta/keetaAdapter.test.ts`** — `describe("Phase 6 / REQ-ingest-adapter-layer: Keeta adapters")` with 7 RED tests:
   - **KeetaXlsxAdapter:**
     - "ingestXlsx with valid buffer (2 rows from buildKeetaXlsxBuffer) calls prisma.driver.findFirst tenant-scoped + prisma.keetaDailyMetrics.upsert × 2 → returns {rowsIn: 2, rowsOk: 2, errors: []}"
     - "ingestXlsx with row whose courierPlatformId not in tenant's drivers → errors[] entry, rowsOk decremented (existing keeta.ts:402-405 behavior preserved)"
     - "ingestXlsx duplicate import (same buffer twice) → KeetaDailyMetrics row count is N, NOT 2N (idempotent upsert per Pitfall 9)"
     - "isAvailable returns true (XLSX is always-available; user-driven)"
     - "fetchOrders throws NotAvailable (XLSX is upload-driven, not pull)"
   - **KeetaScraperAdapter:**
     - "isAvailable: tenant has portalCredentials with hasEncryptedShape=true → returns true; missing creds → returns false"
     - "fetchOrders for tenant without creds throws NotAvailable('No Keeta portal credentials configured for tenant')"
   - **KeetaMobileAdapter:**
     - "isAvailable: zero LocationLog rows in last 24h for any tenant driver → returns false (composite falls through to scraper/XLSX)"

8. **`services/ingest/talabat/talabatAdapter.test.ts`** — `describe("Phase 6 / REQ-ingest-adapter-layer: Talabat adapters")` with 6 RED tests:
   - **TalabatXlsxAdapter:**
     - "ingestXlsx with MVP shape rows {date, driver_id, orders_count, online_minutes, attendance_status} → upserts TalabatDailyMetrics for each row, tenant-scoped"
     - "ingestXlsx with malformed headers (buildBadTalabatXlsx) → throws Error with message including 'missing required columns: date, driver_id, orders_count, online_minutes, attendance_status' (Pitfall 10)"
     - "ingestXlsx idempotent: same buffer twice → row count N, not 2N"
     - "ingestXlsx with driver_id from another tenant → errors[] entry, NOT cross-tenant write (Pitfall 3)"
     - **Compile-time @@unique key shape pin (WARNING 12):** add a TypeScript-only assertion at the top of the test file using `expectTypeOf` from `expect-type` (already a transitive dep via Jest's type ecosystem; if not installed, use a hand-rolled type-equality check):
       ```typescript
       import type { Prisma } from "@prisma/client";
       // Pinned per backend/prisma/schema.prisma:1378
       //   @@unique([tenantId, driverId, shiftDate])
       // Compile fails if the schema changes shape, forcing this test (and the Wave 2a adapter)
       // to update in lockstep — no silent drift.
       type _PinTalabatUnique = Prisma.TalabatDailyMetricsWhereUniqueInput["tenantId_driverId_shiftDate"];
       const _pin: _PinTalabatUnique = { tenantId: "x", driverId: "x", shiftDate: new Date() };
       void _pin;
       ```
   - **TalabatScraperAdapter:**
     - "isAvailable returns false unconditionally; fetchOrders throws NotAvailable (Phase 11 work — Pitfall 7)"

9. **`services/ingest/deliveroo/deliverooAdapter.test.ts`** — `describe("Phase 6 / REQ-ingest-adapter-layer: Deliveroo adapters")` with 4 RED tests:
   - "DeliverooXlsxAdapter.ingestXlsx with same MVP shape upserts DeliverooDailyMetrics tenant-scoped"
   - "DeliverooXlsxAdapter idempotent on duplicate import"
   - "DeliverooXlsxAdapter rejects malformed headers with explanatory Error"
   - "DeliverooScraperAdapter.isAvailable=false + fetchOrders throws NotAvailable"
   - **Compile-time @@unique key shape pin (WARNING 12):** mirror the Talabat pin pattern. `Prisma.DeliverooDailyMetricsWhereUniqueInput["tenantId_driverId_shiftDate"]` per schema.prisma:1348.

10. **`services/ingest/americana/americanaAdapter.test.ts`** — `describe("Phase 6 / REQ-ingest-adapter-layer: Americana adapters")` with 5 RED tests:
    - **AmericanaXlsxAdapter:**
      - "ingestXlsx wraps existing parseAmericanaDailyXlsx → produces NormalizedRow<T>[] → calls existing processIngestionRows from americanaIngestWorker (preserves attendance + violation side-effects per RESEARCH.md §'Critical refactor verification steps' #2)"
      - "ingestXlsx response shape: {rowsIn, rowsOk, errors}"
    - **AmericanaEmailAdapter:**
      - "module exports a function that re-exports from services/americanaInboxWatcher.ts (thin-wrap shim per orchestrator resolution #5)"
      - "calling adapter.run() invokes existing pollTenantInbox unchanged"
      - "isAvailable returns true if tenant has americana.ingest IMAP config; false otherwise"

11. **`routes/talabatImport.test.ts`** — `describe("Phase 6 / REQ-ingest-adapter-layer: POST /api/talabat/import")` with 5 RED tests:
    - "valid XLSX upload → 200 {success: true, rowsIn: N, rowsOk: M, errors: []}"
    - "no file in multipart body → 400 {error: 'No file uploaded'}"
    - "writes IngestRun row {tenantId, platform: 'TALABAT', source: 'XLSX_IMPORT', status: 'SUCCESS'}"
    - "duplicate upload (same buffer twice) → second response has same rowsOk, no row-count doubling"
    - "uploaded XLSX with driver_id from a different tenant → errors[] entry; cross-tenant rejection (Pitfall 3)"
    - Use `supertest` against an Express app instance with the route mounted — same harness as Phase 2's existing route tests under `__tests__/routes/`.

12. **`routes/deliverooImport.test.ts`** — same 5 tests structure for Deliveroo; ensures both new routes have parity coverage.

13. **`services/ingest/compositeFetchCash.test.ts`** — `describe("Phase 6 / REQ-ingest-adapter-layer: Cash is XLSX-import-only")` with 6 RED tests (BLOCKER 2 contract):
    - For each platform `KEETA|TALABAT|DELIVEROO|AMERICANA`: build a CompositeAdapter with mocked tier instances; assert `composite.fetchCash(tenantId, range)` returns `[]` (4 tests)
    - "pullChunkPhase6 records cashRows: 0 in the IngestRun row regardless of platform" — mock the worker's `writeIngestRun` and assert the recorded row has `rowsOk` reflecting orders+shifts+attendance+violations only (no cash contribution); errorLog is null when no errors
    - "pullChunkPhase6 NEVER calls adapter.fetchCash" — uses a spy on the registry-returned CompositeAdapter; expects `fetchCash` spy.callCount === 0
    - Implementation note: import the type contract directly: `import { compositeShouldNeverProduceCashInBackwash } from "../../../config"` is NOT needed — assertions go directly against the composite + worker behavior.

14. **`queues/pullChunkPhase6.test.ts`** — `describe("Phase 6 / REQ-ingest-adapter-layer: pullChunkPhase6 chunk handler")` with 6 RED tests (BLOCKER 4 contract):
    - "Calls getAdapter(args.platform, {tenantId: args.tenantId}).fetchOrders + .fetchShifts + .fetchAttendance + .fetchViolations" — verify each capability invoked with `(tenantId, {from, to})`
    - "DOES NOT call adapter.fetchCash" — `expect(fetchCashSpy).not.toHaveBeenCalled()` (XLSX-import-only contract)
    - "NotAvailable thrown by ONE capability does NOT abort the chunk" — mock fetchShifts to throw `new NotAvailable("test")`; assert other capabilities still execute and IngestRun status="SUCCESS" (NotAvailable is expected fallthrough)
    - "Non-NotAvailable error from one capability captured in errors[] and recorded in IngestRun.errorLog; chunk does NOT throw to caller" — mock fetchOrders to throw `new Error("boom")`; assert pullChunkPhase6 returns `{rowsOk: 0}`, IngestRun status is FAILED or PARTIAL with errorLog containing "boom"
    - "Writes exactly 1 IngestRun row per chunk with {tenantId, platform, source: 'BACKWASH', startedAt, finishedAt}" — assert prisma.ingestRun.create called once with these fields
    - "Returns {rowsOk} where rowsOk = orders+shifts+attendance+violations row count, EXCLUDING cash" — even if some adapter were to misbehave and return cash rows from fetchOrders, the rowsOk math doesn't double-count fetchCash output (it isn't called)

15. **`services/ingest/_lint_negative.fixture.ts.txt`** — fixture file (`.txt` extension so the TS compiler ignores it). Contents:
    ```typescript
    // Deliberately broken — used by _lintNegative.test.ts to verify
    // no-prisma-without-tenant fires on the new services/ingest/** scope.
    import { prisma } from "../../config";
    export async function broken() {
      return prisma.driver.findFirst({ where: { platformDriverId: "x" } });
    }
    ```
    The file must NOT compile under the project tsconfig (the `.txt` suffix prevents this) but MUST be readable by ESLint via the rulesdir runner.

16. **`services/ingest/_lintNegative.test.ts`** — `describe("Phase 6 / REQ-ingest-adapter-layer: lint:tenant rule fires on services/ingest/** scope")` with 1 RED test:
    - "Running ESLint with no-prisma-without-tenant against the broken fixture exits non-zero" — uses `child_process.spawnSync("npx", ["eslint", "--rulesdir", "./eslint-rules", "--no-eslintrc", "--parser", "@typescript-eslint/parser", "--parser-options", "ecmaVersion:2022,sourceType:module", "--rule", "{\"no-prisma-without-tenant\":\"error\"}", "src/__tests__/services/ingest/_lint_negative.fixture.ts.txt"], {cwd: backendDir})`. Asserts `result.status !== 0` AND stdout/stderr contains the rule's diagnostic. Demonstrates the rule actually fires on the new scope.

**Existing tests guard:** RED tests must NOT modify existing test files. The Phase 2 `onboardingBackwashWorker.test.ts` continues to assert chunk count + concurrency + progress events on the EXISTING `defaultPullChunkPhase2` OR a new `pullChunkPhase6` wired by Wave 4. Wave 4 swaps the worker's `pullChunk` argument; Phase 2's tests inject their own fake `pullChunk` and remain green.

**Side note on `services/ingest/orderSourceMobileGps.test.ts`:** This test will FAIL with a TypeScript compile error UNTIL Wave 1 runs `prisma migrate dev --name add_mobile_gps_order_source` AND `prisma generate`. That is the Nyquist gate proving the migration ran — keep this test in Wave 0 even though it cannot pass at Wave 0's commit boundary.
  </action>
  <verify>
    <automated>cd /Users/mac/Documents/Darb/backend && [[ $(ls src/__tests__/services/ingest/types.test.ts src/__tests__/services/ingest/composite.test.ts src/__tests__/services/ingest/compositeFetchCash.test.ts src/__tests__/services/ingest/registry.test.ts src/__tests__/services/ingest/audit.test.ts src/__tests__/services/ingest/normalize.test.ts src/__tests__/services/ingest/orderSourceMobileGps.test.ts src/__tests__/services/ingest/keeta/keetaAdapter.test.ts src/__tests__/services/ingest/talabat/talabatAdapter.test.ts src/__tests__/services/ingest/deliveroo/deliverooAdapter.test.ts src/__tests__/services/ingest/americana/americanaAdapter.test.ts src/__tests__/services/ingest/_lintNegative.test.ts src/__tests__/services/ingest/_lint_negative.fixture.ts.txt src/__tests__/routes/talabatImport.test.ts src/__tests__/routes/deliverooImport.test.ts src/__tests__/queues/pullChunkPhase6.test.ts src/__tests__/services/ingest/fixtures/index.ts src/__tests__/services/ingest/fixtures/keetaSample.xlsx.ts src/__tests__/services/ingest/fixtures/talabatSample.xlsx.ts src/__tests__/services/ingest/fixtures/deliverooSample.xlsx.ts src/__tests__/services/ingest/fixtures/americanaSample.xlsx.ts 2>/dev/null | wc -l | tr -d ' ') -eq 21 ]] && cd /Users/mac/Documents/Darb/backend && [[ $(npx jest --testPathPattern='services/ingest|routes/(talabat|deliveroo)Import|queues/pullChunkPhase6' --listTests 2>/dev/null | grep -c "\.test\.ts$") -ge 14 ]] && cd /Users/mac/Documents/Darb/backend && npx jest --testPathPattern='services/ingest' 2>&1 | grep -E "FAIL|Cannot find module" | head -1</automated>
  </verify>
  <done>
    13 RED test files + 4 XLSX fixture builders + fixtures/index.ts + 1 lint negative fixture + 1 lint negative jest test exist (21 files total: 13 .test.ts + 4 fixture builders + 1 fixtures/index.ts + 1 _lint_negative.fixture.ts.txt + 2 .test.ts already counted in 13).
    `cd backend && npx jest --testPathPattern='services/ingest|routes/(talabat|deliveroo)Import|queues/pullChunkPhase6'` exits non-zero (RED — modules don't yet exist).
    `cd backend && npm test -- --testPathPattern='queues/onboardingBackwashWorker|agent/walkingSkeleton'` still GREEN (Phase-1+Phase-2 untouched).
  </done>
</task>

<task type="auto">
  <name>Task 2: Extend lint:tenant scope to backend/src/services/ingest/</name>
  <files>
    backend/package.json
  </files>
  <read_first>
    backend/package.json (current line 17: "lint:tenant" script — read EXACT current command to preserve all existing globs),
    backend/eslint-rules/no-prisma-without-tenant.js (the rule definition — confirm it accepts directory globs),
    .planning/phases/06-ingest-adapter-layer/06-RESEARCH.md (§"Pitfall 3: Tenant scoping bypass" — mandates services/ingest/** in lint:tenant scope; §"Pitfall 5: Mobile adapter consuming non-tenant LocationLog" — mobile adapter MUST go via prisma.locationLog.findMany({where: {driver: {tenantId}}}))
  </read_first>
  <action>
**Edit `backend/package.json` line 17 ("lint:tenant" script).**

Current value (verify by reading; preserve EXACT prefix):
```
"lint:tenant": "eslint src/agent/ src/__tests__/agent/ src/routes/aiChiefOfStaff.ts src/routes/ai.ts src/agent/tools/action/ src/services/decisions/ src/services/billing/ src/services/onboarding/ src/routes/decisions.ts src/routes/admin/ src/middleware/superAdmin.ts src/queues/onboardingBackwashWorker.ts --ext .ts --no-eslintrc --resolve-plugins-relative-to . --rulesdir eslint-rules --parser @typescript-eslint/parser --parser-options=ecmaVersion:2022,sourceType:module --rule \"{\\\"no-prisma-without-tenant\\\":\\\"error\\\"}\" --no-error-on-unmatched-pattern"
```

Insert `src/services/ingest/` immediately after `src/queues/onboardingBackwashWorker.ts` (before `--ext .ts`). Result:
```
"lint:tenant": "eslint src/agent/ src/__tests__/agent/ src/routes/aiChiefOfStaff.ts src/routes/ai.ts src/agent/tools/action/ src/services/decisions/ src/services/billing/ src/services/onboarding/ src/routes/decisions.ts src/routes/admin/ src/middleware/superAdmin.ts src/queues/onboardingBackwashWorker.ts src/services/ingest/ --ext .ts --no-eslintrc --resolve-plugins-relative-to . --rulesdir eslint-rules --parser @typescript-eslint/parser --parser-options=ecmaVersion:2022,sourceType:module --rule \"{\\\"no-prisma-without-tenant\\\":\\\"error\\\"}\" --no-error-on-unmatched-pattern"
```

**Why this works in Wave 0 even though `services/ingest/` doesn't exist yet:** the `--no-error-on-unmatched-pattern` flag (already in the script) keeps lint:tenant exit=0 when the directory is empty. From Wave 1 onward, every adapter file is gated.

**Do NOT change other scripts.** Touch only the lint:tenant line. Do NOT bump `version`. Run `cd backend && npm run lint:tenant` to confirm exit=0 (no source files yet under the new scope).
  </action>
  <verify>
    <automated>cd /Users/mac/Documents/Darb/backend && grep -c "src/services/ingest/" package.json && cd /Users/mac/Documents/Darb/backend && npm run lint:tenant 2>&1 | tail -3 ; echo "exit=$?"</automated>
  </verify>
  <done>
    `backend/package.json::scripts.lint:tenant` includes `src/services/ingest/` glob.
    `cd backend && npm run lint:tenant` exits 0 (new scope present, but empty until Wave 1).
    Existing lint:tenant scope (Phase 1 + Phase 2 surfaces) preserved verbatim.
  </done>
</task>

</tasks>

<verification>
1. **RED tests fail with module-not-found for the right modules:** `cd backend && npx jest --testPathPattern='services/ingest|routes/(talabat|deliveroo)Import' 2>&1 | grep -c "Cannot find module.*services/ingest"` returns ≥11.
2. **Existing tests untouched:** `cd backend && npm test -- --testPathPattern='queues/onboardingBackwashWorker'` GREEN. `cd backend && npm test -- --testPathPattern='agent/walkingSkeleton'` GREEN.
3. **lint:tenant scope extended:** `grep "src/services/ingest/" backend/package.json` matches.
4. **lint:tenant exits 0 with empty new scope:** `cd backend && npm run lint:tenant; echo $?` prints `0`.
5. **Fixture builders compile:** `cd backend && npx tsc --noEmit -p tsconfig.json 2>&1 | grep -E "ingest/fixtures" | head -3` shows no type errors (the fixtures are pure helper functions; their consumers are RED tests but the builders themselves compile).
6. **No source files under services/ingest/ yet:** `find backend/src/services/ingest -type f 2>/dev/null | wc -l` returns 0 (Wave 1 ships the production code).
</verification>

<success_criteria>
- 11 RED test files + 4 XLSX fixture builders + 1 fixtures index file (17 total) exist under `backend/src/__tests__/services/ingest/` and `backend/src/__tests__/routes/`
- `cd backend && npx jest --testPathPattern='services/ingest|routes/(talabat|deliveroo)Import'` exits non-zero (RED state — modules absent)
- `cd backend && npm test -- --testPathPattern='queues/onboardingBackwashWorker'` exits 0 (Phase 2 RED test — should now be Phase-2 GREEN — preserved)
- `cd backend && npm run lint:tenant` exits 0 with `src/services/ingest/` in scope
- `services/ingest/orderSourceMobileGps.test.ts` is the dedicated Wave-1-migration verification gate (turns GREEN only after `prisma migrate dev --name add_mobile_gps_order_source` + `prisma generate`)
- No commits modify existing source files outside `package.json` line 17
</success_criteria>

<output>
After completion, create `.planning/phases/06-ingest-adapter-layer/06-00-SUMMARY.md` per `@$HOME/.claude/get-shit-done/templates/summary.md`.
</output>
